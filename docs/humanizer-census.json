[
 {
  "name": "gzh-rewrite-skill (gongzhonghao-rewrite)",
  "stars": 298793,
  "category": "prompt-guide",
  "detector_in_loop": "no — the renwei-writing step runs once at the end as a one-pass editorial checklist. No detector score is computed; there is no rewrite-score-rewrite iteration cycle.",
  "meaning_verification": "The SKILL.md validator checklist includes one manual checkpoint: \"核心观点与案例都源自源文,既没丢也没自行编造\" (verify all core viewpoints and cases survive from source and none were fabricated). This is LLM/human judgmen",
  "fact_preservation": "Prompt instruction only: explicit prohibition \"数据不能编\" (data cannot be fabricated); every number cited in the output must have a real, clicka",
  "mechanism": "Entirely implemented as two Markdown SKILL.md instruction files for the Claude Code skills system — no executable code. The workflow defined in `gongzhonghao-rewrite/SKILL.md` runs in six steps: (1) extract core viewpoints and case studies from the source article into two named lists; (2) web-search for real supporting data/citations (enabled by de"
 },
 {
  "name": "binary-husky/gpt_academic",
  "stars": 71185,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no AI-text detector anywhere in the codebase. The pipeline is single-pass: input text → prompt template → one LLM call → output. No score, no re-score, no iterative loop.",
  "meaning_verification": "none. No cosine similarity, NLI, hedge/numeral checks, or semantic-role comparison. Output is accepted as-is from the LLM.",
  "fact_preservation": "Instruction-only, not programmatic. The LaTeX polishing prompt tells the model \"Do not modify any latex command such as \\section, \\cite, \\be",
  "mechanism": "Prompt-engineered LLM rewriting via predefined prefix/suffix templates. `core_functional.py` defines named functions (academic polish, grammar check, translation) each as a prompt string prepended/appended to user text. Larger plugins in `crazy_functions/Latex_Project_Polish.py` strip LaTeX comments, split oversized files into token-limited chunks "
 },
 {
  "name": "kevintsai1202/Humanizer-zh-TW",
  "stars": 68545,
  "category": "prompt-guide",
  "detector_in_loop": "no — the tool explicitly states \"this tool is not designed to fool AI detectors, but to genuinely improve writing quality.\" No detector is called at any stage.",
  "meaning_verification": "none automated. The SKILL.md instructs the LLM to \"verify natural-sounding output with varied structure\" and apply a 50-point self-scoring rubric, but this is the same LLM doing the rewrite — no indep",
  "fact_preservation": "none. No mention of numeral retention, citation locking, or byte-exact preservation anywhere in the skill or README.",
  "mechanism": "Entirely a SKILL.md prompt file — no executable code. When invoked as a Claude Code / AI agent skill (via /humanizer-zh-tw), the LLM is instructed to act as a text editor, scan input for 24 labelled AI-writing patterns across four categories (content, language/grammar, style, communication), rewrite offending passages, then score the result on a 50"
 },
 {
  "name": "blader/humanizer",
  "stars": 33761,
  "category": "prompt-guide",
  "detector_in_loop": "No. The \"audit\" step is the hosting LLM asking itself the question \"What makes this obviously AI generated?\" — it is not a detector score, not a classifier call, and does not iterate on a numeric sign",
  "meaning_verification": "None mechanical. The skill carries a \"no-fabrication rule\" as a plain instruction: the rewrite may not add any fact, name, number, date, quote, or citation absent from the source. There is no cosine s",
  "fact_preservation": "Instruction-level only. The no-fabrication rule is a prompt directive, not byte-exact preserve-locking. Numbers and citations in the source ",
  "mechanism": "Pure Markdown skill prompt (SKILL.md, under 500 lines by enforced budget). No code transforms text directly. The skill instructs whatever LLM agent loads it to: (1) scan input for 33 documented AI-tell patterns across five categories (content, language/grammar, style, communication, filler/hedging); (2) produce a draft rewrite avoiding those patter"
 },
 {
  "name": "linexjlin/GPTs",
  "stars": 32043,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no re-score, and no iteration. Each prompt produces one output and stops.",
  "meaning_verification": "none — no semantic similarity check, no NLI gate, no entailment, no diffing of any kind.",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling. The Humanizer Pro prompt says \"without changing its meaning\" as a bare in",
  "mechanism": "A static archive of 282 leaked OpenAI custom-GPT system prompts stored as markdown files under prompts/. There is no runnable code. The three humanizer entries work as follows. \"Humanizer Pro\" (prompts/Humanizer Pro.md) instructs ChatGPT to rewrite submitted text \"without changing its meaning\" in a single LLM pass; its knowledge base cites two acad"
 },
 {
  "name": "op7418/Humanizer-zh",
  "stars": 14700,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector anywhere. The skill performs a single-pass edit driven entirely by the pattern catalogue in the prompt. No scoring before or after the rewrite.",
  "meaning_verification": "none — the prompt text says \"保留含义\" (preserve meaning) as a human instruction to Claude, but there is no automated check: no cosine-similarity gate, no NLI entailment, no semantic-role comparison. Whet",
  "fact_preservation": "none — there is no byte-exact locking, no numeral retention check, no citation preservation logic. Numbers and quotes receive no special tre",
  "mechanism": "Pure prompt/instruction file delivered as a Claude Code skill. The entire implementation is SKILL.md — a ~300-line system-prompt that instructs Claude to act as a Chinese-language text editor. It lists 24 AI writing pattern categories with Chinese-language before/after examples (e.g. overuse of dashes, three-part lists, \"-ing\" tail phrases, \"AI voc"
 },
 {
  "name": "huggingface/text-generation-inference",
  "stars": 10888,
  "category": "other",
  "detector_in_loop": "no — TGI has no awareness of AI-text detectors. It accepts a prompt, forwards it to the model, and returns the completion. There is no score-rewrite-re-score loop of any kind.",
  "meaning_verification": "none — TGI is transparent pass-through infrastructure. It neither checks nor preserves meaning; that would have to be implemented in a wrapper built on top of it.",
  "fact_preservation": "none — TGI passes inputs and outputs verbatim. It has no parsing of numbers, citations, or quotes and imposes no constraints on model output",
  "mechanism": "TGI is a Rust/Python/gRPC inference server that routes HTTP requests to a loaded LLM and streams back completions. It does not transform text itself — it is serving infrastructure. The model being served does any transformation; TGI provides batching (continuous batching via router/src/scheduler.rs), tensor parallelism, Flash/Paged Attention kernel"
 },
 {
  "name": "leondz/garak",
  "stars": 8720,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes, but only for jailbreaking the target LLM, not for AI-text detector evasion. TAP (garak/probes/tap.py) uses a judge LLM to score attack effectiveness and prunes the search tree accordingly — a rea",
  "meaning_verification": "none. Garak's goal is to make the target LLM produce undesired outputs, so preserving meaning of a rewritten text is irrelevant. No cosine similarity, NLI, or semantic-role checks anywhere in the code",
  "fact_preservation": "none. Numbers, citations, and quotes have no special handling.",
  "mechanism": "Garak is an LLM red-teaming / vulnerability scanner, not an AI-text humanizer. It transforms *input prompts* sent to a target LLM, not the LLM's output. Transformation happens in two places: (1) Buffs — reusable wrappers applied before a probe fires. `garak/buffs/paraphrase.py` paraphrases prompts with locally-downloaded Pegasus (`garak-llm/pegasus"
 },
 {
  "name": "NomaDamas/k-skill (korean-humanizer)",
  "stars": 7019,
  "category": "prompt-guide",
  "detector_in_loop": "No. There is no external detector called at any stage. The \"audit\" step is the LLM scanning its own output for residual pattern hits from the same A–J catalogue — a heuristic self-check, not a score-t",
  "meaning_verification": "Yes, and it is explicit and layered. Four hard rules (4대 철칙): (1) Meaning invariant — facts, claims, numbers, proper nouns, direct quotes preserved 100%, not a single character changed or invented; (2",
  "fact_preservation": "Explicit Do-NOT list in instruction.md: proper nouns, product names, model names, institution names, personal names, place names; numbers, d",
  "mechanism": "All logic lives in `korean-humanizer/instruction.md` (source read directly) and a 60+ sub-pattern taxonomy in `references/ai-tell-taxonomy.md`. The skill is loaded into an LLM agent via `npx -y @nomadamas/k-skill@0 instruct korean-humanizer`, which prints the instruction for the runtime. The LLM then runs a four-step loop: (0) Triage — decide scope"
 },
 {
  "name": "josStorer/RWKV-Runner",
  "stars": 6457,
  "category": "other",
  "detector_in_loop": "no — there is no detector, no scoring step, and no rewrite loop. The tool only runs inference on demand.",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "RWKV-Runner is a local model management and inference runner for RWKV language models — not a text transformer. It automates downloading, configuring, and launching RWKV model weights, exposes an OpenAI-compatible REST API, and provides a chat/completion WebUI. The Go backend (backend-golang/app.go, rwkv.go) manages process lifecycle and a reverse "
 },
 {
  "name": "microsoft/PyRIT",
  "stars": 4254,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — but for the wrong detector class. PAIR and Crescendo in `pyrit/executor/attack/multi_turn/pair.py` and `crescendo.py` both run a genuine iterate-on-score loop: prompt → target response → objecti",
  "meaning_verification": "none. The unicode/zero-width/encoding converters corrupt or obfuscate text with no downstream check. The LLM-based converters (llm_generic_text_converter.py) make a single LLM call and return the resu",
  "fact_preservation": "none. No numeral retention, citation locking, or quote preservation anywhere in the converter or scoring pipeline.",
  "mechanism": "PyRIT is a red-teaming framework that targets LLM content-safety filters, not AI-text detectors (GPTZero et al.). Its transformation logic lives entirely in `pyrit/converter/`, which contains 80+ pluggable converters. The techniques fall into four families: (1) Unicode trickery — `unicode_confusable_converter.py` replaces Latin letters with visuall"
 },
 {
  "name": "epoko77-ai/im-not-ai (Humanize KR)",
  "stars": 4182,
  "category": "prompt-guide",
  "detector_in_loop": "No. There is no AI-text detector scoring output and feeding back into rewriting. The verification loop is: change-rate gate + structural gate (both deterministic Python scripts, exit-coded). Route sel",
  "meaning_verification": "Substantial, layered. (1) `verify_change_rate.py`: character-level difflib change rate; warns at 30%, blocks at 50%, strips the summary metadata block before comparing. (2) `verify_gates.py`: 4-axis s",
  "fact_preservation": "Yes, explicitly and code-enforced. \"철칙 #1 (Fidelity First)\": facts, claims, numerals, dates, proper nouns, product names, model names, legal",
  "mechanism": "Claude Code skill that orchestrates LLM sub-agents to rewrite Korean AI-generated text. Core flow: (1) a Python input shim (`scripts/prepare_monolith_input.py`) computes 22 quantitative metrics (8 v1.6 heuristics + 14 v2.0 post-editese signals from Korean MT literature, all regex/stdlib — no ML libraries) and emits a `route_hint` (light/standard/he"
 },
 {
  "name": "conorbronsdon/avoid-ai-writing",
  "stars": 2826,
  "category": "prompt-guide",
  "detector_in_loop": "No. The skill's \"second pass\" is a second LLM prompt instruction — the model re-reads its own rewrite for surviving patterns — not a programmatic score-threshold loop. The JS detector engine exists an",
  "meaning_verification": "None. There is no cosine similarity gate, NLI entailment check, numeral retention verification, hedge preservation count, or semantic-role comparison. The skill does instruct the LLM not to edit quote",
  "fact_preservation": "Partial policy only, no enforcement. SKILL.md instructs the agent: \"Don't edit quoted material, code blocks, tables, or text attributed to s",
  "mechanism": "The primary deliverable is SKILL.md — a ~14,000-word markdown prompt file that tells any compatible LLM agent (Claude Code, Cursor, Hermes, Copilot, etc.) how to audit and rewrite text for AI writing patterns. The agent reads the skill, runs a structured audit using the embedded 112-entry tiered word-replacement table and 61 pattern categories, rew"
 },
 {
  "name": "chi111i/BypassAIGC",
  "stars": 2029,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector at any stage. The pipeline is a pure forward pass: segment → polish → enhance → output. No score is computed before or after rewriting, and there is no iteration conditional ",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role matching, no back-translation comparison, no numeral or hedge checks. The prompts instruct the model to stay within ±30 characters of s",
  "fact_preservation": "none — technical terms, code, and API names are listed as \"unchanged\" in the system prompts, but there is no programmatic extraction, lockin",
  "mechanism": "Two-stage LLM rewriting pipeline implemented in `package/backend/app/services/optimization_service.py` and `ai_service.py`. Stage 1 (\"polish\"): sends each text segment to an OpenAI-compatible API using a prompt that targets Nature/Science publication standards; for Chinese text it applies \"dynamic explanation expansion\" via extended verb phrases an"
 },
 {
  "name": "iniwap/AIWriteX",
  "stars": 1787,
  "category": "prompt-guide",
  "detector_in_loop": "no — The README documentation claims \"提供对抗检测分数，优化后续处理工作流\" (provides opposition detection scores to optimize subsequent workflows), but reading the source code finds zero detector integration. `unified",
  "meaning_verification": "none. The `preserve_core_info` config flag, when set, appends the string \"要求：在创意变换中保持文章核心信息不变\" (\"keep core article information unchanged\") to the LLM prompt. That is the entire meaning gate — a natura",
  "fact_preservation": "none. No byte-exact locking, no numeral-retention check, no citation preservation. Numbers, dates, and quotes pass through the LLM rewrite s",
  "mechanism": "AIWriteX is a WeChat official-account content-automation platform whose \"去AI味\" (AI-flavor removal) and Zhuque-bypass features are implemented entirely through prompt engineering, not a separate rewriting model or detector loop. The pipeline is: (1) a CrewAI \"writer\" agent generates an article via whatever LLM the user has configured; (2) optionally"
 },
 {
  "name": "thunlp/TAADpapers",
  "stars": 1577,
  "category": "other",
  "detector_in_loop": "no — the repo contains no executable attack or rewrite logic of any kind",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No text transformation is implemented. The repo is a curated bibliography of 155+ papers organized into categories: sentence-level attacks, word-level attacks (synonym substitution, etc.), character-level attacks, multi-level attacks, defenses, and certified robustness. The only code file is count_paper_number.py, which is a trivial Markdown parser"
 },
 {
  "name": "lynote-ai/humanize-text",
  "stars": 1551,
  "category": "back-translation",
  "detector_in_loop": "yes, but only in Method 3 (detection_pipeline.py), and it is very shallow: the loop runs at most 2 rounds, the \"Binoculars\" detector is a simplified single-GPT-2 perplexity wrapper (not the real obser",
  "meaning_verification": "none — no cosine similarity gate, no NLI check, no semantic-role comparison, no hedge/numeral retention test. The showcase claims \"100% information retention\" but this is stated as human expert assess",
  "fact_preservation": "none — PostProcessor applies regex substitutions on a 30-word dict with no special handling for numerals, citations, or quoted strings. Numb",
  "mechanism": "Two distinct paths. Recommended standard pipeline (src/standard/pipeline.py, run_standard_pipeline): 4-step fixed chain — (1) LLM rewrite English→Chinese via DeepSeek/OpenRouter at temperature 1.3, (2) LLM rewrite Chinese→Japanese carrying step-1 as conversation history, (3) Google Translate Japanese→Finnish, (4) Niutrans Finnish→English. No detect"
 },
 {
  "name": "1e0ng/simhash",
  "stars": 1038,
  "category": "other",
  "detector_in_loop": "no — there is no detector, no rewriter, and no loop of any kind. The library only computes fingerprints and measures similarity between existing texts.",
  "meaning_verification": "none — the library does not transform text so there is nothing to verify. Hamming distance between two SimHash fingerprints is a rough near-duplicate signal, not a semantic meaning check.",
  "fact_preservation": "none",
  "mechanism": "No text transformation whatsoever. The library computes 64-bit locality-sensitive hash fingerprints for text. Pipeline in simhash/__init__.py: Simhash.build_by_text() tokenizes text into 4-character sliding windows (_slide()), hashes each token with MD5 (_hashfunc), aggregates bit vectors via weighted majority vote (build_by_features()), producing "
 },
 {
  "name": "THU-BPM/MarkLLM",
  "stars": 1018,
  "category": "research-code",
  "detector_in_loop": "no — The detection pipeline is strictly linear: edits are applied once, then detection runs once per item. There is no outer closed loop that re-scores after a rewrite and iterates toward a target. Th",
  "meaning_verification": "RandomWalkAttack only: uses the OpenAssistant/reward-model-deberta-v3-large-v2 DeBERTa reward model as a quality oracle (oracle.py). It compares paraphrased vs. original bidirectionally via logit scor",
  "fact_preservation": "none — No specific mechanism protects numbers, citations, or quoted passages from modification. WordDeletion and substitution attacks operat",
  "mechanism": "MarkLLM is primarily a watermark-embedding and watermark-removal research toolkit, not a post-hoc text humanizer. It operates on two distinct planes:\n\n1. Watermark embedding (generation-time): Modifies LLM token logits during decoding to bias toward a \"greenlist\" of tokens (KGW family, e.g. watermark/kgw/kgw.py, KGWLogitsProcessor.__call__) or uses"
 },
 {
  "name": "anasu1/text-humanizer",
  "stars": 792,
  "category": "api-wrapper",
  "detector_in_loop": "no — detector.py is `return round(random.uniform(20, 90), 2)`; all scores shown to users are random numbers. No real detector is invoked and there is no rewrite loop.",
  "meaning_verification": "none — mixed_engine._score_naturalness measures unique-word ratio and word-length variance, which is a fluency proxy, not a semantic equivalence gate. No cosine similarity, NLI, or entailment check an",
  "fact_preservation": "none",
  "mechanism": "Two code paths: (1) src/services/humanizer.py calls an LLM (DeepSeek) via HUMANIZE_PROMPT, with fake detector.score() calls before/after (returns random.uniform(20,90)) — single pass, no loop. (2) src/conversion/mixed_engine.py does sentence-level EN→zh-CN→EN back-translation via deep_translator (GoogleTranslator / MyMemoryTranslator), selecting th"
 },
 {
  "name": "AIScientists-Dev/academic-humanizer",
  "stars": 785,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector of any kind in the loop. The tool is a one-shot LLM rewrite guided by static rules; it never scores output against a detector, never iterates, and never re-scores after rewri",
  "meaning_verification": "none — the SKILL.md instructs the LLM to preserve content semantically, but there is no automated check (no cosine similarity gate, no NLI entailment, no semantic-role match). Preservation is entirely",
  "fact_preservation": "Stated as \"non-negotiables\" in the SKILL.md: never alter numbers, equations, citations, or formal definitions. This is a prompt instruction ",
  "mechanism": "Pure prompt-guide distributed as a single SKILL.md file (~17 KB) with no executable code. The file is loaded as a skill into Claude Code, Codex, or MorphMind; the host LLM then performs all editing. The workflow is four steps embedded in the prompt: (1) read the manuscript and any author samples, (2) audit for AI tells and evidence gaps without edi"
 },
 {
  "name": "speak-human-tw",
  "stars": 752,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector scoring, no re-score loop. A 5-dimension LLM self-assessment (directness, rhythm, trust, authenticity, conciseness) is produced at the end of long-text rewrites, but this is ",
  "meaning_verification": "Partial, LLM-self-check only. Step 5 (保真回讀) requires the model to verify that every original information point, number, fact, and judgment is still present in the output, that no new facts were invent",
  "fact_preservation": "Yes, explicit and detailed. A named \"protected list\" (references/protected-list.md) covers: prices and all numeric data; discount codes and ",
  "mechanism": "Pure prompt-guide / skill file. The deliverable is SKILL.md (~21 KB), a structured instruction set loaded into Claude Code, Codex, Cursor, or OpenCode as a named skill. It tells the hosting LLM to run a six-step pipeline: (1) infer publication context (social post, newsletter, sales page, etc.) and set rewrite intensity accordingly; (2) lock a prot"
 },
 {
  "name": "0xwilliamortiz/humanizer-cli",
  "stars": 580,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. The CLI has no LLM integration whatsoever. The regex checker fires offline rules once and exits; it does not score, rewrite, or iterate. The `prompt` command em",
  "meaning_verification": "none programmatically. SKILL.md instructs the LLM: \"Never invent facts — The rewrite must not contain any fact, name, number, date, quote, or citation that isn't in the source text.\" It also asks the ",
  "fact_preservation": "prompt-instruction only. SKILL.md explicitly bans inventing numbers, dates, quotes, and citations (\"Swapping a vague claim for a specific on",
  "mechanism": "Two-layer design. The outer layer is a Node.js CLI (sources/launch.mjs) that unpacks a native Windows x64 C binary from sources/humanizer.zip and launches it. The C binary is a compiled-in reader for SKILL.md that powers three operations: (1) pattern browser — `show N`, `patterns`, `search` print any of the 33 catalogued AI-tell patterns with befor"
 },
 {
  "name": "DadaNanjesha/AI-Text-Humanizer-App",
  "stars": 413,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no detector call anywhere in the codebase. Transformation is one-shot with fixed random probabilities; no scoring, no iteration.",
  "meaning_verification": "none — SentenceTransformer is used only inside _select_closest_synonym to rank individual word replacements by embedding similarity, not to verify the output sentence or document retains the original ",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special handling; they can be mangled by contraction expansion or synonym replaceme",
  "mechanism": "Single-pass rule-based rewriting implemented in transformer/app.py (class AcademicTextHumanizer, method humanize_text). Per sentence it: (1) expands contractions via a hardcoded dictionary; (2) with probability 0.4 prepends a random academic transition phrase (\"Moreover,\", \"Furthermore,\", etc.) from a fixed 8-item list; (3) optionally converts sent"
 },
 {
  "name": "harshaneel/humanize",
  "stars": 332,
  "category": "prompt-guide",
  "detector_in_loop": "no — README explicitly states \"no detector-in-the-loop; the skill is a rulebook.\" An optional advanced pattern of generating 3-5 variants and picking the lowest-scoring against GPTZero or Binoculars i",
  "meaning_verification": "none — no cosine similarity gate, no NLI entailment check, no semantic-role match, no numeral-retention check. The skill is a pure stylistic pass. The README explicitly acknowledges: \"humanizing form ",
  "fact_preservation": "none — no number locking, no citation preservation, no quote protection. The skill operates entirely at the surface stylistic level.",
  "mechanism": "Two SKILL.md natural-language rulebooks loaded into an LLM agent — no executable code exists. humanize/SKILL.md instructs the agent to apply nine levers in a single-pass rewrite: (1) perplexity — replace predictable verbs with domain-specific ones; (2) burstiness — enforce aggressive sentence-length variance, longest minus shortest >= 20 words; (3)"
 },
 {
  "name": "simonepri/lm-scorer",
  "stars": 312,
  "category": "other",
  "detector_in_loop": "no — the library contains no rewriter, no detector, and no iterative loop of any kind. It is a one-shot scoring utility only.",
  "meaning_verification": "none — the library does not transform text, so meaning verification is not applicable",
  "fact_preservation": "none",
  "mechanism": "Pure sentence SCORER, not a text transformer. Loads GPT-2 variants (gpt2, gpt2-medium, gpt2-large, gpt2-xl, distilgpt2) via HuggingFace Transformers and computes the autoregressive log-probability of each sentence token-by-token using causal LM next-token predictions. Key code: lm_scorer/models/gpt2.py (_tokens_log_prob_for_batch) gathers per-token"
 },
 {
  "name": "devswha/patina",
  "stars": 311,
  "category": "rule-based-rewriter",
  "detector_in_loop": "No. There is no external AI-text detector in the loop. The verify path scores meaning preservation (MPS + fidelity via LLM) not detector confidence. The playground displays a \"deterministic AI signal\"",
  "meaning_verification": "Yes, two layers. (1) Deterministic: `droppedNumbers()` in `src/verify.js` extracts every numeric token from source and rewrite, normalizes thousand-grouped forms, and warns on any that vanish. (2) LLM",
  "fact_preservation": "Partial. Numbers: `droppedNumbers()` checks every numeric token survives the rewrite (normalized for thousands grouping). No byte-exact cita",
  "mechanism": "Two-lane pipeline. Lane A (deterministic, LLM-free): `src/features/index.js` runs `analyzeText()`, combining stylometry (burstiness CV, MATTR via `stylometry.js`), AI-vocab lexicon density (`lexicon.js`), discourse-tell detection (`discourse-tells.js`), translationese signals (`translationese.js`), markup-leakage detection (`markup-leakage.js`), an"
 },
 {
  "name": "Eric-Wallace/universal-triggers",
  "stars": 300,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — The loop scores loss/accuracy on the TARGET NLP MODEL (SNLI classifier, SST sentiment model, SQuAD reader, or GPT-2 language model), not on any AI-text detector. The iteration goal is to flip mod",
  "meaning_verification": "none — The entire point of the method is to CHANGE model outputs (break meaning), not preserve it. No cosine similarity, NLI check, or any other meaning gate exists.",
  "fact_preservation": "none",
  "mechanism": "Gradient-based hotflip attack that searches for short universal trigger token sequences (typically 1–3 tokens) to prepend to any input, causing target NLP models to produce attacker-chosen wrong outputs. The core is in attacks.py (hotflip_attack, random_attack, nearest_neighbor_grad) and utils.py (add_hooks, get_average_grad, get_best_candidates). "
 },
 {
  "name": "ImBD",
  "stars": 266,
  "category": "research-code",
  "detector_in_loop": "no — ImBD IS the detector; there is no rewrite-rescore loop anywhere in the codebase. Text goes in, a single machine-vs-human probability comes out.",
  "meaning_verification": "none — it is a classifier, not a rewriter; the concept is not applicable",
  "fact_preservation": "none — it is a classifier, not a rewriter",
  "mechanism": "ImBD is a DETECTOR, not a humanizer. Training: SPO (Style Preference Optimization), a DPO-variant implemented in spo/utils_spo.py::calculate_SPO_loss, fine-tunes gpt-neo-2.7B with LoRA so the model learns to assign higher log-probabilities to machine-revised text than to human-original text. The preferred/dispreferred pairs are (machine-revised, hu"
 },
 {
  "name": "modu-ai/cowork-plugins (humanize-korean)",
  "stars": 265,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — metrics.py measures Korean linguistic features (comma patterns, sentence-length variance, nominalizer density, conclusion-pivot counts) as a proxy for AI-tell density, but it does not call an AI-",
  "meaning_verification": "LLM self-check only, no external semantic gate. After each edit, the LLM checks 6 items inline: (1) proper nouns/numbers/dates/citations 100% preserved, (2) change rate ≤ 30%, (3) no genre drift, (4) ",
  "fact_preservation": "yes — explicit Do-NOT list enforced across all phases: proper nouns, product/model/institution names, numbers, dates, units, directly quoted",
  "mechanism": "A Claude Code / Cowork skill (SKILL.md instruction set) that directs Claude to apply a 10-category x 40+ pattern substitution rulebook to Korean text. The pipeline is: (1) Phase 2 — run `references/metrics.py` (pure Python, stdlib only) on the input to measure comma usage, sentence-length variance, Chinese-character nominalizer density, conclusion-"
 },
 {
  "name": "redbaronyyyyy-eng/humanizer-zh-academic",
  "stars": 236,
  "category": "prompt-guide",
  "detector_in_loop": "No. There is no real detector called at any stage. The 6-dimensional post-revision quality score includes an \"anti-detection\" dimension, but it is Claude's own self-assessment of whether structural pa",
  "meaning_verification": "No automated gates. The 6-point quality matrix includes \"authenticity\" (reads as genuine scholar voice), \"information density\" (no wasted clauses), and \"academic rigor\" (specific attribution, calibrat",
  "fact_preservation": "Partial and informal. Two hard constraints address attribution: \"vague attribution (unattributed claims) = 0 instances\" and \"unattributed cl",
  "mechanism": "Two markdown files (SKILL.md ~414 lines, SKILL_full.md ~554 lines) installed into Claude Code's skills directory. When invoked, Claude follows a 4-step SOP: (1) risk scan — each paragraph scored against 11 AI-pattern markers, paragraphs scoring ≥4 flagged high-risk; (2) staged rewriting — apply 16 named pattern rules (8 content, 5 language, 3 style"
 },
 {
  "name": "lynote-ai/humanize-text-skill",
  "stars": 230,
  "category": "prompt-guide",
  "detector_in_loop": "no — the SKILL.md default flow is a single pass: detect issues, then rewrite, with one residue check. There is no automated score->rewrite->re-score->iterate cycle. The JS detector can be called indep",
  "meaning_verification": "none — no cosine similarity check, NLI entailment gate, semantic-role match, or hedge-preservation test. The protected-spans pre-check (`references/protected-spans.md`) guards numbers, names, commands",
  "fact_preservation": "yes, at the prompt-instruction level. `references/protected-spans.md` enumerates: numbers/dates/ranges/units (no rounding, no vagueness), na",
  "mechanism": "Two-part architecture. (1) A zero-dependency JavaScript detector library (`detector/patterns.js`, `detector/core/scoring.js`) that scans text for 50+ executable issue types across Chinese and English: tier-1 vocabulary always flagged (AI cliches like \"delve\", \"seamless\", \"赋能\", \"闭环\"), tier-2 flagged in clusters, tier-3 flagged by density, structural"
 },
 {
  "name": "BERT-Attack (LinyangLee/BERT-Attack)",
  "stars": 207,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — but the model in the loop is the TARGET NLP CLASSIFIER (a fine-tuned BERT for sentiment/NLI), not an AI-text detector. After each candidate substitution, tgt_model is queried to see if the label",
  "meaning_verification": "Partial. Counter-fitted embedding cosine similarity >= 0.4 filters antonym substitutions when --use_sim_mat 1 is set. Universal Sentence Encoder (USE) similarity is computed in evaluate() but do_use i",
  "fact_preservation": "None. The entire input is lowercased unconditionally (seq.replace('\\n', '').lower() in _tokenize). Numbers are not excluded from substitutio",
  "mechanism": "Two-phase word-substitution attack implemented in bertattack.py::attack(). Phase 1 (get_important_scores): each content word is masked with [UNK] in turn; the drop in the target classifier's confidence on the original label is the importance score. Words are ranked highest-first. Phase 2: for each high-importance word, BERT MLM (bert-base-uncased) "
 },
 {
  "name": "martiansideofthemoon/ai-detection-paraphrases (DIPPER)",
  "stars": 201,
  "category": "paraphrase-model",
  "detector_in_loop": "no. Detection scripts (`detect_detectgpt.py`, `detect_openai.py`, `detect_gptzero.py`, `detect_watermark.py`, `detect_rankgen.py`, `detect_retrieval.py`) are run after paraphrasing as offline evaluati",
  "meaning_verification": "A semantic similarity submodule lives in `sim/` — an averaging or LSTM sentence encoder trained with a margin-based contrastive loss (`sim/main.py`, `sim/models.py`). The `utils.py` functions `load_si",
  "fact_preservation": "none. No mechanism protects numbers, citations, proper nouns, or quoted strings.",
  "mechanism": "DIPPER is an 11B-parameter T5-v1.1-XXL model fine-tuned specifically for controllable paraphrasing. Implemented in `dipper_paraphrases/paraphrase_minimal.py` (`DipperParaphraser.paraphrase`) and `paraphrase.py`. The pipeline: (1) NLTK `sent_tokenize` splits input into sentences; (2) sentences are processed in sliding windows of 3 (`sent_interval=3`"
 },
 {
  "name": "liamdugan/raid",
  "stars": 201,
  "category": "dataset",
  "detector_in_loop": "no. The pipeline is strictly single-pass: apply attack → write CSV. Detectors are run separately afterward via detect_cli.py / raid/detect.py, and evaluation (raid/evaluate.py) computes TPR/AUROC at a",
  "meaning_verification": "none at the document level. The synonym attacker applies a per-word cosine similarity gate (FastText embeddings, threshold 0.5) and POS-tag match before substituting a single token, but there is no se",
  "fact_preservation": "none. Numbers are actively attacked (alter_number.py randomly replaces digits), citations are not identified or locked, and there is no pres",
  "mechanism": "RAID is a benchmark dataset and evaluation framework, not a pipeline that rewrites text on demand. It ships 12 pre-built adversarial attack implementations used to generate the dataset, each a one-shot, single-pass transformation with no feedback loop:\n\n1. Homoglyph (homoglyph.py): random substitution of Latin characters with visually identical Cyr"
 },
 {
  "name": "kalpeshk2011/dipper-paraphraser-xxl",
  "stars": 201,
  "category": "fine-tuned-model",
  "detector_in_loop": "no — The GitHub repo (martiansideofthemoon/ai-detection-paraphrases) includes detector scripts (detect_watermark.py, detect_openai.py, detect_gptzero.py, detect_detectgpt.py, detect_rankgen.py, detect",
  "meaning_verification": "none — the paper reports BERTScore and uses it as a post-hoc evaluation metric, but the runtime pipeline has no gate that rejects an output if meaning diverged. The user picks control codes upfront an",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or quotes. The order diversity control can reorder sentences, which can move or orphan n",
  "mechanism": "Single-pass seq2seq paraphrase generation. T5-XXL (11B parameters) fine-tuned on PAR3 — a dataset of paragraph-aligned translations from novels — to produce paragraph-level paraphrases. At inference, the prompt is formatted as \"lexical = X, order = Y [optional_prefix] <sent> [sentence] </sent>\", where X and Y are integer control codes (0–100 in mul"
 },
 {
  "name": "google-research/google-research dipper (DIPPER)",
  "stars": 201,
  "category": "paraphrase-model",
  "detector_in_loop": "no — the detect_*.py scripts (detect_detectgpt.py, detect_watermark.py, detect_gptzero.py, detect_openai.py, detect_rankgen.py, detect_retrieval.py) are separate evaluation scripts run independently t",
  "meaning_verification": "none — no cosine similarity check, no NLI entailment gate, no ROUGE/BERTScore threshold, no hedge or numeral retention check. The model is trusted to preserve meaning by virtue of its training; no pos",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special handling. Sentences are tokenized uniformly and passed through the model; t",
  "mechanism": "Fine-tuned T5-XXL (11B parameters) paraphrase model (`kalpeshk2011/dipper-paraphraser-xxl` on HuggingFace). Text is split into sentences via NLTK, then processed in sliding windows (default: 3 sentences at a time). Each window is fed to the model as a prompt in the format `\"lexical = {code}, order = {code} {prior_context} <sent> {window} </sent>\"`,"
 },
 {
  "name": "humarin/chatgpt-paraphraser-on-T5-base",
  "stars": 195,
  "category": "fine-tuned-model",
  "detector_in_loop": "no — single-pass rewriter only; no detector is invoked at any stage and there is no iterate-until-clear loop",
  "meaning_verification": "none — no cosine similarity check, no NLI entailment gate, no semantic-role comparison; correctness of the output is not verified programmatically at any point",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special handling; they pass through the seq2seq decoder like any other token and ca",
  "mechanism": "T5-base (220M-parameter encoder-decoder) fine-tuned on ~419K paraphrase pairs drawn from Quora question pairs, SQuAD 2.0 passages, and CNN news articles. At inference the input is prefixed with the literal string \"paraphrase:\" and passed through T5ForConditionalGeneration. Decoding uses diverse beam search: num_beams=5, num_beam_groups=5, diversity"
 },
 {
  "name": "ilyautov/humanizer-ru",
  "stars": 191,
  "category": "prompt-guide",
  "detector_in_loop": "no — the eval harness (eval/run_eval.py) supports optional external detectors (GPTZero, Originality.ai, a local Ollama model) and an LLM judge that score texts before and after humanization, but they ",
  "meaning_verification": "The skill has a \"Fact-lock\" rule in the prompt (Step 3 in SKILL.md): numbers, dates, names, percentages from the source must be transferred without distortion and no invented facts may be added. This ",
  "fact_preservation": "Prompt-level \"Fact-lock\" rule forbids altering or inventing numbers/dates/names/citations. Not enforced by code during rewriting; only check",
  "mechanism": "A Claude Code skill (SKILL.md loaded into the LLM's context) that instructs the model to act as a Russian-language editor. The skill has no rewriting code of its own — the LLM does all the text transformation in a single pass. The process is: (1) diagnose a text against a 54-pattern catalogue (references/katalog.md), traffic-light each paragraph re"
 },
 {
  "name": "B1lli/remove-ai-flavor-writing-skill",
  "stars": 190,
  "category": "prompt-guide",
  "detector_in_loop": "no — the audit script (audit_ai_flavor.py) is a standalone CLI tool run manually after rewriting; it does not feed scores back to trigger another rewrite pass. There is no iterative score→rewrite→resc",
  "meaning_verification": "none mechanically enforced. SKILL.md includes a checklist item \"Original meaning preserved\" as an instruction to the model, but there is no cosine similarity check, NLI gate, semantic-role comparison,",
  "fact_preservation": "none. Instructions say to preserve facts and tone, but there is no byte-exact locking of numbers, citations, or quoted strings, and no techn",
  "mechanism": "A Codex CLI skill (OpenAI agent framework) that transforms Chinese text by instructing an LLM via a detailed system prompt in SKILL.md. The prompt defines a 5-step workflow: classify text type (social/essay/academic/fiction), scan for pattern categories in order, rewrite locally or at paragraph level, preserve functional lists/contrasts, output rev"
 },
 {
  "name": "NulightJens/humanizer-stack",
  "stars": 178,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no re-score, and no iteration. The pipeline is a linear two-pass rewrite with no detector feedback at any stage.",
  "meaning_verification": "none — the SKILL.md says \"Preserve meaning\" as a prose instruction to Claude, but there is no automated gate (no cosine similarity, no NLI entailment, no semantic-role check, no numeral audit).",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or quotes. \"Preserve meaning\" is an informal instruction to the model only.",
  "mechanism": "Two-pass Claude Code skill pipeline. Pass 1 (skills/humanizer/SKILL.md): a detailed prompt catalogue of 24 named surface AI-writing patterns (em-dash overuse, rule-of-three, negative parallelism, AI vocabulary, sycophancy, etc.) instructs Claude to rewrite text matching those patterns. Pass 2 (skills/structural-humanizer/SKILL.md): a second prompt-"
 },
 {
  "name": "miyyer/scpn",
  "stars": 168,
  "category": "research-code",
  "detector_in_loop": "no — pure feed-forward generation; no detector is ever consulted, no score is computed, no iteration loop exists",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no any other semantic check is applied to the output",
  "fact_preservation": "none — numbers, citations, and named entities receive no special treatment; the beam decoder can freely alter or drop them",
  "mechanism": "Syntactically Controlled Paraphrase Network: two-stage neural seq2seq pipeline. (1) A parse generator network (train_parse_generator.py / models/parse_generator.pt) takes the input sentence's leaf-stripped parse tree (computed by deleaf() in scpn_utils.py via Stanford CoreNLP Shift-Reduce Parser) and a target syntactic template (e.g. ROOT(S(SBAR)(,"
 },
 {
  "name": "matsuikentaro1/humanizer_academic",
  "stars": 158,
  "category": "prompt-guide",
  "detector_in_loop": "no — The README documents offline experiments with desklib and Binoculars detectors used to validate pattern importance during development (specifically crediting Pattern 34 sentence rhythm with \"~90%",
  "meaning_verification": "none automated. The skill instructs Claude to \"preserve meaning\" and \"maintain data integrity (numbers, statistics, findings)\" but provides no NLI entailment gate, no cosine similarity check, and no n",
  "fact_preservation": "instructional only. Pattern 3 and Pattern 11 examples show statistics being retained verbatim in rewrites, and the skill explicitly says to ",
  "mechanism": "A Claude Code SKILL.md instruction file (748 lines, no executable code). The transformation is performed entirely by Claude (the LLM) following 34 named rewriting rules documented in SKILL.md. Rules cover: content-inflation removal (Patterns 1–6), AI vocabulary substitution (Patterns 7–12, 19–26), style normalization such as zero-tolerance em dash "
 },
 {
  "name": "smixs/humanizer-ru",
  "stars": 128,
  "category": "prompt-guide",
  "detector_in_loop": "No. The loop is: LLM edit → lint.py regex check → fix ERRORs → re-lint. lint.py is a rule-based linter, not an AI-text detection scorer. There is no call to a detector model, no detection score that g",
  "meaning_verification": "Partial, instruction-based. The skill explicitly forbids inventing facts: numbers, names, quotes, proper nouns from the source must survive verbatim; if a required fact is missing the LLM is told to l",
  "fact_preservation": "Numbers and named quotes are protected by explicit SKILL.md rule and enforced by evals (127 checklist items test survival of specific figure",
  "mechanism": "A Claude Code agent skill (prompt + deterministic linter) for Russian text. Three phases: (1) Audit — the LLM reads references/patterns.md (37 named AI-tell patterns) and builds a findings table of exact quotes with pattern numbers and prescribed fixes. (2) Edit — the LLM applies only the flagged findings using a fixed hierarchy: delete > replace w"
 },
 {
  "name": "obaskly/AiTextDetectionBypass",
  "stars": 125,
  "category": "api-wrapper",
  "detector_in_loop": "no — The scanner tab (ai_scanner.py) and the paraphraser (paraphraser.py) are entirely decoupled. The user must manually invoke the scanner; there is no automated score-then-rewrite-then-rescore itera",
  "meaning_verification": "none — The README disclaimer explicitly states the script is \"NOT responsible for the quality or accuracy of the output.\" No cosine similarity, NLI, semantic-role, or any other semantic check is prese",
  "fact_preservation": "none — Text is chunked at 250-word word-count boundaries with no special handling for numbers, citations, or quoted strings. The only text t",
  "mechanism": "Selenium browser automation (paraphraser.py, main()) that drives Chrome in incognito mode to undetectable.ai/ai-humanizer. Text is chunked to 250-word segments (split_text_preserve_sentences from utils/text_splitter.py or naive word-split). Each chunk is pasted into the site's textarea, tone/readability/purpose dropdowns are set, and the \"Humanize\""
 },
 {
  "name": "DaleSeo/korean-skills",
  "stars": 125,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite only. The tool instructs the agent to detect patterns and correct them in one pass; it does not call a detector, score output, or iterate. The SKILL.md says to \"self-validate ",
  "meaning_verification": "Soft/prompt-only. SKILL.md instructs the agent to \"preserve meaning — never alter facts, dates, proper nouns, or logical relationships\" and to assign a Naturalness Grade (A/B/C/D) post-revision. No co",
  "fact_preservation": "Prompt instruction only: \"never alter facts, dates, proper nouns.\" No byte-exact locking, no numeral retention gate, no regex preserve-lock ",
  "mechanism": "Pure prompt-instruction bundle — no executable code. The repo contains markdown files (SKILL.md + six reference docs per skill) that are loaded into whichever AI agent (Claude Code, Cursor, Windsurf, Copilot) the user installs them into. The humanizer skill encodes 40 Korean AI-writing patterns across 6 categories (punctuation, spacing, POS diversi"
 },
 {
  "name": "gonta223/humanizer-ja",
  "stars": 121,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, no re-score, no iteration. One-shot rewrite only.",
  "meaning_verification": "none — SKILL.md says \"never just remove, replace with human-like expression\" but this is advice to the LLM, not an automated gate. No cosine similarity check, no NLI pass, no semantic-role comparison.",
  "fact_preservation": "none — no mention of number locking, citation preservation, or quote protection anywhere in the repo.",
  "mechanism": "Pure Claude Code skill — the entire repo is two markdown files (README.md, 2.3 KB; SKILL.md, 10 KB) and a LICENSE. SKILL.md is a 20-pattern checklist partitioned into five categories: (1) vocabulary/expression (remove overwrought emphasis, replace generic evaluative phrases like 浮き彫りにしており, reduce katakana overuse, demand concrete sources); (2) form"
 },
 {
  "name": "ai-zixun/humanizer-zh",
  "stars": 109,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, no iteration. The skill is a single-pass LLM rewrite driven entirely by instructional rules. The final-check section in SKILL.md is a human-readable checklist fo",
  "meaning_verification": "none — the final-check checklist in SKILL.md asks the model to confirm that the first paragraph's question is answered at the end and that body paragraphs stay on-topic, but this is a natural-language",
  "fact_preservation": "none automated — SKILL.md Rule 5 says \"不擅自补充事实\" (do not add facts) and \"保留作者原意\" (preserve original intent) as instructional prose, but there",
  "mechanism": "Pure LLM-prompting via a Claude Code / Codex skill. The repo contains no executable code. SKILL.md is the runtime entry point: it gives the LLM a 8-rule Core Rules section (targeting Chinese-specific AI tells: translation-speak, mechanical contrast sentences, buzzwords, sloganised endings, list inflation, punctuation misuse, loose article structure"
 },
 {
  "name": "zero-zerogpt",
  "stars": 108,
  "category": "unicode-trickery",
  "detector_in_loop": "no — there is no scoring step, no iteration, and no feedback. The tool produces all 12 Unicode-space variants simultaneously in one pass and lets the user pick one to copy or download.",
  "meaning_verification": "none — text content is completely unchanged; only the space codepoints differ. Meaning is trivially preserved by construction but is never verified.",
  "fact_preservation": "incidental — numbers, citations, and quotes are preserved because the tool only touches space characters, not any other content. There is no",
  "mechanism": "Pure Unicode space substitution with no text rewriting. The entire transformation is a single function in src/App.js: `replaceSpaces = (text, unicodeCharacter) => text.split(' ').join(unicodeCharacter)`. Every ASCII space (U+0020) in the input is replaced with one of 12 Unicode space characters defined in src/constants/unicodeSpaces.js — Em Space ("
 },
 {
  "name": "brandonwise/humanizer",
  "stars": 108,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — scores once, emits suggestions, stops. There is no re-score after applying fixes, no iteration, and no convergence criterion. The autofix path in humanizer.js applies transforms and returns immed",
  "meaning_verification": "none — no cosine similarity, no NLI entailment check, no round-trip back-translation, no semantic-role comparison. The autofix substitutions are short phrase contractions unlikely to destroy meaning, ",
  "fact_preservation": "none — numbers, citations, and quotes are not tracked or locked. The autofix does not touch numerals, but there is no byte-exact preserve-lo",
  "mechanism": "Two-phase pipeline, both implemented in JavaScript with no LLM. Phase 1 (src/analyzer.js): runs 30 regex/vocabulary pattern detectors (src/patterns.js, 500+ vocabulary terms) plus statistical analysis (src/stats.js: burstiness, type-token ratio, trigram repetition, sentence CoV, Flesch-Kincaid). Phase 2 (src/humanizer.js): `autoFix()` applies safe "
 },
 {
  "name": "crabin/paper-humanizer-skill",
  "stars": 103,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, no re-scoring, and no iteration. Single LLM call only.",
  "meaning_verification": "none (mechanical). The system prompt instructs the LLM not to alter meaning, facts, numbers, or citations, but there is no automated check (no cosine similarity gate, no NLI pass, no diff or assertion",
  "fact_preservation": "Instruction-based, not mechanical. The system prompt states \"never change numeric values, hyperparameters, thresholds, or comparative outcom",
  "mechanism": "Pure prompt-composition tool. The Python script `scripts/paper_humanizer.py` assembles a system prompt (`references/system_prompt.md`) and a parameterised user template (`references/user_template.md`) into a single composed prompt string, then prints it to stdout. Nothing else happens in code — the composed prompt is fed to Claude (via the Claude C"
 },
 {
  "name": "AgentGuard (numbergroup/AgentGuard)",
  "stars": 102,
  "category": "other",
  "detector_in_loop": "no — single-pass analysis; no score→rewrite→re-score loop exists anywhere in the code",
  "meaning_verification": "none — the tool is not trying to preserve meaning during rewriting; it strips attack patterns and is indifferent to whether the remaining text is semantically intact",
  "fact_preservation": "none — not applicable; the tool does not rewrite factual content",
  "mechanism": "Pure pattern-based detection and sanitization — no text humanization or evasion generation. The pipeline in agent_guard.py applies: (1) NFKC Unicode normalization, (2) combining-character stripping, (3) zero-width character removal (U+200B, U+200C, etc.), (4) homoglyph-to-ASCII mapping for 50+ Cyrillic/Greek/math lookalikes (e.g. Cyrillic 'а'→'a', "
 },
 {
  "name": "aloshdenny/reverse-SynthID-text",
  "stars": 95,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes, but weakly — WatermarkDetector.compute_score() runs SynthID's own g-value calculation before and after the attack and prints the mean-g score; however there is NO iteration loop (no rescore-and-r",
  "meaning_verification": "none. The paraphrase path trusts the LLM to preserve meaning by prompt instruction only (\"Paraphrase while keeping the same meaning\"). No cosine similarity check, no NLI gate, no semantic-role compari",
  "fact_preservation": "none. No special handling for numbers, citations, or quoted strings. The synonym table does not filter numerals. Paraphrasing model may free",
  "mechanism": "Five independent attack classes in reverse_synthid.py, each targeting SynthID's n-gram hash mechanism specifically. (1) ParaphrasingAttack: loads a non-watermarked HuggingFace model (default google/gemma-2b-it) and fully regenerates the text from a prompt, destroying the token-sequence hash patterns SynthID relies on. (2) TokenPerturbationAttack: h"
 },
 {
  "name": "gpthuman/ai-humanizer",
  "stars": 95,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector call, no rewrite, and no iteration of any kind in the published source",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "The Space is a pure freemium funnel, not a functioning humanizer. app.py (18.8 kB) is entirely Gradio UI scaffolding. The humanize() function sleeps for 3 seconds, HTML-escapes the input unchanged, and returns it hidden behind a blurred overlay. A call-to-action overlay tells the user their text is \"100% undetectable\" and prompts account creation a"
 },
 {
  "name": "vinusankars/Reliability-of-AI-text-detectors",
  "stars": 92,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — detection is a separate offline evaluation step. `rephrase/detect.py` and `rephrase/eval.py` score outputs after paraphrasing is complete; no detector score is fed back to influence paraphrase de",
  "meaning_verification": "none — the system applies no semantic similarity check, NLI gate, or any other post-hoc meaning gate. The Parrot model's internal adequacy/fluency thresholds (lowered progressively via the \"knob\") are",
  "fact_preservation": "none — no special handling of numbers, citations, or quoted material.",
  "mechanism": "Three distinct attack vectors, all open-loop (paraphrase first, evaluate separately). (1) Recursive paraphrasing via DIPPER (kalpeshk2011/dipper-paraphraser-xxl, a T5-xxl model): `rephrase/recursive_paraphrase.py` runs P=5 rounds, each round feeding the prior output as the next input, sentence-windowed with lex_diversity=60 and order_diversity=60 o"
 },
 {
  "name": "Vladimir-Human/humanizer-ru",
  "stars": 91,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no score-rewrite-re-score feedback loop. The decision tree is one-shot: detect patterns, then rewrite. blind_eval.py is a post-hoc evaluation tool that compares outputs after the fact, n",
  "meaning_verification": "Narrow fact-preservation rule only: the six core editing principles in SKILL.md prohibit introducing numbers, dates, names, units, or causal links not in the source (\"no new facts\"). check_examples.py",
  "fact_preservation": "Yes, explicitly: SKILL.md principle #6 forbids adding facts absent from the source. The check_examples.py script enforces this for before/af",
  "mechanism": "An agentskills.io skill — primarily a structured prompt document (SKILL.md) that an LLM agent (Claude, opencode, etc.) follows, not a standalone pipeline. Transformation logic: (1) SKILL.md's decision tree routes by genre (fiction/contract/academic/blog) and text date; (2) check_markers.py / chatbot-artifacts.md runs 38 regex patterns (class A = hi"
 },
 {
  "name": "jthickstun/watermark",
  "stars": 88,
  "category": "research-code",
  "detector_in_loop": "no — the watermark is embedded at token-sampling time during generation; there is no post-hoc rewrite-and-re-score loop of any kind",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Generation-time keyed pseudorandom watermarking. Two strategies are implemented under watermarking/gumbel/ and watermarking/transform/. Gumbel strategy (gumbel/sampler.py): at each generation step, token is chosen by argmax(xi^(1/p)) where xi is a seeded random matrix drawn from a keyed generator — this is inverse-transform Gumbel sampling tied to "
 },
 {
  "name": "JHL-HUST/PWWS",
  "stars": 85,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — but the \"detector\" is the specific target text classifier being attacked (word-CNN, Bi-LSTM, LSTM, or char-CNN trained on sentiment/topic datasets), not an AI-text detector. After each synonym s",
  "meaning_verification": "minimal. The synonym filter (_synonym_prefilter_fn in paraphrase.py) enforces same POS tag and rejects multi-word synonyms and identity substitutions. Candidates are ranked by context-window spaCy vec",
  "fact_preservation": "none for numbers, citations, or quotes. CARDINAL and ORDINAL NER tags are in the NE substitution pool, meaning numeric expressions are candi",
  "mechanism": "Word-level synonym substitution guided by two signals combined multiplicatively. Step 1 (evaluate_word_saliency.py: evaluate_word_saliency()): for every token, zeroes its embedding vector and measures how much the target classifier's confidence in the true class drops — this is word saliency S(x, w_i). Step 2 (paraphrase.py: PWWS(), adversarial_too"
 },
 {
  "name": "StealthHumanizer (rudra496/StealthHumanizer)",
  "stars": 84,
  "category": "api-wrapper",
  "detector_in_loop": "yes — the local heuristic detector (detectAI from lib/detector.ts) is called between passes; sentences scored \"ai\" or \"maybe\" are re-humanized in-place, up to 3 passes total. However the in-loop detec",
  "meaning_verification": "Computed but not a blocking gate. lib/semantic-fidelity.ts — assessSemanticFidelity() — produces a weighted score from unigram/bigram cosine similarity, keyword recall, length ratio, sentence-count al",
  "fact_preservation": "Structural regions (fenced code, inline code, markdown images, markdown links, bare URLs, email addresses, @-mentions, hashtags, blockquotes",
  "mechanism": "Four-layer pipeline orchestrated in lib/humanizer.ts. Layer 1: LLM rewrite via generateWithProvider() using anti-detection system prompts from lib/prompts.ts (length anchors, style/tone/domain configuration, burstiness and perplexity injection instructions). Layer 2: deterministic post-processing in lib/postprocess.ts — 500+ synonym swaps, 230+ col"
 },
 {
  "name": "marmbiz/humanizer-de",
  "stars": 80,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — The linters are the \"detector\" here; they flag AI-writing patterns rather than computing a probability score. The QGIR loop iterates on linter findings, not on any external detector (GPTZero, etc",
  "meaning_verification": "Substantial. `evidence_lint.py` extracts \"anchor\" spans (numbers, dates, URLs, DOIs, legal paragraph references, inline code, quoted strings) from the original text into a versioned JSON ledger (`--wr",
  "fact_preservation": "Strong. `evidence_lint.py` has dedicated regex patterns for numbers (with German unit suffixes: %, Prozent, €, Euro, km, kg, Mio., Millionen",
  "mechanism": "German-specific deterministic linter suite plus LLM rewriter, wired as a Claude Code / Codex agent skill. Pass 0 runs `scripts/humanizer_audit.py` (which orchestrates `german_pattern_lint.py`, `rhythm_lint.py`, `register_lint.py`, `unicode_lint.py`, `syntax_lint.py`). The linters fire regex-based detectors against a 72-pattern German AI-writing cat"
 },
 {
  "name": "lengsukq/ParaphrasingToolClient",
  "stars": 78,
  "category": "api-wrapper",
  "detector_in_loop": "No. The AIGC probability scores shown in the UI are parsed directly from an already-generated PaperYY HTML report uploaded by the user — this tool never calls a detector itself. The workflow is: user ",
  "meaning_verification": "None. The LLM prompt instructs the model to \"maintain the same meaning,\" but no automated check (cosine similarity, NLI, semantic-role matching, or any other gate) verifies whether the output actually",
  "fact_preservation": "None. Numbers, citations, and quotes receive no special handling; they pass through the LLM prompt unchanged and can be freely altered by th",
  "mechanism": "Single-shot LLM paraphrasing via an OpenAI-compatible API. The tool has two phases: (1) client-side HTML parsing of uploaded plagiarism/AIGC detection reports (PaperYY, DaYa formats) using a CSS-selector rule engine in src/lib/rule-parser.ts, extracting flagged text segments; (2) each segment is sent to an LLM via src/composables/useParaphrase.ts →"
 },
 {
  "name": "IBM/RADAR",
  "stars": 76,
  "category": "detector-with-evasion",
  "detector_in_loop": "no — The notebook is strictly sequential: generate AI text, paraphrase once, score with detector. The three stages are independent cells with no feedback. There is no re-score-and-iterate cycle anywhe",
  "meaning_verification": "none — No cosine similarity, no NLI check, no BLEU, no semantic-role gate. The paraphrased text is used directly with no validation that meaning survived.",
  "fact_preservation": "none — Numbers, citations, and quotes receive no special handling. The GPT prompt asks only for word-choice enhancement; there is no preserv",
  "mechanism": "RADAR is primarily a robust AI-text DETECTOR (RoBERTa-large fine-tuned via joint adversarial training against a paraphraser), with a companion evasion demo. The repo ships no training code — only inference. The evasion side is a single-shot paraphrase: either a GPT-3.5-turbo/GPT-4 API call with the fixed system prompt \"Enhance the word choices in t"
 },
 {
  "name": "GPTZzzs",
  "stars": 72,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — single-pass transformation with no scoring, no detector call, and no iteration. The output is produced once and written to output.txt. There is no feedback mechanism of any kind.",
  "meaning_verification": "none — words are replaced by a random entry from a thesaurus list with no context check. The README explicitly warns that \"synonyms can be very odd and would require fixing by humans.\" No similarity m",
  "fact_preservation": "Partial quote protection only: the ignore_quotes flag (default True) tracks double-quote parity and skips swapping tokens that fall inside a",
  "mechanism": "Two-pass rule-based text mutation, no LLM or API involved. All logic lives in src/gptzzzs/normal_change.py (change_text function) and src/gptzzzs/__init__.py (Gptzzzs class). Pass 1: randomly selects N% of whitespace-delimited tokens (default 50%), strips trailing punctuation, checks word length > 3 and presence in a bundled JSON synonym dictionary"
 },
 {
  "name": "ksanyok/TextHumanize",
  "stars": 71,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — two separate loops. (1) humanize_until_human() in texthumanize/core.py runs the full pipeline, scores with the internal three-layer detector ensemble (heuristic 18-metric, 35-feature logistic re",
  "meaning_verification": "Weak structural checks only. SentenceValidator™ (texthumanize/sentence_validator.py) fires at 7 pipeline checkpoints and catches surface artifacts: duplicate adjacent words, broken contractions, orpha",
  "fact_preservation": "Yes, explicit. Segmentation stage protects URLs, emails, code blocks, Markdown/HTML markup, named entities (multi-token), and explicit keywo",
  "mechanism": "38-stage adaptive pipeline implemented across ~131 Python modules (orchestrated in texthumanize/pipeline.py). Stages run in order: watermark scrubbing (zero-width, homoglyphs, invisible Unicode) → segmentation with URL/code/email protection → typography normalisation → debureaucratisation → structure diversification → repetition reduction → livelin"
 },
 {
  "name": "ColinLu50/Evade-GPT-Detector",
  "stars": 70,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — the proxy detector is called inside _optimize_ic_outputs via a _human_score closure that calls self.detector(text_list) and returns 1 - AI_prob. Candidate in-context examples are ranked by this ",
  "meaning_verification": "none — no cosine similarity gate, no NLI check, no semantic-role match, no numeral retention check. The only optimization signal is the detector score. Semantic preservation of in-context examples is ",
  "fact_preservation": "none — word and sentence substitutions can replace anything in the in-context examples; numbers and proper nouns are not locked.",
  "mechanism": "SICO (Substitution-based In-Context example Optimization). Two-phase: (1) Training — the LLM is asked to extract a \"feature\" text describing differences between human and AI outputs (extract_feature in incontext_trainer.py), then that feature is used to paraphrase AI outputs into human-sounding in-context examples (construct_incontext_outputs). Tho"
 },
 {
  "name": "hyunwoongko/asian-bart",
  "stars": 70,
  "category": "paraphrase-model",
  "detector_in_loop": "no — no detector, no scoring loop, no iteration of any kind",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Thin wrapper around Hugging Face MBartForConditionalGeneration for multilingual (English/Chinese/Japanese/Korean) seq2seq generation. The entire modeling_asian_bart.py file is empty stub classes: `class AsianBartForConditionalGeneration(MBartForConditionalGeneration): pass` — no logic added whatsoever. The tokenizer similarly wraps MBart tokenizati"
 },
 {
  "name": "nihe0909/zh-humanizer-literary",
  "stars": 67,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, no re-score, and no iteration loop of any kind. The skill is a single LLM call guided by prompt rules.",
  "meaning_verification": "none — the rules say \"protect facts\" as priority 1 and the SKILL.md mentions preserving factual accuracy, but there is no automated gate, cosine check, NLI entailment, or any other mechanical verifica",
  "fact_preservation": "Rule-based guidance only: the priority framework in anti-ai-rules.md lists \"protect facts\" first, and the writing-standard.md forbids fabric",
  "mechanism": "A Codex skill (OpenAI agent wrapper) composed entirely of markdown instruction files. The openai.yaml agent config passes the skill documents as context to an LLM with a default system prompt: \"apply zh-humanizer-literary to remove AI taste, elevate literary quality, preserve facts and author voice.\" The actual transformation logic lives in four re"
 },
 {
  "name": "sanjaysah101/humanize-ai",
  "stars": 62,
  "category": "api-wrapper",
  "detector_in_loop": "no — The `src/services/verification/authenticityVerifier.ts` produces a weighted heuristic score (language 0.3, style 0.3, pattern 0.2, emotional 0.2) using Compromise NLP features and the `PatternDet",
  "meaning_verification": "Minimal. After the LLM call, `transformerModel.ts` computes a combined score from weighted Levenshtein distance and a \"semantic overlap\" measure; if it falls below 0.2 (a very loose floor) the code re",
  "fact_preservation": "Partial and shallow. `wordLevel.ts` skips tokens matching `^\\d+$` (bare integers) and ALL-CAPS tokens. There is no byte-exact preserve-locki",
  "mechanism": "Three-layer pipeline in a Next.js web app. (1) Word-level synonym replacement: `src/services/transformation/wordLevel.ts` uses the Datamuse API via `src/infrastructure/apis/datamuse.ts` to fetch candidates, ranks them with POS matching and contextual scoring via `src/services/transformation/synonymSelector.ts`, and skips pure numerals (`^\\d+$`), AL"
 },
 {
  "name": "zhouying20/HMGC",
  "stars": 60,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — via surrogate only. The surrogate RoBERTa classifier is in the loop as the goal function; the search iterates until the surrogate flips from AI to human. The live victim detector is never querie",
  "meaning_verification": "Cosine similarity constraint via Universal Sentence Encoder (threshold 0.75, window_size=50) is enforced on every candidate swap during the attack — candidates that fall below the threshold are discar",
  "fact_preservation": "none. Word substitutions are applied to any non-stopword position including numerals, proper nouns, and quoted text. There is no preserve-lo",
  "mechanism": "Two-stage adversarial word-substitution attack built on top of the TextAttack and TextFlint frameworks. Stage 1 (train_detector.py): fine-tune a RoBERTa surrogate detector that mimics the target (\"victim\") AI-text detector by distilling its labels. Stage 2 (attack/multi_flint_attack.py + attack/recipes/rspmu_mlm_dualir.py): run a greedy dual Word I"
 },
 {
  "name": "Text2Go/ai-humanizer-mcp-server",
  "stars": 58,
  "category": "api-wrapper",
  "detector_in_loop": "no — the only tool is detect; there is no rewrite tool, no scoring loop, and no iteration of any kind",
  "meaning_verification": "none — no rewriting occurs in this codebase",
  "fact_preservation": "none — no rewriting occurs in this codebase",
  "mechanism": "A single TypeScript file (src/index.ts) registers one MCP tool called \"detect\". It POSTs text to https://api.edgeshop.ai/rewrite/text-detection with a list of detector names (COPYLEAKS, HEMINGWAY). The tool's own description string instructs the host LLM to build a URL from the returned taskId and show it to the user (https://pre-www.text2go.ai/?ut"
 },
 {
  "name": "HairDiverLive/undetectable-browser-helper-toolkit",
  "stars": 57,
  "category": "other",
  "detector_in_loop": "no — no code of any kind is present",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No source code exists. The repository contains only a README.md (1504 bytes) that describes a generic \"productivity platform\" and links to an external download at undetectable-browser-helper-toolkit.softhvn.xyz. The GitHub topics include discordtokengrabber, grabber-fud, digispark-payload, and discord-logger alongside the ai-humanizer tags. The rep"
 },
 {
  "name": "Squirbie/im-not-ai-codex",
  "stars": 55,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — in strict mode the naturalness-reviewer agent re-runs the full tell-detector on the rewritten output and compares severity-weighted scores against the original detection JSON. If S1 patterns rem",
  "meaning_verification": "13-item checklist enforced by the content-fidelity-auditor agent (references/agents/content-fidelity-auditor.md): absolute-immutable items — proper nouns, numbers/units, dates/times, direct quotes, le",
  "fact_preservation": "Explicit byte-exact Do-NOT list applied at every stage: numbers/units/dates, proper nouns (names, products, models, institutions), content i",
  "mechanism": "LLM-prompt-driven rewriting with regex-based quantitative pre-scoring. Before calling the LLM, `plugins/im-not-ai/scripts/prepare_monolith_input.py` runs `metrics_v2.py` (falling back to `metrics.py`) to compute 8+14 quantitative signals — comma-inclusion rate, connective-ending-before-comma rate, hanja nominalizer density (-성/-적/-화), lexical diver"
 },
 {
  "name": "conversantech/humanizer-ai",
  "stars": 55,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — the code computes internal proxy metrics (perplexity and burstiness) and semantic similarity as feedback, but never calls ZeroGPT, GPTZero, or any real detector. The \"0% AI Detection\" UI claim is",
  "meaning_verification": "partial — `final_quality_check()` checks cosine similarity of `all-MiniLM-L6-v2` embeddings against a 0.7 threshold; falls back to Jaccard overlap. Flesch readability is also checked. No NLI entailmen",
  "fact_preservation": "none — no number, citation, or quote protection of any kind. No preserve-locking mechanism.",
  "mechanism": "Five-pass pipeline in a single `app.py` class `AdvancedAIHumanizer`. Pass 1 (`replace_ai_patterns`): regex replacement of 60+ AI-flagged phrases (\"delve into\", \"facilitate\", \"furthermore\", etc.) with casual alternatives. Pass 2 (`restructure_sentences`): adverb relocation, compound-sentence splitting, passive-voice changes. Pass 3 (`enhance_vocabul"
 },
 {
  "name": "OUTFOX",
  "stars": 53,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes, but offline not real-time. The loop is: run detector on training essays -> label each as Good/Bad -> store labels -> use stored labels as in-context examples for next attacker generation round. T",
  "meaning_verification": "none. No cosine similarity check, no NLI gate, no semantic-role comparison. The code validates nothing about the generated essay beyond parsing the ChatGPT response string.",
  "fact_preservation": "none. No numeral retention, no citation locking, no quote preservation of any kind.",
  "mechanism": "Dual adversarial in-context learning system. The attacker (src/attacking/outfox_attacking.py) builds a 10-shot prompt for ChatGPT that embeds pre-labelled LM-generated essays: examples where detection was fooled are labelled \"Good\", examples that were caught are labelled \"Bad\". The `make_prompt_for_attack` function (utils/utils.py) assembles this p"
 },
 {
  "name": "ZAYUVALYA/AI-Text-Humanizer",
  "stars": 50,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector, no scoring, no iteration. Input goes to the API once and the result is returned. No feedback loop of any kind.",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes pass through the downstream LLM with no guards or byte-exact locking",
  "mechanism": "Single-pass browser tool. The live code in script.js constructs a fixed prompt (\"vary sentence structures, eliminate repetitive phrases, reduce passive voice...\") and sends it as a URL-encoded GET request to https://text.pollinations.ai/, a free public LLM inference endpoint. Pollinations.ai runs the actual rewrite on its own backend (model unspeci"
 },
 {
  "name": "humanizer-zh-next",
  "stars": 48,
  "category": "prompt-guide",
  "detector_in_loop": "no — The three-step workflow (draft → self-check → final) is LLM introspection only. The self-check step asks the model \"what still looks like AI here?\" in natural language; there is no detector score",
  "meaning_verification": "none in a programmatic sense. The prompt instructs \"保留原意\" (preserve core meaning) and contains a 6-row genre decision matrix that constrains fact-addition per genre (e.g., academic/legal genre: 严格禁止新增",
  "fact_preservation": "Prompt-level only. The genre matrix instructs that for technical/README content 不得新增功能、参数、性能数据 (no new features, parameters, or performance ",
  "mechanism": "Pure prompt/skill file (SKILL.md, ~54 KB). The repo contains no executable code — only markdown. It installs as a Claude Code (or any-agent) skill that injects an LLM persona: a Chinese-language copy-editor who scans text against a 33-pattern AI-tell catalogue and rewrites using a three-step loop (通读原文 → 写初稿 → 自检\"是什么让初稿还像AI\" → 终稿). The 33 patterns "
 },
 {
  "name": "chengez/Adversarial-Paraphrasing",
  "stars": 46,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — the detector is called at every single token step, not just between full rewrites. For each of the top-k candidate next tokens the partial output is scored and the least-AI-looking token wins. T",
  "meaning_verification": "none programmatic. The LLM system prompt instructs \"rephrase without changing meaning\" but there is no cosine, NLI, or any automated gate checking that meaning survived. GPT-4o quality evaluation is p",
  "fact_preservation": "none. No numeral-locking, citation-locking, or any byte-exact preservation mechanism exists in the codebase.",
  "mechanism": "Token-level adversarial decoding, implemented in `utils.py` in the `Paraphraser.paraphrase()` method. At every autoregressive generation step the model produces top-k candidate tokens; each candidate is decoded into a partial string (generated-so-far + new token) and fed to the guidance detector (`classifier.get_scores()`). The token whose partial "
 },
 {
  "name": "cookielee77/CLARE",
  "stars": 44,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — but the \"detector\" is the target NLP classifier being attacked (a BERT or TextCNN sentiment/NLI classifier), not an AI-text detector. The attack loop in bert_attack_classification.py queries cla",
  "meaning_verification": "USE (Universal Sentence Encoder) cosine similarity via TensorFlow Hub (models/similarity_model.py). Each of the three operations calls similarity_calculation() and rejects candidates below a per-opera",
  "fact_preservation": "none. No special handling for numbers, citations, or quotes.",
  "mechanism": "Mask-then-infill procedure using RoBERTa as a masked language model (models/Roberta.py, models/pipeline.py). Three perturbation operations implemented in models/attack_operations.py: (1) word_replacement — masks the target token and samples RoBERTa candidates; (2) word_insertion — masks a gap position and infills a new word; (3) word_merge — replac"
 },
 {
  "name": "samrand96/Undetectable-AI",
  "stars": 43,
  "category": "detector-with-evasion",
  "detector_in_loop": "yes — writer.com public AJAX endpoint is polled after every transformation step; the loop continues until writer.com returns \"Human-Generated\". Only one detector; no ensemble. The zerogpt function is ",
  "meaning_verification": "none — no cosine similarity, no NLI, no semantic-role check, no hedge or numeral audit. The rewritten text is accepted as long as the detector approves it.",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling and can be altered or dropped by the back-translation or LLM steps.",
  "mechanism": "Single Python file (main.py, ~200 lines). Processes a DOCX paragraph by paragraph. For each paragraph: (1) scrapes writer.com's undocumented AJAX endpoint (`/wp-admin/admin-ajax.php`, action `ai_content_detector_v2`) to get a human/AI label; (2) if already labeled human, copies it as-is; (3) otherwise enters a `while True` loop that alternates betw"
 },
 {
  "name": "AhmadHassan-BTed/Turnitout-Humanizer",
  "stars": 43,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — The ZipPy local detector (zlib/lzma entropy + phrase matching) exists as a standalone checker dashboard (\"QuillBot-style stats\"), not as a score gate that drives rewrite iterations. There is no d",
  "meaning_verification": "none — No cosine similarity check, NLI entailment, semantic-role matching, or any other meaning gate is described or implemented. The architecture docs and README make no mention of verifying that mea",
  "fact_preservation": "Partial — LaTeX math environments, equations, matrices, and formatting macros are placeholder-masked before any transformation and restored ",
  "mechanism": "Deterministic, 16-stage rule-based pipeline with NO generative AI. Stages include: phrase rewrites (JSON synonym dictionaries), morphological stemmed synonyms, determiner cycles (FIFO anti-repetition queue), clause reordering, compound sentence splits, active-to-passive voice inversion, sentence fusion, nominalization, appositive injection, and dis"
 },
 {
  "name": "BoHeFan/academic-paper-writer-pro-2",
  "stars": 42,
  "category": "prompt-guide",
  "detector_in_loop": "no — no detector or score is ever computed. The system applies a fixed rule set and asks the LLM to qualitatively judge whether the text \"already appears natural\"; it does not query any detector, thre",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role check, no round-trip back-translation. The only constraint is a qualitative prompt instruction not to alter logical relationships and t",
  "fact_preservation": "Technical terms protected via regex token-masking ([[TECH:term]] placeholder protocol) with post-processing verification that they survived.",
  "mechanism": "A Claude Code Skill (prompt-file collection) that drives an LLM agent through a five-pipeline academic-paper workflow. The AI-reduction component (Pipeline E / 降AI率) is a single-pass, rule-based LLM rewriting step guided by two prompt files: ai_reduction/prompts/chinese_rewrite.md and ai_reduction/prompts/english_latex.md. Chinese rewriting expands"
 },
 {
  "name": "jpeggdev/humanize-writing",
  "stars": 40,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, no iterative loop. It is a single editorial pass executed by Claude based on the checklist in SKILL.md. There is no re-score or threshold check.",
  "meaning_verification": "none — the skill includes editorial instructions to \"preserve technical accuracy and specific data points\" and \"keep the core argument and structure,\" but these are natural-language instructions to th",
  "fact_preservation": "none mechanical — the skill instructs the model to preserve \"proper nouns, product names, and attributions\" and \"specific data points,\" but ",
  "mechanism": "Pure prompt-engineering skill. The entire transformation logic lives in SKILL.md, a natural-language instruction file that Claude Code loads and follows. It specifies an 8-pass editorial workflow: (1) kill structural tells — formulaic section endings, identical list lengths, \"Despite X... Despite these challenges...\" loops; (2) strip significance i"
 },
 {
  "name": "frolvanya/ai-humanizer",
  "stars": 37,
  "category": "api-wrapper",
  "detector_in_loop": "no — single-shot API call, no scoring, no iteration",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Single HTTP POST to the Rephrasy.ai commercial API (endpoint: https://v1-humanizer.rephrasy.ai/api). The entire transformation logic is one axios call in src/index.tsx, function humanize(). The repo is a Raycast macOS launcher extension; it contributes zero rewriting logic itself — it just forwards text and a model choice (\"undetectable\" or \"SEO Mo"
 },
 {
  "name": "stephenlzc/humanize-mba-text-skill",
  "stars": 36,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — but not fully automated. The SKILL.md workflow is: detect → LLM/human rewrite → re-run detect_ai_patterns to confirm modify_plan cleared. detect_compare.py provides the before/after metric delta",
  "meaning_verification": "none. No cosine similarity, NLI, or semantic-role check. The rewrite checklist in references/mba_rewrite_checklist.toml includes a plagiarism rate hard limit of less than 15% as a proxy for divergence",
  "fact_preservation": "guidelines only, not programmatic. The checklist and SKILL.md principles forbid fabricating data and require all numbers to have cited sourc",
  "mechanism": "Three-layer Python detection pipeline feeds a Claude Code Skill manifest (SKILL.md) that drives Claude to rewrite. Layer 1: regex pattern matching against 7 TOML rule categories (structure, rhythm_quality, formatting, content, evidence, language, chapter-categories) — scripts/detect_ai_patterns.py via rule_loader.iter_regex_categories. Layer 2: sta"
 },
 {
  "name": "Firdavs-coder/ai_humanizer",
  "stars": 35,
  "category": "paraphrase-model",
  "detector_in_loop": "no — The \"AI Detection Check\" tab computes a one-time heuristic humanness_score (0-100) based on contraction count, filler-word frequency, sentence-length variance, and transitional-phrase density. It",
  "meaning_verification": "none — no cosine similarity, NLI, semantic-role match, or any other check. The system trusts the phi3 model to follow its prompt.",
  "fact_preservation": "none — no byte-locking, numeral retention checks, or citation guards of any kind.",
  "mechanism": "Single-file Streamlit app (ai_humanizer.py). Two-stage pipeline: (1) `humanize_text()` sends the input to a locally-running phi3 model via Ollama's REST API at localhost:11434/api/generate, with a prompt instructing \"Extreme\" humanization — varied sentence structure, contractions, filler words, natural imperfections. (2) `additional_humanization()`"
 },
 {
  "name": "jooray/humanizer",
  "stars": 34,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector of any kind. The two-pass \"loop\" (draft → self-audit → final) is a single LLM call chain with a prompted self-check question, not a score-and-iterate cycle.",
  "meaning_verification": "LLM self-check only: step 3 of the process asks the model \"Does the rewrite state any fact, name, number, date, or citation that isn't in the source?\" and instructs it to treat a fabrication as a defe",
  "fact_preservation": "Prompt instruction only: \"The rewrite must not contain any fact, name, number, date, quote, or citation that isn't in the source text.\" No b",
  "mechanism": "Pure prompt engineering: the entire product is SKILL.md, a Markdown file with YAML frontmatter that, when loaded as a system prompt by Claude Code (or any compatible agent harness), instructs the LLM to follow a two-pass process — draft rewrite, self-audit (\"what makes this obviously AI generated?\"), then final rewrite. The file defines 35 numbered"
 },
 {
  "name": "qingshanliuci/cnki-aigc---skill",
  "stars": 34,
  "category": "prompt-guide",
  "detector_in_loop": "no — The workflow includes a \"self-check\" step but it is Claude internally reviewing its own output against the prompt's rules, not submitting text to CNKI or any other detector and reading back a sco",
  "meaning_verification": "Prompt-level only. The self-check step instructs Claude to verify that numbers, citations, statistics, and domain terms are unchanged and that segment length stays within ±15% of the original. There i",
  "fact_preservation": "Prompt-level instruction only. The SKILL.md hard-constraints section explicitly lists numbers, variables, statistics, p-values, coefficients",
  "mechanism": "A Claude Skill encoded entirely in SKILL.md — no executable code. The file is a structured prompt that instructs Claude to identify and rewrite five linguistic patterns CNKI's detector targets: (1) sentence-length clustering at 20-35 chars, (2) uniform information density across paragraphs, (3) fixed subject-position placement of domain terms, (4) "
 },
 {
  "name": "RAIDAR (cvlab-columbia/RaidarLLMDetect)",
  "stars": 34,
  "category": "detector-with-evasion",
  "detector_in_loop": "yes — RAIDAR's detection signal IS the rewrite loop: LLM rewrites the text, then edit distance is measured. There is no iterative humanization loop; the single rewrite pass is the detection mechanism,",
  "meaning_verification": "none — RAIDAR does not verify meaning preservation. It deliberately measures how much the LLM changes text (more change = more likely human); preserving meaning is not a goal and no gates exist for it",
  "fact_preservation": "none — no special handling for numerals, citations, or quotes. This is a detector, not a humanizing rewriter, so byte-exact locking of facts",
  "mechanism": "RAIDAR is an AI-text DETECTOR, not a humanizer. It exploits the observation that LLMs perceive AI-generated text as high-quality and therefore make fewer edits when asked to rewrite it. The pipeline: (1) send the candidate text to an LLM with one of several rewrite prompts (e.g. \"Concise this for me and keep all the information\", \"Help me polish th"
 },
 {
  "name": "thevseprod/humanizer-ru",
  "stars": 34,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. The two-pass process is both LLM-internal: pass 1 rewrites by rules, pass 2 is a self-review prompt (\"what still smells like an AI?\"). No external scoring, no r",
  "meaning_verification": "none — the prompt instructs \"Keep the meaning, facts, and numbers\" (both RU and EN versions carry this wording) but there is no automated check of any kind; no similarity score, no NLI, no diff gate.",
  "fact_preservation": "instruction only — both rule files say \"do not add anything or invent data\" and \"keep facts and numbers\", but there is no byte-exact locking",
  "mechanism": "Pure markdown prompt/rule collection — no executable code whatsoever. Two files do all the work: `humanizer.ru.md` (Russian) and `humanizer.en.md` (English). Each is a structured system prompt with 13 numbered rule categories covering: em-dash replacement, removal of filler/bureaucratic phrases (Russian \"канцелярит\"), buzzword purging, hollow hedge"
 },
 {
  "name": "CarlanLark/Robust-AIGC-Detector",
  "stars": 33,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — textattack's Attacker loop scores the candidate text against the wrapped detector after every perturbation and continues until the detector's prediction flips (or the attack exhausts candidates)",
  "meaning_verification": "none — the only constraints are RepeatModification (don't re-substitute already-changed words) and StopwordModification (skip stopwords). No cosine similarity, no NLI entailment, no semantic-role chec",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling.",
  "mechanism": "Three textattack-library attack recipes applied against a target detector model wrapper. PWWS (Ren 2019): WordNet synonym substitution ranked by weighted saliency score — implemented via `textattack.attack_recipes.PWWSRen2019` in `run_attack.py`. Pruthi 2019: character-level typo attacks (substitution/deletion/insertion) capped by `max_num_word_swa"
 },
 {
  "name": "THU-KEG/WaterBench",
  "stars": 32,
  "category": "research-code",
  "detector_in_loop": "no — detection (detect.py, which computes z-scores) is a measurement step only. The pipeline is one-directional: generate watermarked text → detect → score quality. There is no re-generation or optimi",
  "meaning_verification": "none — metrics.py contains ROUGE, F1, and fuzzy-match scorers measuring downstream NLP task accuracy, but these assess whether watermarking degrades task utility, not whether any paraphrase preserved ",
  "fact_preservation": "none — no special handling of numbers, citations, or quotes in any of the generation or detection code paths.",
  "mechanism": "WaterBench is a benchmark framework for evaluating LLM watermarking schemes — it does NOT transform existing text to evade detection. The actual mechanism is generation-time logit biasing: during LLM inference, a LogitsProcessor (BlacklistLogitsProcessor in watermark/old_watermark.py, GPTWatermarkLogitsWarper in watermark/gptwm.py, WatermarkLogitsP"
 },
 {
  "name": "ADEMOLA200/Humanize-AI",
  "stars": 31,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration. Text goes in once, paraphrase comes back once.",
  "meaning_verification": "none — no cosine similarity, no NLI, no semantic-role check, no entailment gate of any kind",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling; they pass through the T5 model unchanged (or corrupted)",
  "mechanism": "Two-service pipeline: (1) A Python/Flask server (Humanize-AI-Server repo, api/index.py) calls the Hugging Face Inference API with model humarin/chatgpt_paraphraser_on_T5_base (beam search, top-k sampling, max 512 tokens). (2) A Go/Fiber service (DSk/services/rewrite.go, function RewriteText) calls that Flask endpoint via HTTP and returns the result"
 },
 {
  "name": "SemStamp",
  "stars": 30,
  "category": "research-code",
  "detector_in_loop": "no — the watermark is baked in during generation via rejection sampling; the detector is a separate z-test run after the fact on completed text, not used to steer rewriting. There is no detect-rewrite",
  "meaning_verification": "none for the watermark embedding itself. The paraphrase robustness test in paraphrase_gen.py uses a bert_threshold to filter paraphrase candidates that drift too far semantically, but this is for test",
  "fact_preservation": "none",
  "mechanism": "SemStamp is a watermark EMBEDDER and DETECTOR for LLM-generated text, not a humanizer or evasion tool. It works at generation time, not as a post-hoc rewriter. Two variants: (1) LSH mode — `sampling_lsh_utils.py::lsh_reject_completion()` encodes each candidate sentence with a fine-tuned sentence-transformer (SBERTLSHModel), computes its LSH signatu"
 },
 {
  "name": "eth-sri/watermark-stealing",
  "stars": 30,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no. The detector (KGW z-score) is called in the evaluator after generation for measurement, but its output never feeds back to trigger a rewrite. Generation is a single forward pass through the attack",
  "meaning_verification": "PSP (Paraphrase Semantic Preservation, via a downloaded PSP model) is computed as an evaluation metric and logged per-sample, with a threshold of 0.7 used in the is_good flag (evaluator.py). For scrub",
  "fact_preservation": "none. No mechanism protects numbers, citations, or quoted strings. The logit-bias approach in SpoofedProcessor operates at the token level w",
  "mechanism": "Three-phase black-box attack against KGW-family LLM watermarks. (1) Querying: the attacker calls the watermarked API with C4 corpus prompts and saves token-level responses as JSONL files (src/attackers/our_attacker.py, query_server_and_save). (2) Learning: a CountStore (src/attackers/count_store.py) accumulates context-keyed token frequency tables "
 },
 {
  "name": "jmpu/DeepfakeTextDetection",
  "stars": 30,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — explicitly designed to be query-free; no victim classifier is queried before, during, or after substitution. The only model consulted is the backend LM (BERT or GPT-2 XL) used to measure token pr",
  "meaning_verification": "Partial. Two gates are applied per substitution: (a) Universal Sentence Encoder cosine similarity on a local 15-token window around the changed position (threshold 0.7, implemented in sentence_similar",
  "fact_preservation": "none — numbers, citations, and quoted strings have no special handling; they could be altered if they appear in the counter-fitted vocabular",
  "mechanism": "DFTFooler performs query-free word-substitution adversarial attack in DFTFooler/DFTFooler_attack.py. The pipeline: (1) score every non-stop-word in the AI-generated text with a backend LM (BERT-large or GPT-2 XL) to get per-token probabilities; (2) rank words by descending probability — high-probability tokens are the \"AI tells\"; (3) for each candi"
 },
 {
  "name": "SemStamp (abehou/SemStamp)",
  "stars": 30,
  "category": "research-code",
  "detector_in_loop": "no — There is no score-rewrite-rescore loop. The watermark is embedded during generation by rejection sampling; a detector runs separately in evaluation experiments to measure watermark survival after",
  "meaning_verification": "none — Rejection sampling enforces only the LSH hash constraint, not semantic fidelity. The margin parameter filters out boundary-ambiguous sentences for robustness but does not compare meaning to any",
  "fact_preservation": "none",
  "mechanism": "SemStamp is a watermarking system that embeds hidden signals INTO AI-generated text during generation — the inverse of a humanizer. It does NOT transform already-written text to evade detectors; it constrains the LLM's output at generation time so that a matching detector can later confirm provenance.\n\nTechnically, it uses sentence-level rejection "
 },
 {
  "name": "AATMF-Adversarial-AI-Threat-Modeling-Framework",
  "stars": 27,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring, no rewriting loop, no iteration. The framework documents techniques humans or attackers can apply manually; it does not automate any feedback cycle.",
  "meaning_verification": "none — no automated check of any kind that meaning survived a transformation.",
  "fact_preservation": "none — no mechanism to preserve numbers, citations, or quotes.",
  "mechanism": "Pure taxonomy and documentation: 15 adversarial AI tactics, 240 techniques, 2,152+ attack procedures, and 4,980+ curated prompts organized in Markdown chapters. It does NOT transform text programmatically. The three Python files (scripts/aatmf_taxonomy.py, scripts/build_export.py, scripts/validate.py) are Markdown parsers and CI integrity checkers "
 },
 {
  "name": "Smith-2758/Humanizer-zh-academic",
  "stars": 27,
  "category": "prompt-guide",
  "detector_in_loop": "no — no detector is called at any stage; output is returned after a single model call with no re-scoring or iteration",
  "meaning_verification": "none automated. The system prompt instructs the model to \"retain concrete facts and reduce empty meaning-amplification,\" but there is no cosine similarity gate, NLI entailment check, or any other prog",
  "fact_preservation": "none automated. The system prompt says to preserve specific facts, but there is no byte-exact locking of numerals or citations — preservatio",
  "mechanism": "Single-pass LLM call driven entirely by a crafted system prompt (SKILL.md + web/lib/prompt/systemPrompt.ts). The prompt instructs the model to eliminate ~25 high-risk AI-tell phrases per category across vocabulary, syntax, content, and formatting layers. A buildRewriteMessages function (web/lib/prompt/buildPrompt.ts) assembles a fixed system messag"
 },
 {
  "name": "Advancing-Machine-Human-Reasoning-Lab/apt",
  "stars": 25,
  "category": "research-code",
  "detector_in_loop": "no — a paraphrase-detector (sentence equivalence classifier) is the downstream evaluation target, but it is not in the generation loop. The in-loop gate is a RoBERTa-NLI bidirectional entailment check",
  "meaning_verification": "Yes, bidirectional NLI entailment via RoBERTa-NLI (`get_mi_score()` in `nap_generation.py`): both s1→s2 and s2→s1 must be predicted \"entailment\" for a candidate to be accepted. BLEURT similarity is al",
  "fact_preservation": "none — no special handling for numerals, citations, or quoted strings",
  "mechanism": "Fine-tuned T5-base model (`coderpotter/T5-for-Adversarial-Paraphrasing`, trained on MSRP + TwitterPPDB) generates up to 10 paraphrase candidates per sentence using top-k / top-p sampling. `nap_generation.py` then filters candidates: a RoBERTa-NLI model scores bidirectional entailment (`get_mi_score()`), BLEURT measures surface similarity (`get_bleu"
 },
 {
  "name": "shizhouxing/LLM-Detector-Robustness",
  "stars": 24,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — but only in the instructional-prompt attack. prompt_attack.py calls the OpenAI AI Text Classifier on every generated candidate and iterates over prompts to minimise detection score. The word-sub",
  "meaning_verification": "none. No cosine similarity gate, no NLI entailment check, no semantic-role comparison. The genetic algorithm's fitness is LM log-likelihood of the output, which encourages fluency but is not a semanti",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special handling; any word in the text is a candidate for substitution.",
  "mechanism": "Three distinct attack strategies, all implemented in Python. (1) Word-substitution attacks: an LLM (ChatGPT or LLaMA) generates synonym candidates for each content word; a genetic algorithm (genetic_attack_agent in DetectGPT/attackers.py) then evolves a population of substituted sentences, using language-model log-likelihood as fitness, with crosso"
 },
 {
  "name": "hlzhang109/impossibility-watermark",
  "stars": 24,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — the watermark detector is used only in post-hoc evaluation (detection.py) to measure whether the watermark survived; it does not drive iteration. The quality oracle (reward model or GPT) IS in th",
  "meaning_verification": "indirect proxy only — the quality oracle (evaluation/tools/oracle.py, class QualityOracle) scores grammar, coherence, relevance, and accuracy using either a HuggingFace reward model (OpenAssistant deb",
  "fact_preservation": "none — no byte-exact locking of numbers or citations, no numeral-retention gate, no quote-preservation mechanism.",
  "mechanism": "Random-walk attack on watermarked text. Core loop (text/evaluation/tools/text_editor.py, class RandomWalkAttack.perturb()): randomly mask a span of tokens in the watermarked output, use T5 span-infilling to regenerate the span, accept the replacement if a quality oracle approves (reward model score above threshold, or GPT-3.5/4 rating), and repeat "
 },
 {
  "name": "zhouxingshi/red_team_text_detectors (claimed) → actual: shizhouxing/LLM-Detector-Robustness",
  "stars": 24,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — both main attacks use detector feedback. The genetic word-substitution attack minimises the detector's score directly: each candidate substitution set is evaluated against the target detector, a",
  "meaning_verification": "none automated. The only quality check is a post-hoc human evaluation (Amazon Mechanical Turk) reported in the paper for fluency and plausibility. No NLI gate, no cosine-similarity threshold, no numer",
  "fact_preservation": "none. Word substitution can replace numerals and named entities; no preserve-lock or bypass list for citations, numbers, or quoted text exis",
  "mechanism": "Two complementary attack strategies, both implemented at shizhouxing/LLM-Detector-Robustness. (1) Word-substitution attack (DetectGPT/attackers.py, classes genetic_attack_agent and random_attack_agent): an auxiliary LLM (LLaMA-65B or ChatGPT) proposes synonym candidates for each non-stop word; selection is either random (\"query-free\") or genetic/ev"
 },
 {
  "name": "ai-kunkun/PASA",
  "stars": 23,
  "category": "research-code",
  "detector_in_loop": "no — the detection score is computed once over completed text to measure the watermark hit rate; there is no generate-score-rewrite-repeat loop",
  "meaning_verification": "none — watermarking biases token-level sampling probabilities but has no semantic-equivalence check; the paper claims \"semantic-invariant\" in the opposite sense (the watermark survives paraphrase atta",
  "fact_preservation": "none — numbers and names are tokenized and may be biased toward a particular cluster like any other tokens; no byte-exact locking or numeral",
  "mechanism": "Generation-time embedding-space watermarking. During autoregressive token sampling, PASA clusters the entire vocabulary into K semantic groups via a pre-computed token-to-cluster CSV (`key_map_path`). At each generation step (after position `start`), it hashes the previous `window_size` tokens (mapped to their cluster IDs) with a secret key via SHA"
 },
 {
  "name": "THU-BPM/Watermark-Radioactivity-Attack",
  "stars": 23,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — `detect.py` (median p-value across chunked token groups, supports KGW/Unigram/SynthID) is evaluation-only. It runs before and after neutralization to measure success, but is never used as a feedb",
  "meaning_verification": "none at the sentence or document level. Capability retention is checked at the model level via ARC Challenge (multiple-choice benchmark) and MT-Bench (instruction-following, judged by GPT-4o), not per",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special treatment. The attack manipulates token probability distributions, not content.",
  "mechanism": "Three attack strategies against LLM watermark radioactivity (the property that lets auditors detect when a student model was distilled from a watermarked teacher). (1) Untargeted Paraphrasing: rewrites all watermarked training data before distillation, breaking watermark signal. (2) Targeted Paraphrasing: same but selectively targets high-watermark"
 },
 {
  "name": "lguz/humanize-writing-skill",
  "stars": 23,
  "category": "prompt-guide",
  "detector_in_loop": "no — no detector is invoked at any stage. The skill rewrites once and returns. There is no scoring step, no iteration, and no feedback loop of any kind.",
  "meaning_verification": "none — SKILL.md contains a prose directive (\"keep the meaning and factual content intact\") addressed to the rewriting LLM, but no automated check is performed. The 17-point quality checklist is entire",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling. The only protection is the prose directive to keep factual content intact",
  "mechanism": "Pure prompt-engineering skill packaged as a Claude Code plugin. The two operative files are `skills/humanize-writing/SKILL.md` (instructions) and `skills/humanize-writing/references/ai-patterns-dictionary.md` (word/pattern lists). SKILL.md instructs the host LLM to run a 3-pass edit: Pass 1 scans word-by-word for items on the tiered banned-vocabula"
 },
 {
  "name": "syq-cmdi/Academic-DeAI",
  "stars": 23,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, no re-score, no iteration of any kind. The rewrite is one-shot. Detection evasion is not the stated goal; the stated goal is academic writing quality.",
  "meaning_verification": "Instruction-only, not mechanical. SKILL.md instructs the LLM to preserve all LaTeX equations, BibTeX entries, citation keys, numerical results (coefficients, p-values, t-values, standard errors, confi",
  "fact_preservation": "Explicit instruction-level protection for: LaTeX math and commands, BibTeX entries, citation keys (`\\citep{}`, `\\citet{}`, `\\cite{}`, `[1]`,",
  "mechanism": "Pure prompt-skill file. The entire tool is SKILL.md — a Markdown file with a YAML frontmatter (`name`, `version`, `description`, `allowed-tools`) that Claude Code or OpenCode loads as an agent skill. When invoked via `/academic-deai`, the host LLM reads the instructions and applies them to input text in a single inference pass. No code runs; there "
 },
 {
  "name": "shyuan/writing-humanizer",
  "stars": 23,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector anywhere in the pipeline. The 2-pass \"self-audit\" is the LLM asking itself whether any AI patterns remain; it does not call any external or local detector, does not produce a",
  "meaning_verification": "none. The self-audit in pass 2 checks for residual AI patterns, not semantic preservation. There is no cosine similarity gate, no NLI check, no structured round-trip comparison.",
  "fact_preservation": "none. Numbers, citations, and quoted text receive no special treatment and are not locked or verified.",
  "mechanism": "Pure prompt engineering — no executable code touches the text directly. The repo is a Claude Code plugin (and multi-host skill) whose entire transformation logic lives in Markdown files. `skills/writing-humanizer/SKILL.md` is the hub: it gives the LLM a 2-pass rewriting process (first pass: scan + rewrite; second pass: mandatory self-audit), a rati"
 },
 {
  "name": "bejek/humanizer-czech",
  "stars": 22,
  "category": "prompt-guide",
  "detector_in_loop": "No. The \"self-check\" second pass is a qualitative prompt-level reflection, not automated scoring with a detector. No detector is called; no numerical score is computed or iterated on.",
  "meaning_verification": "None automated. The prompt instructs the LLM to preserve meaning, but there is no cosine similarity check, NLI entailment gate, semantic-role match, or any mechanical verification step.",
  "fact_preservation": "None automated. The prompt flags \"hallucinated statistics\" and vague attribution as anti-patterns (#8 and #24), and tells the rewriter to ve",
  "mechanism": "The entire repo is two large markdown prompt files (PROMPT.md ~27 KB, SKILL.md ~28 KB) with no executable code. Mechanism: a system prompt is loaded into any LLM (Claude, ChatGPT, Gemini). The prompt instructs the model to: (1) scan the input against a hand-written catalogue of 27 Czech-specific AI-tell patterns organised into categories (opening i"
 },
 {
  "name": "indranilbanerjee/contentforge",
  "stars": 20,
  "category": "prompt-guide",
  "detector_in_loop": "no — ContentForge has no call to any external AI-text detector (GPTZero, Originality.ai, Winston, Copyleaks, or any local model). The readme marketing copy claims output \"passes GPTZero\" but no detect",
  "meaning_verification": "Partial and structurally different from untell's approach. Before humanization, Phase 2 runs a fact-checker subagent that verifies URLs and cross-references claims (max_unresolved_flags: 0, max_unveri",
  "fact_preservation": "Meaningful: Phase 2 (fact-checker subagent, agents/02-fact-checker.md) verifies all citations are live URLs and cross-references all statist",
  "mechanism": "ContentForge is an LLM-orchestrated content production pipeline where humanization is Phase 6.5 of 10. The humanizer agent (agents/06.5-humanizer.md) is a detailed prompt fed to Claude that instructs it to walk a 35-pattern AI-tell catalog (config/humanization-patterns.json), grouped into six buckets: Content Patterns (01-06, significance inflation"
 },
 {
  "name": "Hakku/finnish-humanizer",
  "stars": 19,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iterative loop. The LLM applies the 27-pattern ruleset in a single pass and returns the result.",
  "meaning_verification": "none mechanically. The prompt instructs the LLM to \"preserve factual content, formality level, and code/technical terms,\" but this is a soft instruction to the host model, not a gated semantic check.",
  "fact_preservation": "Soft prompt instruction only: the skill tells the LLM to preserve factual content and technical terms. No byte-exact locking, no numeral-ret",
  "mechanism": "A structured system prompt (SKILL.md) defining 27 AI-tell patterns specific to Finnish text — 12 Finnish-specific (passive voice overuse, missing colloquial particles like -han/-hän, translation artifacts, filler phrases) plus 15 universal. The SKILL.md is distributed as platform-specific prompt snippets to 15 AI tool integrations (Claude Code, Cha"
 },
 {
  "name": "moli238/humanizer-zh-moli",
  "stars": 19,
  "category": "prompt-guide",
  "detector_in_loop": "no — the repo explicitly states \"不承诺绕过任何 AI 检测器\" (makes no promise to bypass any AI detector). There is no score-rewrite-rescore loop anywhere in the codebase.",
  "meaning_verification": "none (automated). The SKILL.md lists non-negotiable boundaries as prompt instructions (do not fabricate names/dates/numbers, do not alter direct quotations/code/links, do not resolve contradictions wi",
  "fact_preservation": "Prompt-instruction only. The skill forbids the model from adding or altering names, numbers, dates, quotes, code blocks, or links — stated a",
  "mechanism": "A Claude agent skill (SKILL.md + agents/openai.yaml) that instructs Claude to rewrite Chinese text by applying 36 named anti-AI-tell patterns. There is no standalone executable or custom model: the transformation is entirely prompt-driven, with Claude as the engine. The patterns are enumerated in SKILL.md (verb nominalization, bureaucratic jargon, "
 },
 {
  "name": "StealthRL (suraj-ranganath/StealthRL)",
  "stars": 18,
  "category": "fine-tuned-model",
  "detector_in_loop": "yes — during training only. Detectors (FastDetectGPT, Ghostbuster, Binoculars, defined in stealthrl/detectors/) are queried each GRPO step to produce the detector-evasion reward signal. At inference t",
  "meaning_verification": "Cosine similarity (threshold 0.90 in training reward) and BERTScore F1 (threshold 0.80) are baked into the GRPO reward, so the model is penalised during training for dropping below these bars. GPT-bas",
  "fact_preservation": "none — the repository explicitly excludes protection mechanisms for numbers, citations, or specific factual references.",
  "mechanism": "Trains a paraphrase policy (Qwen3-4B-Instruct + LoRA rank 32) using GRPO (Group Relative Policy Optimization) against a weighted detector ensemble (RoBERTa 0.6 + Fast-DetectGPT 0.4 during training; FastDetectGPT, Ghostbuster, Binoculars in the full detector module). The composite reward in stealthrl/rewards/composite_reward.py is R = -w1*D' + w2*S'"
 },
 {
  "name": "obaskly/AiDetectionBypass",
  "stars": 18,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector, no scoring, no re-score, and no iteration. Text goes in once, paraphrased text comes out once. No quality gate of any kind.",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes pass through gptinf.com's black-box rewriter with no guards; whatever gptinf.com changes, the script a",
  "mechanism": "Browser automation wrapper around gptinf.com, a commercial AI-humanizer SaaS. The repo does zero text transformation itself. main.py uses Selenium to: (1) auto-register throwaway Gmail variants on gptinf.com via create_new_account(), bypassing reCAPTCHA with an audio-challenge technique (RecaptchaSolver.py — downloads the audio challenge, transcrib"
 },
 {
  "name": "itallstartedwithaidea/writing-agent",
  "stars": 16,
  "category": "prompt-guide",
  "detector_in_loop": "Partial/no. The QA revision loop (ghost.js lines 56-63) iterates up to 3 times using local heuristic checks (sentence-length variance, phrase blacklist, type-token ratio etc.), NOT real detector score",
  "meaning_verification": "none. The revise() call asks Claude to \"fix the failing checks with targeted surgical edits\" but there is no cosine similarity gate, no NLI entailment check, no semantic-role comparison between input ",
  "fact_preservation": "none. Numbers, citations, and quotes pass through LLM rewrites with no locking, byte-exact preservation, or post-hoc comparison.",
  "mechanism": "5-stage pipeline that generates content from scratch (not a rewriter of existing text). Stage 1 (profile-agent.js): classifies topic and loads a named voice profile. Stage 2 (writer-agent.js): calls the Anthropic Claude API (claude-sonnet-4) with a system prompt encoding human-pattern injection rules at structural, sentence, and word levels — eleva"
 },
 {
  "name": "vardhin/Humanizer",
  "stars": 15,
  "category": "paraphrase-model",
  "detector_in_loop": "no — the detector is a completely separate REST service. HumanizerService.humanize_text() never calls any scoring function; it runs paraphrase then rewrite then returns. The detector endpoints exist i",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role check, no numeral retention gate. The rewrite stage applies transformations blindly and returns without any verification that meaning s",
  "fact_preservation": "none — numbers, citations, and quoted material receive no special handling. WordNet synonym substitution and phrase replacement operate on a",
  "mechanism": "Two-stage open-loop pipeline. Stage 1 (paraphraser.py, paraphrase_text()): loads a HuggingFace seq2seq model (T5-small/base, Vamsi/T5_Paraphrase_Paws, humarin/chatgpt_paraphraser_on_T5_base, facebook/bart-base, facebook/bart-large, or tuner007/pegasus_paraphrase) and runs beam-search generation with temperature sampling. Stage 2 (rewriter.py, TextR"
 },
 {
  "name": "XiangJinyu/humanize-zh",
  "stars": 14,
  "category": "fine-tuned-model",
  "detector_in_loop": "no — detectors are used only post-hoc to report a single before/after number (AI-flagging rate ~30% → <10% on an unspecified sample subset). There is no score-rewrite-re-score iteration at inference t",
  "meaning_verification": "none automated — README states \"manual spot-checks indicate that key semantics and factual content are largely preserved.\" No cosine-similarity gate, no NLI entailment check, no semantic-role match, n",
  "fact_preservation": "none — no mention of byte-exact locking, numeral retention, citation preservation, or any structured guard for factual content.",
  "mechanism": "Three-stage fine-tune of Qwen3.5-9B. Stage 1 (SFT): ~18,000 (AI-rewrite, original-human) pairs drawn from the CSL Chinese academic-abstract corpus, trained for ~900 steps. Stage 2 (DPO Round 1): 4,000 dual-direction rejected samples (2,000 formal + 2,000 casual) so the model learns the human writing distribution rather than drifting toward colloqui"
 },
 {
  "name": "lazyjerry/Humanizer-zh",
  "stars": 14,
  "category": "prompt-guide",
  "detector_in_loop": "no — the 6-dimension quality rubric is self-scored by Claude, not an AI-text detector. No external detector is queried, no score threshold gates iteration, and there is no loop at all; rewriting is a ",
  "meaning_verification": "none automated — the skill prompt instructs Claude to \"preserve core information intact\" and \"match the intended tone,\" but there is no programmatic similarity check, NLI gate, or numeral-retention te",
  "fact_preservation": "none — the prompt says \"preserve core information\" but there is no byte-exact locking, numeral-retention check, citation preservation rule, ",
  "mechanism": "A Claude Code skill defined entirely in markdown files (SKILL.md, patterns.md, taiwan-vocab.md). When invoked, it instructs Claude to read the 24-pattern catalogue (patterns.md) and Taiwan vocabulary table (taiwan-vocab.md), then rewrite the submitted text by: (1) identifying instances of each AI-tell pattern, (2) replacing them with natural altern"
 },
 {
  "name": "ThoratSoham/TextHumaniser",
  "stars": 14,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — a naturalness quality score is described in the README (sentence-length variance, word-length distribution, re-pass on low-scoring paragraphs) but no AI-text detector is mentioned and no scoring ",
  "meaning_verification": "none — entity/NER protection and naturalness scoring are described but no code exists",
  "fact_preservation": "none — spaCy NER entity protection is described in the README but the module does not exist",
  "mechanism": "Claimed but entirely unimplemented. The README describes a six-stage pipeline: spaCy NER entity protection, WordNet POS-aware synonym substitution, spaCy dependency-parse sentence splitting/merging, phrase-bank substitution, controlled punctuation/contraction noise injection, and a pure-Python \"naturalness\" score (sentence-length variance, word-len"
 },
 {
  "name": "CAU-ISS-Lab/AIGT-Detection-Evade-Detection",
  "stars": 13,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes, but only during the training/feature-optimization phase. feature_construct.py scores each generated candidate with a local detector and uses pass/fail to determine which examples feed the feature",
  "meaning_verification": "none. No cosine similarity check, no NLI gate, no ROUGE/BERTScore guard on the output. The only signal used is the detector's binary pass/fail.",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special treatment.",
  "mechanism": "The repo has three components. Two are pure detectors (C-Net: compression-based classifier; DP-Net: DDPG-RL perturbation encoder). The evasion component is SDA (Self-Disguise Attack), in SDA/. It works in two phases. Phase 1 — Adversarial Feature Extraction (feature_construct.py + prompt_constructor.py): a training loop generates LLM responses to d"
 },
 {
  "name": "OrbitWebTools/Humanize-AI",
  "stars": 13,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — no detector is consulted at any point. The tool makes no network requests and calls no scoring API. The output is produced in a single deterministic pass.",
  "meaning_verification": "none — the substitution is unconditional. Words are replaced regardless of context; there is no similarity check, NLI gate, or any other semantic guard. Meaning corruption is likely on ambiguous words",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special handling. They pass through only because they happen not to match any SYNON",
  "mechanism": "Pure client-side JavaScript synonym substitution, implemented entirely inside index.html in the humanize() function (lines 805–841). The transform has three steps: (1) a hardcoded ~30-entry SYNONYMS dictionary performs multi-word phrase replacement first (e.g. \"in conclusion\" → \"to wrap up\", \"in order to\" → \"to\") then single-word swaps (e.g. \"utili"
 },
 {
  "name": "OrbitWebTools/Free-Unlimited-Humanize-AI-Text-Bypass-Turnitin-2026",
  "stars": 13,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no scoring step at any point. The tool does not call Turnitin, ZeroGPT, Originality.ai, or any local detector. It rewrites once and stops.",
  "meaning_verification": "none — no similarity check, no NLI, no semantic role match. The synonym pairs are manually curated and assumed safe.",
  "fact_preservation": "none — numbers, citations, and quoted strings pass through only because the synonym dictionary happens not to match them; there is no explic",
  "mechanism": "Pure client-side JavaScript synonym substitution. The core is a static dictionary of roughly 30 word pairs (e.g. \"moreover\"->\"also\", \"utilize\"->\"use\", \"demonstrate\"->\"show\") applied via regex replace over the input text. Multi-word phrases are substituted first, then single words sentence by sentence with capitalization preserved. A small amount of"
 },
 {
  "name": "YichenZW/Robust-Det",
  "stars": 13,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — detectors are applied only for post-hoc evaluation; no feedback loop drives further rewrites. The attack is one-shot: transform text, then score. The distance_measure.py computes cosine similarit",
  "meaning_verification": "none at runtime. distance_measure.py computes semantic similarity (BART cosine, BARTScore, BERTScore, MAUVE) and edit distance as post-hoc research metrics for the paper, not as automated filters that",
  "fact_preservation": "none. No mechanism protects numerals, citations, or quoted strings from alteration by any of the attack methods.",
  "mechanism": "Research benchmark framework for stress-testing AI-text detectors across 15+ attack types organized into three phases. Post-generation attacks (applied after text is created): character-level typo variants (transposition, substitution, deletion, insertion via post_attack.py), homoglyph substitution using the VIPER neighbor-dictionary approach (ECES"
 },
 {
  "name": "aoi3142/Waterfall",
  "stars": 13,
  "category": "research-code",
  "detector_in_loop": "no — STS similarity scores each beam candidate during group beam search to select the semantically closest paraphrase, but there is no detect-rewrite-re-score loop. The watermark score (q) is only com",
  "meaning_verification": "STS cosine similarity via sentence-transformers/all-mpnet-base-v2 (sts.py, STS_scorer / STS_scorer_batch). Used during beam selection to pick the highest-similarity candidate among group beam search o",
  "fact_preservation": "none — no special handling of numbers, citations, named entities, or quotes. The LLM paraphraser can freely alter any content; only overall ",
  "mechanism": "Training-free text watermarking via LLM-based paraphrasing with logit-space perturbation. Concretely: the original text is fed to an LLM paraphraser (default: Llama-3.1-8B-Instruct); before token sampling, a pseudo-random permutation keyed on (watermark ID, preceding n-gram) reorders the logit vector into a \"watermark vocabulary space\"; a Fourier o"
 },
 {
  "name": "adamsyy/cheatGPT",
  "stars": 13,
  "category": "api-wrapper",
  "detector_in_loop": "no — the frontend fires one POST and renders the response; there is no score, no threshold check, and no retry anywhere in the code",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "React frontend that POSTs user text to a closed-source Flask backend (https://flask-hello-world-theta-green.vercel.app/paraphrase1) and displays the returned string. The single call is in inputField.js `handleSubmit`. The backend source is not in this repo; the transformation technique (almost certainly a one-shot GPT paraphrase prompt) is opaque. "
 },
 {
  "name": "GREATER (Liyuuuu111/GREATER)",
  "stars": 11,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — partially. The target discriminator is queried each iteration inside the attack loop; the loop halts early when the classifier flips (label change = attack success). This is an adversarial-ML in",
  "meaning_verification": "none as a gate. GPT-2 perplexity, Universal Sentence Encoder cosine similarity, and Flesch-Kincaid readability are computed and logged as evaluation metrics (attack.py), but they do not block or roll ",
  "fact_preservation": "none. No special handling of numbers, citations, or quoted strings anywhere in the codebase.",
  "mechanism": "Two-component adversarial ML framework. GREATER-A (attack): a trainable importance module scores tokens by MSE alignment with a proxy GPT-2 model's gradients; top-scoring tokens are either substituted via RoBERTa masked-LM (top-5 candidates, `generate_adversarial_text_mask` in attack.py) or perturbed in embedding space (`generate_adversarial_text_p"
 },
 {
  "name": "bypass-aigc-detection",
  "stars": 11,
  "category": "prompt-guide",
  "detector_in_loop": "no — the pipeline is open-loop. No detector is invoked before, between, or after stages. There is no re-score or iteration condition. The skill rewrites once (two internal stages) and exits.",
  "meaning_verification": "Minimal LLM self-check only. The prompt instructs the model to verify: word count differs by no more than 30 characters from original, paragraph structure is preserved, core causal logic and functiona",
  "fact_preservation": "Partial and prompt-based only. The instructions explicitly forbid modifying technical terms, proper nouns, code snippets, and API paths. Num",
  "mechanism": "Pure prompt-only agent skill (no executable code). Two-stage serial rewriting: Stage 1 \"Polish\" instructs the LLM to act as a \"top academic editor\" applying systematic vocabulary substitution, verb-phrase expansion, and bracket-content integration. Stage 2 \"Enhance\" instructs a \"style mimic expert\" to push the same transformations further and injec"
 },
 {
  "name": "MASH (githigher/MASH)",
  "stars": 10,
  "category": "fine-tuned-model",
  "detector_in_loop": "yes — two distinct uses. Offline (training): Stage 1 uses detector to filter training data; Stage 2-to-3 uses detector to identify hard negatives for DPO. Online (inference): Stage 4 queries the detec",
  "meaning_verification": "none. No cosine similarity, no NLI entailment check, no semantic-role match, no hedge or numeral retention gate. The LLM in Stage 4 receives the sentence as context but the only acceptance criterion i",
  "fact_preservation": "none. Numbers, citations, and quoted material are treated as ordinary tokens throughout all stages. No byte-exact locking, no numeral-retent",
  "mechanism": "Four-stage offline-training-then-inference pipeline. Stage 1 (stage1_data_construction.py): streams a HuggingFace dataset and retains only samples that a RoBERTa detector classifies as human-written, building a parallel AI-human corpus. Stage 2 (stage2_style_sft.py): fine-tunes facebook/bart-base (~0.1B params) with a dual-objective loss — reconstr"
 },
 {
  "name": "Qi-Pang/LLM-Watermark-Attacks",
  "stars": 10,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — the query-based attack (attack_query.py) is a tight detector-in-the-loop: for every token position it queries the detector with each top candidate appended to the growing sequence, reads back th",
  "meaning_verification": "none — and one attack deliberately destroys meaning: modify_words() prompts GPT-4 to make text \"inaccurate or have opposite meanings.\" No semantic similarity check, NLI gate, or any other meaning gate",
  "fact_preservation": "none — numbers, citations, and quotes receive no protection. The banned-word insertion inserts arbitrary tokens anywhere in the text; the GP",
  "mechanism": "Three distinct attack families, all targeting statistical watermark signals (z-scores) embedded during LLM generation rather than surface-level \"AI tells\":\n\n1. Query-based attack (KGW-Watermark/attack_query.py, Unigram-Watermark/attack_query.py, Exp-Watermark/experiments/attack_query.py): Token-by-token greedy generation that queries the watermark "
 },
 {
  "name": "bypassessay/bypass-ai-detectors",
  "stars": 10,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step at all. The repo contains no code, no API calls, and no feedback mechanism. It is a static reading guide.",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes are not mentioned as requiring protection. The only citation-adjacent advice is \"add authentic citatio",
  "mechanism": "Pure markdown documentation with no executable code. Describes manual human-editing techniques: paraphrasing and sentence restructuring, synonym replacement, sentence-length variation, adding contractions and rhetorical questions, injecting personal anecdotes and conversational openers, blending AI text with human-written sections, and reordering p"
 },
 {
  "name": "xszcs546/ai-text-humanizer",
  "stars": 10,
  "category": "other",
  "detector_in_loop": "no — the repo itself has no loop or any runnable code. Several of the listed commercial products claim to include built-in detection testing (Undetectable AI claims scoring against 8 detectors, Humbot",
  "meaning_verification": "none — no code exists in the repo. The README reproduces marketing copy from listed services that claim meaning preservation, but provides no technical detail and no verifiable method.",
  "fact_preservation": "none — no code. One listed product (Humanize AI Tool) is mentioned to have a \"Keywords Freezer\" feature for locking specific terms, but that",
  "mechanism": "The repository contains no code. It is a single README.md (32 KB) that is a curated affiliate/marketing directory of 13+ closed-source commercial AI humanizer SaaS products (AISEO, Undetectable AI, StealthGPT, WriteHuman, Humbot, Smodin, Bypass GPT, Quillbot, Humanize AI Pro, HumanizeAI.com, GrubbyAI, JustDone, StealthWriter, Monica AI) plus an ima"
 },
 {
  "name": "swaylq/humanize-chinese",
  "stars": 10,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — the internal detector (`scripts/detect_cn.py`) is never called during rewriting. It is used only in the benchmark (`evals/run_hc3_benchmark.py`) to measure before/after delta. The rewriter fires ",
  "meaning_verification": "none in the rewrite path. The regression suite (`tests/test_regression.py`) checks paragraph count loss (flags if more than 2 paragraphs disappear) and mutation safety (no empty paragraphs produced), ",
  "fact_preservation": "none explicitly. Substring collision detection prevents corrupting compound nouns (e.g. blocking \"确定\"→\"敲定\" inside \"确定性\"), but there is no nu",
  "mechanism": "Three-pass local rewriter targeting Chinese text. (1) Synonym substitution: `pick_best_replacement()` in `scripts/humanize_cn.py` ranks candidates by n-gram perplexity and picks a mid-ranked option — avoiding both the lowest-perplexity (too weird) and highest-perplexity (too predictable) choices; ~200 hand-curated word pairs plus CiLin thesaurus. ("
 },
 {
  "name": "Show-Chan97/Humanizer-zh",
  "stars": 10,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector call anywhere in the repo. The workflow is single-pass: invoke skill → Claude rewrites → done. No scoring, no re-score, no iteration.",
  "meaning_verification": "none — the prompt contains a 50-point quality rubric across five dimensions and a six-step anti-AI checklist, but these are instructions for the LLM to self-evaluate subjectively; there is no automate",
  "fact_preservation": "none — no byte-exact locking, no numeral-retention logic. Numbers and citations pass through only as well as the LLM happens to preserve the",
  "mechanism": "Pure-prompt Claude Code skill — no executable code at all. The repo contains three Markdown files: SKILL.md (YAML frontmatter + prompt instructions), README.md, and WARP.md (doc). Installing via `npx skills add` or git-clone drops the folder into ~/.claude/skills/. The user then invokes `/Humanizer-zh [text]` inside Claude Code and Claude rewrites "
 },
 {
  "name": "wuisabel-gif/Cadence",
  "stars": 9,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — scripted but explicit. `skills/cadence/SKILL.md` mandates: \"Every command that produces prose ends by running it through the detector. If the score is above ~25, the draft still sounds like AI. ",
  "meaning_verification": "none mechanical. The recast instructions say \"Keep every claim, fact, and piece of structure the user put there. Change how it sounds, not what it says.\" This is a prompt constraint, not a gate. No co",
  "fact_preservation": "none. No byte-exact locking of numbers, citations, or quoted strings. The instructions warn against dropping facts, but there is no enforcem",
  "mechanism": "Two-engine design. Engine 1 is a deterministic, zero-dependency Node.js analyzer (`skills/cadence/scripts/deslop.mjs`). It tokenizes text, splits sentences with an abbreviation-aware splitter, then scores eight categories: banned-phrase matches (BANNED_PHRASES list, ~30 entries), hollow-confidence words (~35 entries), cliché openers, negation-pivot"
 },
 {
  "name": "masterball-w/Master-humanizer-skill",
  "stars": 9,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, and no iteration. The checklist is a human-designed rule pass executed by the LLM in a single post-write review. No automated score drives any retry.",
  "meaning_verification": "none — the rules are entirely style/pattern focused. No cosine similarity, NLI, semantic-role, or any other automated meaning gate is mentioned or implied. The genre supplements add domain constraints",
  "fact_preservation": "partial, prompt-level only — Rule 18 (narrative uncompression) requires preserving tool names, version numbers, original quotes/prompt text ",
  "mechanism": "Pure prompt engineering delivered as a Claude Code skill file. SKILL.md (~20 KB) encodes 18 named rules (Rules A/B/C, 1-18) plus a 9-step post-write checklist the LLM executes on its own output. The workflow is write-first, check-second: the LLM writes the article without self-censorship, then does a structured sentence-by-sentence pass applying ea"
 },
 {
  "name": "SilverSpeak (ACMCMC/silverspeak)",
  "stars": 8,
  "category": "unicode-trickery",
  "detector_in_loop": "no — the attack runs once to a target percentage; no detector is consulted before, during, or after substitution; no re-scoring loop of any kind",
  "meaning_verification": "none — substitution is purely at the character byte level; no semantic, syntactic, or lexical check is performed",
  "fact_preservation": "none — numbers, citations, and quoted strings are processed identically to all other characters; if a homoglyph mapping exists for a digit o",
  "mechanism": "Character-level Unicode homoglyph substitution. Four JSON maps are loaded at runtime: identical-character map, Unicode confusables map, OCR-error map, and refined-OCR map (`silverspeak/homoglyphs/homoglyph_replacer.py`). Three attack modes exist: `random_attack` (random substitution up to a target percentage), `greedy_attack` (sequential substituti"
 },
 {
  "name": "cashwu/Humanizer-tw",
  "stars": 8,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector of any kind. The skill contains no scoring step, no external API call, and no feedback loop. Rewriting is single-pass based solely on the pattern catalogue and rubric in the ",
  "meaning_verification": "The 50-point rubric asks Claude to self-assess the rewrite on five named dimensions, but this is a prose checklist inside the prompt, not an automated gate. No cosine similarity, no NLI, no numeral re",
  "fact_preservation": "none — no mechanism for preserving numbers, citations, or named entities. The prompt does mention keeping technical abbreviations and produc",
  "mechanism": "Pure LLM prompt-guide packaged as a Claude Code skill. The entire implementation is a single SKILL.md file (in skills/humanizer-tw/SKILL.md) containing: a catalogue of 24 AI-writing tells across four categories (content, language/grammar, style, communication), rewriting instructions, a 50-point quality rubric across five dimensions (directness, rh"
 },
 {
  "name": "humanizer-x",
  "stars": 8,
  "category": "prompt-guide",
  "detector_in_loop": "No. No external detector is consulted at any stage. The internal verification loop checks a self-scored 8-point heuristic checklist (sentence-length variance, residual AI vocabulary, opener diversity,",
  "meaning_verification": "None. The 8-point checklist measures stylistic properties only (variance, vocabulary, structure). There is no cosine similarity check, no NLI entailment gate, no semantic-role comparison, and no hedge",
  "fact_preservation": "None. The documentation explicitly describes no mechanism for preserving numbers, citations, URLs, or quoted material during transformation.",
  "mechanism": "The entire tool is a Claude Code skill delivered as SKILL.md (47 KB of prompt instructions). There is no executable code, no importable library, and no named functions. The workflow is a four-pass prompt sequence Claude executes inline: (1) Pattern Removal — strip 30 severity-ranked AI fingerprints (vocabulary tells like \"delve\", sentence-length un"
 },
 {
  "name": "walterwritesai/walter-skills",
  "stars": 8,
  "category": "api-wrapper",
  "detector_in_loop": "yes — by prompt instruction, not by code. Every skill instructs Claude to: (1) run Walter's detection API on the draft, (2) if the score is above 30 escalate from balanced to aggressive mode, (3) re-r",
  "meaning_verification": "none — the skill prompts have no meaning-preservation step. The brand-voice skill says \"confirm any factual claims are unchanged\" when rewriting pasted text, but this is an instruction to Claude's gen",
  "fact_preservation": "Keyword locking is the only explicit preservation mechanism: skills instruct Claude to call Walter's \"keyword preservation\" tool to protect ",
  "mechanism": "Twelve markdown prompt files (\"skills\") installed into Claude project instructions. Each skill tells Claude to call the Walter Writes AI commercial API (via \"Walter MCP\", a separately installed MCP connector) for three operations: humanize text (light/balanced/aggressive modes), run AI detection, and lock keywords during rewriting. The skill files "
 },
 {
  "name": "Ramtin-Karbaschi/enHumanizer_Bot",
  "stars": 8,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector anywhere in the codebase. The pipeline applies transformations once and returns. No score-rewrite-rescore loop exists.",
  "meaning_verification": "none. The `TextStructureAnalyzer.ensure_structure_preserved()` checks that paragraph and sentence counts stay within ~10% of the original, but this is a structural count check, not a semantic check. T",
  "fact_preservation": "none. No byte-exact locking of numbers, citations, or quotes is present. The homoglyph substitution in advanced_humanizer.py would corrupt n",
  "mechanism": "Three-layer pipeline, applied in order of priority. (1) Primary path (gemini_api.py): calls Google Gemini (gemini-2.5-flash-preview-04-17) via `model.generate_content(prompt)` with a detailed system prompt from `format_humanize_prompt()` instructing the model to rewrite text to sound human-written. Handles long texts by chunking. (2) First fallback"
 },
 {
  "name": "bam2008/ai-cloaker",
  "stars": 8,
  "category": "unicode-trickery",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration. The user clicks Cloak once and gets back the same text with invisible characters scattered through it.",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers and citations pass through unmodified only because nothing in the text is altered at all (words are untouched); there is no e",
  "mechanism": "Inserts U+034F (COMBINING GRAPHEME JOINER, an invisible combining character) randomly into the text. The `cloak` function in `src/pages/Home.tsx` iterates over every character and with 5% probability appends U+034F after it (skipping positions that already hold one). A `decloak` button strips all U+034F back out. No LLM, no paraphrase, no synonym r"
 },
 {
  "name": "MadsDoodle/Detecting-the-Machine-A-Comprehensive-Benchmark-of-AI-Generated-Text-Detectors-Across-Architectures",
  "stars": 7,
  "category": "research-code",
  "detector_in_loop": "no — the detector is evaluated AFTER humanization to measure degradation, not used to guide or gate the rewrite. There is no score→rewrite→re-score loop of any kind.",
  "meaning_verification": "none — the rewrite prompts instruct the model not to add information or change meaning, but there is no programmatic verification: no cosine similarity check, no NLI entailment gate, no semantic-role ",
  "fact_preservation": "none — no numeral retention check, no citation locking, no byte-exact preservation. Input texts are also hard-truncated to 800 chars before ",
  "mechanism": "Primarily a detector benchmark, not a humanizer. The evasion component lives entirely in notebooks/09_adversarial_humanization.ipynb. It applies a one-shot prompt-based rewrite using Qwen2.5-1.5B-Instruct (4-bit NF4 quantized) at two intensity levels: L1 (\"sound more natural and human, varied sentence lengths, occasional informal language\") and L2 "
 },
 {
  "name": "haidrrrry/humanize-ai-writing",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — There is no detector score used at any point. The SKILL.md workflow has the LLM self-check its own output against the rule catalog, but that is the same LLM re-reading its draft against the same ",
  "meaning_verification": "none — SKILL.md instructs \"Keep meaning, facts, numbers, names, and the author's position intact\" and warns against mangling sentences to remove a single flagged word. These are plain-English instruct",
  "fact_preservation": "none automated — Numbers, names, and citations are mentioned in the instruction text (\"keep facts, numbers, names\") but there is no byte-exa",
  "mechanism": "Two surfaces, same ruleset. (1) PROMPT.md: a paste-in system prompt with 12 hard rules — a banned-vocabulary list (~40 words: delve, tapestry, pivotal, etc.), banned phrase patterns (em dashes, \"not just X but Y\", hollow conclusions, weasel attribution, promotional tone, fake significance, trailing -ing clauses), and voice defaults. The LLM receivi"
 },
 {
  "name": "Joabutt/gptnegative",
  "stars": 7,
  "category": "api-wrapper",
  "detector_in_loop": "no — GPTZero is never called, scored against, or consulted at any point. Despite the \"GPTZero bypasser\" claim, there is no score-rewrite-rescore loop of any kind. One-shot: text in, two spins, text ou",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quoted strings pass through both spinners without any locking or protection; they survive only by chance if t",
  "mechanism": "Chains two external spinner/paraphraser APIs sequentially. In `index.js`, the `rewrite()` function strips line breaks from the input, then POST-s to `api.spinbot.com/spin/rewrite-text` (Spinbot), takes that response, and immediately POST-s it to `backend-spinbot-wrapper-prod.azurewebsites.net/spin/rewrite-text` (a wrapper around free-article-spinne"
 },
 {
  "name": "yulia-glukhova/humanizer-ru-voice",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no detector, and no iteration. The 6-step workflow runs once through Claude and returns output. No re-score-and-repeat loop of any kind.",
  "meaning_verification": "none — the SKILL.md states the tool will not \"fact-check, alter conclusions, or add information,\" but this is a design constraint on the prompt, not a mechanical verification gate. No cosine similarit",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or quotes. The prompt instructs Claude not to change facts, but there is no enforcement ",
  "mechanism": "Entirely a prompt guide — two SKILL.md files (one for Claude.ai custom skills, one for Claude Code CLI) that instruct Claude to follow a 6-step rewrite workflow: (0) optionally load a user-supplied CORPUS.md to fingerprint the author's rhythm, tone, and structural habits; (1) detect format (Telegram post / carousel / Reels script / client report); "
 },
 {
  "name": "Shirhussain/humanize",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — The linter is a static pattern checker, not an evasion-oriented detector. It runs once on a file and reports density. There is no score-rewrite-re-score loop of any kind. The workflow is: LLM app",
  "meaning_verification": "none — The SKILL.md instructs the LLM not to change quotes, numbers, citations, proper nouns, or facts in rewrite mode, but this is an honor-system instruction with no automated check. No cosine simil",
  "fact_preservation": "Instructed but not enforced. SKILL.md rule: \"Never alter direct quotes... Never change citations, numbers, statistics, dates, URLs, or prope",
  "mechanism": "Two-layer approach. Primary layer: a Markdown skill file (SKILL.md) that any agent harness (Claude Code, Cursor, Copilot, Codex) reads as instructions. It encodes 36 numbered AI-writing patterns from Wikipedia's WikiProject AI Cleanup and two arXiv corpus studies, and tells the LLM either to avoid them while generating (generation mode) or to strip"
 },
 {
  "name": "shuohui-air-technology/aigc-humanizer-zh",
  "stars": 7,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — the pipeline is linear: detect → plan → [external LLM rewrites, outside the server] → quality-check. There is no score-rewrite-rescore iteration. The assess_quality tool validates after a single ",
  "meaning_verification": "none in the automated NLI/cosine sense. The assess_quality tool scores 6 dimensions (directness, rhythm, authenticity, information density, academic rigor, detection-resistance) as quality heuristics,",
  "fact_preservation": "Yes for LaTeX formulas: mask_latex extracts formulas into semantic placeholders using a stack-based scanner before any rewrite, and restore_",
  "mechanism": "Two-layer detection + rule-guided rewrite pipeline for Chinese academic text. L1 layer: PatternDetector class (src/detector.py) applies precompiled regex against 16 AI writing patterns (theory-first openings, closing clichés, numbered lists, passive constructions, triple parallelism, vague attribution, etc.) and 7 hard constraints (HC-1 through HC-"
 },
 {
  "name": "open-bbug/humanizer-zh-TW",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. The only quality feedback is an LLM self-scoring rubric (1-10 across five axes: directness, rhythm, trust, authenticity, concision) applied after rewriting. No ",
  "meaning_verification": "none mechanically. The skill instructs the agent to \"preserve core information\" (保留原意) and \"match the intended tone,\" but these are plain-language instructions to the LLM, not automated checks. No cos",
  "fact_preservation": "none. No numeral retention check, no citation locking, no byte-exact preserve mechanism of any kind.",
  "mechanism": "Pure prompt-engineering skill. The repo contains two files: README.md and SKILL.md. SKILL.md is a structured LLM agent skill (compatible with Claude Code, Gemini, Codex) that enumerates 24 named AI-tell patterns in Traditional Chinese (e.g., over-emphasis on historical significance, vague attribution, sycophantic opener, three-part lists, em-dash o"
 },
 {
  "name": "13201722520lbx-art/Content-Rewriter-Skill",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no automated scoring step. The workflow describes human-in-the-loop iteration: after each rewrite round the model asks the user for feedback, and the user decides whether to iterate. No ",
  "meaning_verification": "none — the prompt states \"ensure information completeness is not lost\" as a Constraint, but there is no automated check (no cosine similarity, no NLI, no semantic-role gate). Verification is entirely ",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling. The only relevant constraint is a general rule that \"information richness",
  "mechanism": "A single markdown file (content-rewriter.md) that functions as a Claude Code / Cline skill prompt. It defines a system persona and a six-strategy rewrite protocol targeting the Weipu (维普) AIGC detector: (1) raise perplexity — use non-standard but semantically correct vocabulary, colloquialisms, dialect-tinged words; (2) create burstiness — delibera"
 },
 {
  "name": "hjongc/humanizer-kr",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no AI-text detector in the loop. The README mentions \"quality loops: rewrite, review, and iterate in one prompt\" and the evals directory shows a three-stage sample (initial rewrite, qual",
  "meaning_verification": "none automated. The playbook contains the editorial instruction \"preserve all facts, numbers, and names\" but this is a guideline for the LLM, not a computed gate. No cosine similarity, NLI entailment,",
  "fact_preservation": "editorial guideline only — the playbook and SKILL.md both instruct the LLM not to add or remove facts. No byte-exact locking, no numeral-ret",
  "mechanism": "A Claude Code / Codex skill (SKILL.md prompt) that instructs the host LLM to follow a five-category, 20-pattern Korean writing guide. The rewriting is done entirely by the LLM guided by the prompt; there is no separate model. Alongside the skill lives a standalone offline audit script (`skills/humanizer-kr/scripts/audit_korean_text.py`) that uses 1"
 },
 {
  "name": "dotoricode/korean-humanizer",
  "stars": 7,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, and no rewrite loop. The workflow is single-pass: paste text, LLM applies substitutions, done. The eval harness (scripts/eval-harness.py) checks metric complianc",
  "meaning_verification": "Heuristic quantitative caps enforced by prompt instruction and verified post-hoc by the eval harness: M1 — no more than 20% of sentences modified (measured by normalized Levenshtein distance > 0.20 th",
  "fact_preservation": "Stated as absolute rule #1 in both PROMPT.md and PROMPT.short.md: facts, numbers, proper nouns, citations, and links must not be touched. En",
  "mechanism": "Pure prompt-guide / rule-based rewriter operating entirely inside an LLM. The repo ships two prompt files (PROMPT.md, the full version; PROMPT.short.md, a compact version) and a Claude Code skill file (SKILL.md). When invoked, the LLM is instructed to apply 12 named substitution categories targeting Korean-specific AI tells: empty intensifiers (매우,"
 },
 {
  "name": "Khizer-Data/AI-Text-Humanizer",
  "stars": 6,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — the RewardModel.score() used to decide a 3rd pass is a pure heuristic: it checks average word length, average sentence length, presence of an apostrophe, and presence of filler words like \"well\"/",
  "meaning_verification": "none — the preserve_meaning flag only controls whether the most aggressive transformations are skipped. No cosine similarity, NLI entailment, semantic-role match, or any other semantic comparison is p",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special treatment. The synonym dictionary does include \"number\" as a replaceable no",
  "mechanism": "Rule-based pipeline in app/core/agent.py (HumanizerAgent). Three stacked layers: (1) synonym substitution via a hardcoded ~300-entry dictionary (_load_synonyms); (2) sentence restructuring — clause reordering, passive/active voice flip, sentence split/merge (_reorder_clauses, _change_voice, etc.); (3) unicode/homoglyph injection — Cyrillic, Greek, "
 },
 {
  "name": "teslaproduuction/humanizer_ru",
  "stars": 6,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scorer, no detector call, and no iteration. The skill is a single-pass rewrite guided by pattern rules. Claude applies the rules once and performs a manual self-check scan for hard-ba",
  "meaning_verification": "none — the skill instructs Claude to \"сохранить смысл\" (preserve meaning) as a stated goal, but there is no automated gate (no cosine similarity, no NLI, no semantic-role check). Meaning preservation ",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling. The skill contains no preserve-locking or byte-exact retention logic.",
  "mechanism": "Pure prompt guide delivered as a Claude Code skill plugin. The repo contains no executable code — only markdown skill files. The core file is `skills/humanizer-ru/SKILL.md` (v3.0.0, ~39 KB), a richly detailed instruction document that directs Claude to scan input text for 28+ named AI-tell patterns (called out by Russian name and marker words), rew"
 },
 {
  "name": "puneethkotha/humanizer-workbench",
  "stars": 6,
  "category": "api-wrapper",
  "detector_in_loop": "no — the heuristic scorer runs once before the pipeline and once after to produce a before/after delta shown to the user. The AUDIT stage re-runs detection to refresh the prompt's context, but there i",
  "meaning_verification": "none — the REWRITE prompt instructs the model to \"Preserve all factual information, names, numbers, and core meaning,\" but this is a prose instruction only. No cosine similarity, no NLI entailment che",
  "fact_preservation": "prompt-instruction only — the REWRITE prompt includes \"Preserve all factual information, names, numbers, and core meaning\" (src/humanizer/tr",
  "mechanism": "Three-stage LLM-prompt pipeline using the Anthropic API (claude-sonnet-4-6 by default). Stages: REWRITE (temperature 0.7), REFINE (0.45), AUDIT (0.3); intensity level selects which stages run (light=REWRITE only, medium=REWRITE+REFINE, aggressive=all three). Before any LLM call, two heuristic detectors scan the input — LexicalDetector (src/humanize"
 },
 {
  "name": "diaiq/claude-skill-humanizer",
  "stars": 6,
  "category": "api-wrapper",
  "detector_in_loop": "no — the skill calls the API once (or up to 3 times if the user picks multiple passes), returns the result, and stops. There is no scoring step, no local detector, and no re-score-and-retry loop of an",
  "meaning_verification": "none — the SKILL.md prompt includes the marketing claim that the API \"preserves meaning, facts, and formatting,\" but there is no client-side check (no cosine similarity, no NLI, no semantic-role compa",
  "fact_preservation": "none — the SKILL.md prompt makes no attempt to lock numbers, citations, or quoted text before sending to the API. There is no byte-exact pre",
  "mechanism": "The repo contains exactly one functional file: SKILL.md. That file is a Claude Code skill definition (a markdown prompt) that instructs Claude to call the DiaIQ Humanizer REST API via a single `curl -X POST https://humanizer.diaiq.com/api/humanize` with a JSON body containing the text and a pass count (1–3). There is no local transformation code of"
 },
 {
  "name": "Mangoz1x/GptRewrite",
  "stars": 6,
  "category": "api-wrapper",
  "detector_in_loop": "no — the API handler (`pages/api/gpt/rewrite.js`) validates auth and credit limits, calls the OpenAI API once, stores the result to MongoDB, and returns it immediately. There is no detector call, no s",
  "meaning_verification": "none — output is returned to the caller as-is. No cosine similarity, no NLI, no semantic role match, no diff, nothing.",
  "fact_preservation": "none — numbers, citations, and quotes pass through the raw GPT completion with no locking, no extraction, and no post-hoc reinsertion.",
  "mechanism": "Single-pass prompt injection via the OpenAI legacy completions endpoint. The sole rewrite logic lives in `pages/api/gpt/rewrite.js`: it calls `openai.createCompletion()` with model `text-davinci-003` (a now-deprecated completion model), temperature 0, frequency_penalty 0.5, presence_penalty 0, and max_tokens 3000. The prompt template is hard-coded:"
 },
 {
  "name": "SocialYjj/bypassAIGC",
  "stars": 5,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector, no scoring, no re-scoring, and no iteration. The pipeline is a strictly linear single-pass: polish → enhance, then done.",
  "meaning_verification": "none. The prompt instructs the LLM to keep word count within 30 characters of the original and preserve technical terms, but this is enforced only by instruction, not by any automated gate (no cosine ",
  "fact_preservation": "partial, by instruction only. The system prompt says \"absolutely do not modify technical terms, proper nouns, code snippets, library names, ",
  "mechanism": "Two-stage prompt-based LLM rewriting with no detector loop. Text is split into segments (max 500 Chinese chars) in `split_text_into_segments()` (ai_service.py). Each segment is sent sequentially to an OpenAI-compatible API in a \"polish\" pass (get_default_polish_prompt), then an \"enhance\" pass (get_default_enhance_prompt). For Chinese text the promp"
 },
 {
  "name": "ryuryukke/MINT",
  "stars": 5,
  "category": "research-code",
  "detector_in_loop": "no — there is no rewrite step anywhere in the codebase. It is a static evaluation pipeline: load texts, score with detectors/MIAs, compute AUROC, save. No generation, no paraphrasing, no iterative loo",
  "meaning_verification": "none — no text is produced or transformed, so meaning verification is not applicable.",
  "fact_preservation": "none — not applicable; the tool never modifies text.",
  "mechanism": "MINT does not transform text at all. It is a unified evaluation suite that benchmarks membership inference attacks (MIAs) and AI-text detectors under a shared scoring interface. run.py loads texts from MIMIR (MIA benchmark) or RAID (detection benchmark), passes them through whichever methods are selected (loss, entropy, rank, logrank, reference, zl"
 },
 {
  "name": "humanizer-ja",
  "stars": 5,
  "category": "prompt-guide",
  "detector_in_loop": "no — The \"anti-AI pass\" is a prompted self-critique step where Claude asks itself \"what still reads as AI-generated?\" and checks a qualitative checklist (sentence-length uniformity, connector-word fre",
  "meaning_verification": "none automated. SKILL.md instructs Claude to \"preserve the core of what you want to convey\" (意味を保つ) but provides no programmatic gate — no cosine similarity check, no NLI entailment, no semantic-role ",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special treatment. The instructions do not mention byte-exact locking or any preserve mechan",
  "mechanism": "Purely a Claude Code skill — the entire implementation is SKILL.md, a system-prompt that instructs Claude to act as a Japanese-text editor. When invoked via `/humanizer-ja`, Claude receives the prompt and applies a catalogued 25-pattern scan-and-rewrite in three layers: Layer 1 removes formatting residue (colons, Markdown fragments, emoji); Layer 2"
 },
 {
  "name": "jiji262/humanizer-chinese",
  "stars": 5,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, scorer, or automated feedback. The only \"loop\" is the LLM agent's own internal three-question self-audit before producing the final draft, which is cognitive/prompt-level no",
  "meaning_verification": "Prompt-level only. Task rule 2 says \"preserve every claim from the original, information takes priority over structure.\" Task rule 3 is the strongest gate: no new facts, names, numbers, dates, quotes,",
  "fact_preservation": "Prompt-level only. Task rule 3 explicitly forbids adding facts, names, numbers, dates, citations not in the original. The DeepSeek fingerpri",
  "mechanism": "Pure Markdown agent skill (SKILL.md, ~465 lines). A fork of blader/humanizer localized for Chinese. The entire product is a structured LLM system prompt that instructs an agent to: (1) scan the input against 36 numbered Chinese AI-writing patterns (each with a curated 盯防词 watchlist and before/after example), (2) rewrite while preserving all informa"
 },
 {
  "name": "aint (AI, Ain't)",
  "stars": 5,
  "category": "unicode-trickery",
  "detector_in_loop": "no — single-pass transformation; no scoring step, no iteration, no feedback loop of any kind",
  "meaning_verification": "none — output is visually pixel-identical to input for a human reader; correctness is assumed because no characters are added or removed semantically",
  "fact_preservation": "none explicitly; numerals happen to pass through unchanged only because no digit homoglyphs are in the substitution table, not because of an",
  "mechanism": "Stochastic homoglyph substitution plus invisible character injection. The single function `spoof_text` in `aint.py` iterates over every character and, with probability 1/scramble_aggressiveness (default 10, so ~10% per eligible character), replaces an ASCII letter with a visually identical Unicode codepoint from another script: Latin 'a' → Cyrillic"
 },
 {
  "name": "TwilightXQY/humanizer-zh-codex",
  "stars": 5,
  "category": "prompt-guide",
  "detector_in_loop": "no — The README explicitly states the goal is writing-quality improvement, not detector bypass (\"这个 skill 的目标是提升写作质量，不是承诺绕过 AI 检测器\"). There is no scoring step, no re-score loop, and no mention of any ",
  "meaning_verification": "none — Preservation of meaning is stated as a goal in the instructions to the LLM (\"preserving meaning and factual integrity\"), but there is no automated gate, no cosine similarity check, no NLI entai",
  "fact_preservation": "Instruction-only, not mechanical. SKILL.md says \"never invent numbers, sources, personal experience, quotes, or examples\" and \"Preserve all ",
  "mechanism": "Pure LLM instruction set — the entire repo is two files: SKILL.md (a structured system prompt for Codex) and agents/openai.yaml (UI metadata). SKILL.md defines a 5-step workflow: identify genre, scan 24 named Chinese AI-writing patterns, rewrite preserving meaning, replace vague claims only with facts already in the source, vary sentence rhythm. Th"
 },
 {
  "name": "ll-humanizer-zh",
  "stars": 5,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no re-scoring, and no iteration. A single LLM rewrite pass is the entire pipeline.",
  "meaning_verification": "none — SKILL.md instructs the model to \"protect the core point\" and not invent facts, and says to mark uncertain claims as [to verify: ...], but these are soft instructions to the LLM, not mechanical ",
  "fact_preservation": "Soft prompt only: SKILL.md says do not invent facts, data, or personal experience, and keep exact product names and technical identifiers in",
  "mechanism": "A Claude Code / Codex skill with zero executable code. The entire transformation lives in SKILL.md (the system prompt loaded into the LLM) and three supporting reference markdown files: ai-tone-checklist.md defines 8 Chinese AI-tone patterns; rewrite-patterns.md gives before/after rewrites for each; output-formats.md and style-profile-template.md h"
 },
 {
  "name": "BypassAIGC",
  "stars": 5,
  "category": "api-wrapper",
  "detector_in_loop": "no — the pipeline is open-loop: prompt → LLM → output. No detector scoring at any stage, no re-scoring, no iteration. Once each segment is processed it is committed as final.",
  "meaning_verification": "none — the LLM prompts instruct the model to preserve meaning, but no programmatic check is applied (no cosine similarity, NLI, hedge retention, or semantic-role comparison).",
  "fact_preservation": "partial via prompt instruction only — system prompts forbid modifying technical terms, code snippets, API paths, library names, and formulas",
  "mechanism": "Two-stage LLM rewriting pipeline via an OpenAI-compatible API. Stage 1 (\"polish\") instructs the model to expand terse verb phrases into longer process-describing phrases, add logical connectives, and systematically substitute vocabulary — implemented as a large system prompt in `get_default_polish_prompt()` (ai_service.py). For English text the sam"
 },
 {
  "name": "sut-qi/BypassAIGC",
  "stars": 5,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step anywhere in the pipeline. Text goes through polish → enhance once and is returned. No detector is called, no score threshold governs iteration.",
  "meaning_verification": "none — no cosine similarity, NLI, entailment, or semantic-role check. The only protection is prompt instructions (\"keep technical terms unchanged\", \"maintain paragraph count\", \"same logical structure\"",
  "fact_preservation": "prompt-instruction only — the enhance prompt includes a section \"保持技术准确性\" that tells the LLM not to modify code identifiers, API paths, libr",
  "mechanism": "Two-stage LLM prompt pipeline targeting academic writing. Stage 1 (\"polish\") targets English text: prompts in `ai_service.get_default_polish_prompt()` instruct the model to replace formal Latin-root verbs with phrasal verbs (e.g. \"establish\" -> \"set up\"), break smooth sentence flow via interrupting clauses, start 20-30% of sentences with filler phr"
 },
 {
  "name": "imethanlee/AliMark",
  "stars": 4,
  "category": "research-code",
  "detector_in_loop": "no. The pipeline is linear: generate watermarked text → paraphrase-attack (offline) → detect. There is no rewrite-score-rewrite loop. The detection step in 3_detection.py runs once over pre-attacked f",
  "meaning_verification": "none. The paraphrase-attack scripts apply Pegasus/Parrot/DIPPER/GPT-3.5 to the watermarked text with no check that meaning survived. No cosine similarity gate, no NLI, no semantic-role check.",
  "fact_preservation": "none. No special handling of numbers, citations, or quoted text anywhere in the codebase.",
  "mechanism": "AliMark is a sentence-level LLM watermarking system, not a humanizer. During generation (watermark/alimark.py: generate_watermarked_text), the LLM produces Q candidate next-sentences (default Q=64 via vLLM); each candidate is embedded with a SentenceTransformer (all-mpnet-base-v2) and dot-producted against secret vectors to extract a bit block; the"
 },
 {
  "name": "RAFT (JamesLWang/RAFT)",
  "stars": 4,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — the target detector's `crit()` score is evaluated after every candidate substitution; a replacement is accepted only if it strictly lowers that score. This is a true score-guided greedy search, ",
  "meaning_verification": "POS-tag matching only (NLTK): replacement words must share the original word's part-of-speech tag. The GPT-3.5 prompt requests \"semantic fluency\" but there is no post-generation validation of that cla",
  "fact_preservation": "none — no special handling for numerals, citations, quoted strings, or named entities. Numbers and proper nouns are eligible for substitutio",
  "mechanism": "Proxy-model-guided word substitution. The core `raft()` method in `experiment.py` (lines 497–671) ranks every word in the target text by token probability under a proxy LLM (GPT-2, OPT, Neo, or GPT-J for autoregressive mode; fine-tuned RoBERTa-GPT2 for detection-proxy mode). The lowest-probability tokens are selected as candidates — these are the p"
 },
 {
  "name": "icarodredd/HumanizeAI",
  "stars": 4,
  "category": "api-wrapper",
  "detector_in_loop": "no — single pass, no scoring step at any point",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Single POST to AI21 Studio's paraphrase endpoint (`https://api.ai21.com/studio/v1/paraphrase`) using `jamba-instruct`. A one-line system prompt instructs the model to rewrite AI text to sound human. The call is made in `background.js` (the only logic file, 31 lines). Result is rendered in the popup and a Copy button is appended. No iteration, no lo"
 },
 {
  "name": "nifzaa0/ai-humanizer-tools",
  "stars": 4,
  "category": "other",
  "detector_in_loop": "no — the repo has no executable code at all. Some listed commercial products (Walter Writes AI, Uncheck AI, WriteHuman) are described as bundling a detector alongside the rewriter, but this is marketi",
  "meaning_verification": "none — no code exists to verify anything. The README mentions that \"good humanizers keep your key points and facts intact\" in an FAQ section, but this is marketing prose, not an implemented check.",
  "fact_preservation": "none — no code. GPTinf is described as having a \"freeze list\" feature that locks specific words or phrases, but that is a feature of a third",
  "mechanism": "This repo contains no code whatsoever. It is a single README.md file (plus a folder of screenshots) that lists and describes 25 commercial AI humanizer SaaS products — Walter Writes AI, Undetectable AI, QuillBot, Writesonic, HIX.AI Bypass, StealthWriter, WriteHybrid, etc. The repo itself performs no text transformation. Each tool entry is a marketi"
 },
 {
  "name": "tweetpie/stance-directed-humanizing-ai",
  "stars": 4,
  "category": "fine-tuned-model",
  "detector_in_loop": "yes — but for a completely different purpose. The toxic content classifier runs on every generated tweet and presumably gates re-generation until the output is non-toxic. It is NOT an AI-text detector",
  "meaning_verification": "none — the system generates new tweets from structured prompts; it does not transform an existing passage, so there is no input meaning to preserve or verify.",
  "fact_preservation": "none",
  "mechanism": "Three-stage pipeline applied to tweet generation from scratch (not transformation of existing text): (1) a fine-tuned LLM (tweetpie/stance-directed-tweet-generator on HuggingFace) generates tweets conditioned on ideology camp, pro/anti/neutral aspects and entities; (2) a toxic content classifier (tweetpie/toxic-content-detector) labels each output "
 },
 {
  "name": "saplingai/sapling-py",
  "stars": 4,
  "category": "api-wrapper",
  "detector_in_loop": "no — the library exposes individual API calls; it implements no score-rewrite-rescore loop. A consumer would have to build that loop themselves by alternating aidetect() and rephrase() calls.",
  "meaning_verification": "none — the client forwards text to the API and returns raw JSON; no cosine similarity, NLI, or any other semantic check is performed at any layer of this library.",
  "fact_preservation": "none — no numeral retention, quote locking, or citation preservation logic exists anywhere in the codebase.",
  "mechanism": "Thin synchronous HTTP wrapper around Sapling's REST API using `requests.Session`. The relevant method for detection evasion use is `aidetect(text, sent_scores=None)` in `sapling/client.py` — it POSTs to `/api/v1/aidetect` and returns a float 0-1 probability that the text is AI-generated, plus optional per-sentence scores. A separate `rephrase(text,"
 },
 {
  "name": "personal-humanizer-maker",
  "stars": 4,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no AI-text detector anywhere in the loop. The \"score → rewrite → re-score\" iteration in untell is replaced here by a style-band convergence gate (`roundtrip_check.py`) that measures whet",
  "meaning_verification": "Partial but real. The meaning-invariance covenant (`references/ironclad.md`) is injected verbatim as §0 of every emitted skill. It prohibits changing facts, numbers, units, years, names, proper nouns,",
  "fact_preservation": "Yes, explicitly. The ironclad covenant protects: facts, numbers, units, years, names, proper nouns, causality, order, and citations (wikilin",
  "mechanism": "Three-layer pipeline acting as a \"skill factory.\" Layer 1 (CODE, deterministic, stdlib-only): `scripts/profile_corpus.py` reads one or more author sample documents and emits a `quant_profile.json` with statistical distributions (mean/median/p90/max/stdev) across 7 style axes — sentence architecture, register/modality, lexical register, cohesion/arg"
 },
 {
  "name": "LangeVC/txtHumanizer",
  "stars": 4,
  "category": "prompt-guide",
  "detector_in_loop": "no — the tool analyses AI-tell patterns at the start (ANALYSE stage) and rewrites once (FINETUNE stage), but there is no automated re-scoring of the output and no iteration. The user can re-run manual",
  "meaning_verification": "none — the \"Texttreue\" (text fidelity) slider lets the user declare a preference (conservative / balanced / bold), but there is no automated semantic similarity check, NLI entailment gate, or any othe",
  "fact_preservation": "none — no numeral-retention check, no citation preserve-locking, no byte-exact freezing of quoted strings. Numbers and citations can be alte",
  "mechanism": "A Claude Code / Claude Desktop skill (distributed as SKILL.md via Capacium). There is no executable program code — the entire implementation is a structured system prompt that instructs Claude to run a three-stage pipeline: (1) ANALYSE: score input text against 14 quick-test criteria (K1–K14) plus an optional 29-criterion Wikipedia-sourced longlist"
 },
 {
  "name": "daniel-bogale/anti-ai-writing-humanizer",
  "stars": 4,
  "category": "prompt-guide",
  "detector_in_loop": "no — the heavy pass iterates to self-convergence (the LLM re-reads its own output until no new edits are found), but there is no external detector, no numerical score, and no re-score step. It is a pr",
  "meaning_verification": "none — the SKILL.md instructs the LLM to \"Preserve meaning, facts, numbers, names, quotes, and citations exactly\" as a prose directive, but there is no automated semantic gate (no cosine similarity ch",
  "fact_preservation": "none automated — house-rules.md and SKILL.md contain the instruction \"Preserve... numbers, names, quotes, and citations exactly\" and the wri",
  "mechanism": "Pure prompt-guide packaged as an LLM agent skill. The entire implementation is three Markdown files: SKILL.md (the operative instructions), references/write-human.md (prescriptive ruleset synthesized from Wikipedia \"Signs of AI Writing\" and the vendored Conor Bronsdon avoid-ai-writing engine), and references/house-rules.md (user-editable banned wor"
 },
 {
  "name": "humanizer-semitic",
  "stars": 4,
  "category": "prompt-guide",
  "detector_in_loop": "No. Stage 3 has a 50-point quality rubric (five dimensions, each 1-10: Directness, Rhythm, Authenticity, Density, Rhetoric) that the LLM self-scores before and after rewriting, and the skill requires ",
  "meaning_verification": "The Stage 2 instructions include an explicit principle \"Preserve meaning — the humanized text must convey exactly the same information and argument as the original. If in doubt, lean toward the origin",
  "fact_preservation": "None. There is no mention of protecting numbers, citations, statistical claims, or quoted material. No byte-exact preserve-locking of any ki",
  "mechanism": "A Claude Code skill package (four SKILL.md prompt files, one per language variant) that instructs the LLM to run a fixed three-stage workflow: Stage 1 Identify — read the full text without changes, flag every pattern violation from a numbered catalogue, assign a baseline score on a 50-point rubric; Stage 2 Rewrite — work paragraph-by-paragraph appl"
 },
 {
  "name": "HuzefaUsama25/ai-humanizer-api",
  "stars": 4,
  "category": "api-wrapper",
  "detector_in_loop": "no — The API response includes a `detection_score` field, but there is no client-side loop, no re-scoring, and no iteration logic in the repo (there is no code at all). Whether the server internally i",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Purely a documentation/client-stub repo — two files (README.md + LICENSE), zero source code. It describes POSTing text to a commercial endpoint at https://whbserver.com/api/v1 (the \"WriteHybrid\" service) with a Bearer token, a `complexity` field (simple/medium/complex), and a `purpose` field (general/seo/academic/marketing). The server returns huma"
 },
 {
  "name": "lxgicstudios/humanize-cli",
  "stars": 4,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — The `transformText` function in `src/index.js` computes a before-score and an after-score using the local scoring module and prints the delta, but this is display-only. There is no loop: substitu",
  "meaning_verification": "none — No cosine similarity, NLI entailment, semantic-role matching, or any other meaning check is performed. The transform applies dictionary substitutions blindly.",
  "fact_preservation": "none — Numbers, citations, and quoted material receive no special treatment. They are subject to the same regex substitution pass as everyth",
  "mechanism": "Pure static rule-based substitution with no LLM involvement. The `transform` command (driven by `src/suggestions.js` → `suggestions.transform()`) does two things: (1) replaces entries from an ~90-term AI_VOCABULARY dictionary in `src/patterns.js` with simpler alternatives (e.g. \"leverage\" → \"use\", \"delve into\" → \"explore\"), and (2) contracts formal"
 },
 {
  "name": "nagameTW/humanizer-zh-tw",
  "stars": 4,
  "category": "prompt-guide",
  "detector_in_loop": "no — Pattern clustering is assessed by the LLM reading the skill instructions, not by a separate detector model. There is no score-rewrite-rescore loop; a single pass applies the 46-pattern checklist ",
  "meaning_verification": "A never_fabricate hard rule (tagged <never_fabricate> in SKILL.md) forbids adding any fact, name, date, or citation not literally present in the source text. Concretising vague statements by inference",
  "fact_preservation": "Partial. Pattern 46 detects suspicious numeric precision or unverifiable quotes and appends a 〔需查證來源〕 marker rather than replacing or removi",
  "mechanism": "Pure Markdown skill (SKILL.md, 511 lines) that instructs a supporting LLM agent (Claude Code, Codex CLI, etc.) to scan input text for 46 categorised AI-writing patterns and rewrite to eliminate them. No executable code does the rewriting — the skill is a structured system-prompt loaded into the agent context. Pattern detection uses \"cluster evidenc"
 },
 {
  "name": "Cade250/chinese-academic-humanizer",
  "stars": 4,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass heuristic; no scoring step, no re-check after rewrite, no iteration described anywhere",
  "meaning_verification": "none — the skill instructs the LLM to \"preserve core logic and technical terminology\" and avoid deleting subjects, but there is no automated gate (no cosine similarity, no NLI check, no semantic-role ",
  "fact_preservation": "none explicitly documented — specialized domain terms (e.g. \"梯度爆炸\") are mentioned as things to leave untouched, but there is no byte-exact l",
  "mechanism": "A Claude Code / OpenCode skill (SKILL.md + references/patterns.md) that injects prompt rules into the LLM context. Two-phase heuristic: (1) scan the LaTeX source to decide whether the passage is already natural; (2) if not, apply one of three strategies (simplify, rewrite, expand) guided by six named AI-tell categories (translation-style long sente"
 },
 {
  "name": "xzy111222333/paper-ai-humanizer",
  "stars": 3,
  "category": "other",
  "detector_in_loop": "no — there is no detector call anywhere in the source. The metric field returned by runLocalAdapter is the string literal \"未接入检测服务\" (detection service not connected).",
  "meaning_verification": "none — the output is not derived from the input at all, so meaning verification is moot.",
  "fact_preservation": "none — numbers, citations, and quotes in the input are discarded; the output is always one of two fixed sample sentences.",
  "mechanism": "The repo is a static HTML/JS frontend demo shell with zero transformation logic. The single processing file, src/pipeline.js, exports a runPipeline function that walks through four named stages (prepare, segment, rewrite, review) by sleeping 180 ms between each, then calls runLocalAdapter, which sleeps another 260 ms and returns one of exactly two "
 },
 {
  "name": "Zeng-xiangkai/humanizer-document-zh",
  "stars": 3,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector of any kind. No scoring, no iteration, no re-score step. Single-pass rewrite driven entirely by pattern rules in the prompt.",
  "meaning_verification": "none automated. The \"零捏造原则\" (zero-fabrication principle) in SKILL.md is a prompt-level instruction telling Claude not to add data, metrics, citations, or new assertions — it is not a mechanical check.",
  "fact_preservation": "Prompt-level only. The zero-fabrication principle explicitly lists: no new numbers, no new experimental setups, no new citations, no new con",
  "mechanism": "Pure prompt engineering packaged as a Claude Code Skill. The repo contains no executable code — only markdown files. SKILL.md instructs Claude to act as an \"INFP paper editor\" and process text in a single pass: (1) read references/patterns.md to identify 28 Chinese AI-writing patterns (grouped into first/second/third priority tiers), (2) rewrite fl"
 },
 {
  "name": "peggywritesforyou",
  "stars": 3,
  "category": "detector-with-evasion",
  "detector_in_loop": "yes — local RoBERTa (roberta-base-openai-detector) is the inner loop gate for every sentence rewrite in the adversarial_loop; Sapling AI and ZeroGPT APIs are queried at the end of each full pipeline r",
  "meaning_verification": "none — the only output quality check is a length ratio guard (candidate must be ≥40% of the original's word count). No cosine similarity gate, no NLI entailment check, no hedge/numeral/citation retent",
  "fact_preservation": "none — no numeral retention, no citation detection, no preserve-locking. Numbers, quotes, and citations are passed through the LLM rewrites ",
  "mechanism": "7-phase pipeline implemented in pipeline.py / nlp_utils.py. Phase 0: spaCy splits long sentences at subordinate conjunctions (spacy_restructure). Phase 1: for texts ≥150 words, runs semantic extraction → regeneration across two different LLMs (run_semantic_regen); shorter texts run a 4-pass chunked LLM loop (process_chunk) with different Groq model"
 },
 {
  "name": "skpig/B4-watermark-attack",
  "stars": 3,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — attack.sh runs the paraphrase once, then calls the detector once for evaluation only; there is no score-rewrite-re-score loop",
  "meaning_verification": "none — the paraphrase prompts instruct the LLM to preserve meaning in natural language, but there is no automated semantic check, NLI gate, cosine similarity filter, or any other programmatic verifica",
  "fact_preservation": "none — no special handling for numbers, citations, named entities, or quoted text",
  "mechanism": "Black-box bi-directional contrastive decoding. Three models are loaded simultaneously: a paraphrase model (P_f, the fluent generator), an amateur proxy model (P_w, fine-tuned to approximate the watermarked distribution), and the clean base checkpoint of P_w (for approximation-error adjustment). At every beam-search step the custom LogitsProcessor i"
 },
 {
  "name": "amondnet/yoonmoon",
  "stars": 3,
  "category": "prompt-guide",
  "detector_in_loop": "yes, weakly. The polish-all orchestrator runs yoonmoon:detect at Phase 1 (pre-diagnosis) and again at Phase 6 (QA re-detection) to confirm AI signals decreased. However both calls go to the same Claud",
  "meaning_verification": "Yes, prompt-based. humanize Phase 3 runs a 6-point LLI self-check against the original: (1) fact preservation, (2) meaning equivalence, (3) change rate appropriateness, (4) genre/register consistency,",
  "fact_preservation": "Explicitly required by prompt instruction: proper nouns, numbers with units, dates, currency, direct quotations, code/URL/identifiers, and c",
  "mechanism": "Pure prompt-guide Claude Code plugin. All six skills (humanize, detect, proofread, translate-polish, restyle, polish-all) are SKILL.md markdown files with structured workflow instructions for Claude to follow. The package.json has zero runtime dependencies — only eslint and prettier as devDependencies. No JavaScript code performs any text transform"
 },
 {
  "name": "horisystems/humanicer",
  "stars": 3,
  "category": "api-wrapper",
  "detector_in_loop": "unclear — The README claims \"its AI content detector confirms that the text is undetectable,\" which implies some detection step, but no source code exists to confirm whether this is a genuine score-re",
  "meaning_verification": "none — No source code; no verifiable meaning gate of any kind.",
  "fact_preservation": "The README describes \"Keyword Freezing\" (locking brand names, SEO phrases, technical jargon), but this is a marketing claim with no code to ",
  "mechanism": "No source code exists in the repository. The repo contains only README.md, LICENSE, and CODEOWNERS. The README markets a closed-source SaaS at humanicer.com. Transformation technique is described only as \"proprietary technology\" that rewrites AI text into \"natural, human-like content.\" No files, functions, models, or algorithms are present to inspe"
 },
 {
  "name": "decurved-agreement62/humanizalo",
  "stars": 3,
  "category": "prompt-guide",
  "detector_in_loop": "no — the \"self-interrogation\" second pass is the LLM scoring its own output against the internal 6-dimension rubric; no external AI-text detector (GPTZero, Originality, etc.) is called at any point",
  "meaning_verification": "none — the six scoring dimensions (directness, rhythm, trust, authenticity, density, soul) are subjective LLM self-assessments produced by the same model doing the rewrite; there is no cosine similari",
  "fact_preservation": "none — no mention of numeral retention, citation locking, or quote preservation anywhere in the skill or reference files",
  "mechanism": "Pure prompt-engineering / Claude Code skill. The entire tool is a SKILL.md file that instructs an LLM (designed for Claude Code) to run a structured editing loop: (1) scan input for 40 documented AI-tell patterns across five categories (content inflation P01-P08, vocabulary P09-P14, structure P15-P26, formatting P27-P31, communication artifacts P32"
 },
 {
  "name": "craigtrim/pystylometry",
  "stars": 3,
  "category": "other",
  "detector_in_loop": "no — there is no rewrite step at all, so no loop is possible. The library detects; it does not transform.",
  "meaning_verification": "none — no text is produced so nothing to verify.",
  "fact_preservation": "none — the library never modifies text, so the question is moot.",
  "mechanism": "Pure stylometric measurement library — no text transformation at all. Provides 50+ analytic metrics across 11 modules (lexical diversity via TTR/MTLD/Hapax, readability via Flesch/Gunning-Fog/SMOG, authorship attribution via Burrows Delta/Zeta/Kilgarriff chi-squared, syntactic parse-tree stats, n-gram entropy, dialect markers, intra-document drift "
 },
 {
  "name": "humantone-mcp",
  "stars": 3,
  "category": "api-wrapper",
  "detector_in_loop": "no — detect_ai and humanize are independent MCP tools. There is no score-then-rewrite-then-rescore loop anywhere in the server code. A human user could call them in sequence, but the server itself pro",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Thin MCP adapter over the closed HumanTone commercial API. index.js calls client.humanize() and client.detect() from the humantone npm SDK (v0.0.2), which makes HTTP requests to HumanTone's backend. The open-source layer does only: parameter normalization (strength level, output format, word limits by plan tier), error-code mapping (mapError()), an"
 },
 {
  "name": "chongzixuan-ai/humanizer-mcp-server",
  "stars": 3,
  "category": "api-wrapper",
  "detector_in_loop": "no — the only tool is detection; there is no rewrite call, no scoring loop, no iteration",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "The repo ships a single TypeScript file (`src/index.ts`) that exposes one MCP tool: `detect`. It POST-requests `https://api.edgeshop.ai/rewrite/text-detection` with the user's text and a list of detector names (COPYLEAKS or HEMINGWAY), returns the JSON response, and constructs a redirect URL to `https://pre-www.text2go.ai/?utm_source=claude_mcp&tas"
 },
 {
  "name": "RobinZorro86/humanizer-zh-plus",
  "stars": 3,
  "category": "prompt-guide",
  "detector_in_loop": "no — the repo explicitly disclaims detector evasion (\"不提供 AI 检测规避保证\" in v2.0.0 release notes and echoed in reports/output-risk-profile.md). There is no score→rewrite→re-score loop of any kind. The 10-",
  "meaning_verification": "none mechanical. SKILL.md step 4 instructs the LLM \"禁止编造\" (no fabrication) and the output format section says not to add facts, numbers, dates, or quotes not in the source. These are prompt-level rule",
  "fact_preservation": "instruction-only. SKILL.md tells the LLM to preserve facts, terminology, and stance, and the output format section lists what must not be ad",
  "mechanism": "Pure LLM instruction set — no executable code. The entire tool is SKILL.md, a Claude Code / agent-skills prompt template defining a 38-pattern catalogue and a 10-step rewriting protocol. The LLM is instructed to scan input text against the patterns and replace AI-writing artifacts with more natural Chinese. Patterns 1-33 are ported from blader/huma"
 },
 {
  "name": "pqcqaq/BypassAIGC-Skill",
  "stars": 3,
  "category": "prompt-guide",
  "detector_in_loop": "no — the project explicitly disavows scoring against or targeting any named AI-text detector (GPTZero, Turnitin, Zhiwang, etc.); there is no score-rewrite-rescore loop of any kind",
  "meaning_verification": "Protected-token multiset comparison via latex_segmenter.py check: citation keys (\\cite{}), cross-reference labels (\\ref{}, \\label{}), environment begin/end pairs, and include paths are compared betwee",
  "fact_preservation": "LaTeX-structural: \\cite{}, \\citep{}, \\citet{}, \\ref{}, \\autoref{}, \\eqref{}, \\label{}, all math environments (equation, align, gather, multl",
  "mechanism": "A Codex/OpenAI Agents skill (SKILL.md + Python helper scripts) for LaTeX academic prose revision. Pipeline: (1) latex_segmenter.py extracts editable prose segments from .tex files while marking LaTeX commands, citations, math, and environments as immutable protected tokens; (2) chinese_ai_style_lint.py heuristically flags Chinese thesis sentences t"
 },
 {
  "name": "monologg/humanizer-kr",
  "stars": 3,
  "category": "prompt-guide",
  "detector_in_loop": "No. The 2-pass loop is a self-critique prompt within the same LLM call (\"아래 글이 왜 AI가 쓴 것처럼 보이는가?\" / \"이제 AI가 쓴 것처럼 보이지 않게 고쳐라\"), not a score-rewrite-rescore loop against an external detector. No detect",
  "meaning_verification": "Minimal LLM self-check only. The process step list includes \"의미 보존 — 핵심 메시지를 유지\" (preserve meaning — maintain core message) and a post-correction checklist (\"does it sound natural when read aloud?\", \"",
  "fact_preservation": "None. There is no mention of byte-exact locking of numbers, citations, or quotes anywhere in the skill or references. Numbers are not protec",
  "mechanism": "Pure LLM prompt skill — no executable code. The repo contains two files that matter: README.md (documentation) and SKILL.md (the actual skill prompt installed via `npx skills add`). SKILL.md is a structured system prompt that instructs Claude Code (or compatible LLM agents) to rewrite Korean text by scanning for 38 named patterns across 5 categorie"
 },
 {
  "name": "UndetectableAIDiscord",
  "stars": 2,
  "category": "api-wrapper",
  "detector_in_loop": "no — single-pass API call, no scoring at any point, no iteration",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes are passed through unmodified only insofar as the Undetectable AI API happens to preserve them; the bo",
  "mechanism": "Thin Discord bot wrapper around the Undetectable AI commercial API. In undetectable.js, the `humanizeText(content, readability, purpose)` function POSTs to `https://api.undetectable.ai/submit` with the message text and two parameters (readability tier and purpose), then polls `https://api.undetectable.ai/document` every 30 seconds until status is \""
 },
 {
  "name": "danieleschmidt/lm-watermark-lab",
  "stars": 2,
  "category": "research-code",
  "detector_in_loop": "No. The RobustnessEvaluator applies random synonym swaps then calls detect() to measure watermark survival — a one-shot stress test, not a score→rewrite→re-score loop. There is no iterative rewriting ",
  "meaning_verification": "None. The paraphrase simulation replaces tokens with hardcoded synonyms (a ~50-word dict in robustness.py) and never checks semantic equivalence of the result.",
  "fact_preservation": "None. Numbers, citations, and proper nouns receive no special handling.",
  "mechanism": "Implements three post-generation watermarking algorithms on word/token sequences — no LLM or logit access required. (1) GreenRedWatermark (green_red.py): hashes the previous token with a secret key to partition the vocabulary into green/red lists; replaces red tokens with same-first-letter green alternatives; detects via one-sided z-test on green f"
 },
 {
  "name": "RLCracker",
  "stars": 2,
  "category": "adversarial-perturbation",
  "detector_in_loop": "No during training — the core novelty is detector-free: it never queries the watermark detector while learning, using distributional KL rewards instead. Yes at evaluation time only: evaluation.py call",
  "meaning_verification": "Partial — PSP semantic similarity (a trained paraphrase-quality model from metrics/p_sp_utils/psp/) and sentence-level cosine similarity (sentence-transformers/all-MiniLM-L6-v2) are used as reward sig",
  "fact_preservation": "None. No protection for numerals, citations, or quoted strings is implemented anywhere in the codebase.",
  "mechanism": "Trains a rephrasing LLM (e.g., Qwen3-8B) via GRPO (Group Relative Policy Optimization, a PPO variant) to paraphrase watermarked text in a way that removes watermark signals. The training reward in GRPOTrainer_reph.py is a weighted sum of three signals: (1) a distributional KL reward — kl_reward = KL_wm - KL_h — which rewards moving the output token"
 },
 {
  "name": "yllscar728/academic-humanizer-zh",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector call, no scoring step, no iteration. A single LLM pass is directed by the prompt rules. No re-scoring loop of any kind.",
  "meaning_verification": "none — the SKILL.md instructs the LLM to preserve meaning (terms, data, conclusions) but there is no automated gate, cosine-similarity check, NLI pass, or any programmatic verification that meaning su",
  "fact_preservation": "Prompt-level only. SKILL.md lists locked items (terminology, data, years, institution/person names, research conclusions, citation pointers)",
  "mechanism": "Pure Codex skill (structured markdown prompt set). The repo contains three files — README.md, SKILL.md, and references/rewrite-checklist.md — and zero executable code. SKILL.md is a ~200-line system prompt that instructs Claude Code / Codex to rewrite Chinese academic text by: (1) scanning for a named catalogue of Chinese AI tells (套话 openers like "
 },
 {
  "name": "ywy5734/paper-humanizer-skill",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. The skill is single-pass prompt-guided rewriting. No scoring, no iteration, no re-check after rewriting.",
  "meaning_verification": "Prompt-only self-check via a \"Quality Checklist\" section in SKILL.md (verify citation markers, formulas, variables, abbreviations, figure/table labels, and data are not lost; no unsupported facts adde",
  "fact_preservation": "Instructional only. SKILL.md explicitly tells the LLM not to change \"data, metrics, sample sizes, statistical claims, years, or citation mar",
  "mechanism": "Pure LLM prompt/instruction skill. There is no executable code. The entire implementation is academic-humanizer/SKILL.md — a detailed system-prompt document that instructs a language model to (1) diagnose nine named AI-tell pattern categories (generic openings, empty significance, mechanical parallelism, synonym-substitution damage, excessive hedgi"
 },
 {
  "name": "Para_Paper (noora-noureldin2000/Para_Paper)",
  "stars": 2,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no detector anywhere in the codebase. The pipeline is strictly one-way: text in, transformed text out. No scoring, no threshold check, no re-rewrite loop.",
  "meaning_verification": "none — no cosine similarity, no NLI gate, no back-translation, no semantic-role check. The Gemini path asks the model to preserve meaning in the prompt but does not verify that it did.",
  "fact_preservation": "Partial, medical domain only. The medical_paraphrase() function leaves unknown words untouched and only substitutes words found in MEDICAL_S",
  "mechanism": "Two-tier pipeline in backend/agent_wrapper.py. Primary path (no API key): run_local_simulation() applies a hardcoded SIMULATION_DICTIONARY (~15 word entries, e.g. \"show\"→\"elucidate\") for synonym swaps, plus clean_cliches() removing a fixed list of AI clichés (delve, tapestry, underscore, em-dashes), and medical_paraphrase() which substitutes agains"
 },
 {
  "name": "hsannn/ditto",
  "stars": 2,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — detect.py is a post-hoc evaluation tool only. It measures TPR / p-value / perplexity on already-generated spoofed text. There is no generate-score-rewrite loop.",
  "meaning_verification": "none — the only quality signal checked is perplexity (via a Llama-2-7b PPL model in detect.py). No semantic similarity, NLI, or any content-preservation gate is applied.",
  "fact_preservation": "none",
  "mechanism": "Three-stage knowledge-distillation watermark forgery. (1) A student model (e.g., Llama-3.2-3B) is supervised-fine-tuned with LLaMA-Factory on 10 000 samples generated by a watermarked victim/teacher model (generate_training_data.py / generate_watermarked_data.py). (2) The Extracted Watermark Signal (EWS) is computed by diffing the logit distributio"
 },
 {
  "name": "AInnovateLab/watermark-collision",
  "stars": 2,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — detectors are run at the end of each generation pass (rephrase.py scores with both old and new detector and writes results to JSONL) but the scores are not used to gate further rewrites. There is",
  "meaning_verification": "none — no cosine similarity, NLI, BLEU, or any semantic check is applied to the rephrased output before it is accepted.",
  "fact_preservation": "none — numbers, citations, and quotes receive no special treatment; the LLM paraphraser can freely alter them.",
  "mechanism": "Watermark collision attack: takes text already watermarked by LLM A (scheme KGW/SIR/PRW/RDW/GS, key K1) and feeds it through a second LLM (default: Llama-2-7B-GPTQ) configured to generate with its own watermarking scheme (key K2). Because the second watermark's logit bias interferes with the token distribution shaped by the first, the original wate"
 },
 {
  "name": "ia-espana/humanizador-de-ia",
  "stars": 2,
  "category": "other",
  "detector_in_loop": "no — there is no code of any kind, so no loop is possible",
  "meaning_verification": "none — no implementation exists to verify anything",
  "fact_preservation": "none",
  "mechanism": "No code exists. The repository contains a single README.md (13 KB) that is a Spanish-language promotional guide for a closed-source commercial SaaS product called \"Bypass de Hix AI\" (part of HIX.AI). The README explains humanization conceptually — vocabulary/syntax/style adjustments — but provides zero implementation. It links to the commercial ser"
 },
 {
  "name": "kizmocuDev/content-humanizer",
  "stars": 2,
  "category": "other",
  "detector_in_loop": "no — there is no source code of any kind, so no scoring loop is possible to verify or rule out. The README never mentions detection or scoring.",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No source code is present. The repository contains only a README and a single binary zip file (`suffragial/humanizer_content_2.0.zip`). All four commits are README edits. The README describes \"advanced algorithms to improve text quality\" and \"tone/style adjustment\" but provides zero technical detail — these are pure marketing phrases. The actual tr"
 },
 {
  "name": "omaelbaz/Ai-humanizer",
  "stars": 2,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — single pass, no scoring at any stage",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes pass through the regex substitution and sentence-shuffle steps with no protection",
  "mechanism": "Pure Python, no LLM, no external dependencies. A single file (Humanizer.py) runs a four-stage pipeline: (1) context_aware_substitution — regex-replaces a hardcoded list of ~14 formal words (utilize, commence, leverage, etc.) and ~5 technical words with randomly selected informal synonyms; (2) enhance_flow — detects keywords in the previous paragrap"
 },
 {
  "name": "ghostproof-lite",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scorer, no rewrite loop of any kind. The user applies the rules once at generation time by prepending the prompt.",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes receive no special treatment",
  "mechanism": "A single Markdown file (ghostproof-lite.md) containing 15 editorial rules framed as a system-level prompt. The user pastes it before their writing instructions in any LLM chat. The rules target fiction-specific AI tells: eliminate em dashes, remove semicolons, cut perception-filter phrases (\"she noticed\"/\"he saw\"), avoid \"the way\" and \"the kind of "
 },
 {
  "name": "codeproexpert1/AutoHumanize-Automated-DOCX-Text-Humanization",
  "stars": 2,
  "category": "api-wrapper",
  "detector_in_loop": "yes, but only indirectly and only in finaltexttohuman.py. The texttohuman.com site surfaces its own detection scores as colored mark elements in the UI; the script reads those CSS classes (bg-yellow-1",
  "meaning_verification": "none. No cosine similarity, NLI entailment, hedge preservation, semantic-role match, or any other gate. If the site's rewrite changes meaning, it is accepted unconditionally.",
  "fact_preservation": "none. Numbers, citations, and quoted strings are passed into the textarea along with surrounding prose and are subject to whatever rewrite t",
  "mechanism": "Selenium browser automation wrapper around texttohuman.com. The pipeline is: (1) read DOCX via python-docx, preserving heading/list/bold structure; (2) split into ~1200-word chunks via split_text_by_words(); (3) for each chunk, launch an undetected_chromedriver Chrome session, navigate to texttohuman.com, paste text via clipboard (pyperclip + Ctrl+"
 },
 {
  "name": "Intern4711/Better_Humanizer",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rule-based revision with no scoring, no re-score after edit, and no iteration. The skill does not call any detector at any point.",
  "meaning_verification": "none — the prompt instructs Claude to \"preserve meaning and the author's real content\" as a directive, but there is no automated check (no cosine similarity, no NLI, no semantic-role comparison). Comp",
  "fact_preservation": "Prompt-level only: the skill includes a hard rule \"never invent facts to fill gaps\" and states that removing puffery that leaves nothing beh",
  "mechanism": "A Claude skill (packaged prompt) that instructs Claude to revise text against a structured 8-category checklist of AI tells, sourced from Wikipedia's \"Signs of AI writing\" field guide. There is no code — the entire transformation is performed by Claude following the instructions in SKILL.md and references/signs-checklist.md. The workflow is: read t"
 },
 {
  "name": "Neptuneland/AI-humanizer-for-academic-writing",
  "stars": 2,
  "category": "api-wrapper",
  "detector_in_loop": "no — no detector, no scoring, no iteration loop anywhere in the repo; the concept is not mentioned except as a marketing claim (\"bypasses GPTZero, Turnitin\") on the landing page",
  "meaning_verification": "none — no verification code exists; the README asserts \"Content Preservation\" as a differentiating feature but it is purely a marketing claim",
  "fact_preservation": "claimed in marketing only — README asserts automatic detection and preservation of APA, MLA, Chicago, and Vancouver citations; no implementa",
  "mechanism": "The repo contains exactly four files: README.md, LICENSE, style.css, and index.html. The index.html is a static landing page whose only JavaScript (a single inline script block at the bottom) cycles through three hardcoded before/after example pairs for display purposes — no HTTP calls, no model invocations, no transformation logic of any kind. All"
 },
 {
  "name": "Cam3L1/ghost-skill",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — the self-check is a 10-question qualitative checklist the LLM performs by reasoning, not a real detector call. The README advises the user to manually paste the output into GPTZero/Originality.ai",
  "meaning_verification": "none — no cosine similarity, no NLI entailment check, no semantic-role match, no hedging-preservation gate. The \"Read Aloud Test\" and 10-point self-check are qualitative and LLM-reasoned, not mechanic",
  "fact_preservation": "none — no byte-exact locking of citations, numbers, or quoted material. Numbers are not mentioned in the preservation context at all. The wo",
  "mechanism": "A Claude Code skill installed as SKILL.md into ~/.claude/skills/ghost/. It operates in two layers, both expressed entirely as natural-language instructions the LLM follows — no executable code exists. Layer 1 is a 500-word prompt-injection block the user pastes before generation; it mandates perplexity rules (unpredictable word choices, idiom injec"
 },
 {
  "name": "eluckydog/stylized-writing-workshop",
  "stars": 2,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — their own AI-taste detector (edge_detector_essay, deterministic/rule-based) is in the loop: score → LLM revise → re-score → repeat up to max_iterations. However, NO external commercial detector ",
  "meaning_verification": "none — no cosine-similarity gate, no NLI entailment check, no semantic-role match. Quality checks measure style compliance (rhythm, rhetoric, argument structure, AI-word frequency) and citation hygien",
  "fact_preservation": "Partial, generation-side only. citation_guard.py detects unsourced numeric and percentage claims in the output and flags or rejects them (P0",
  "mechanism": "Two-layer pipeline. Layer 1 (generation): LLM-driven autonomous writing guided by style-profile agent prompts in `agents/` (maqianzu, lukewen, jiubian, natgeo styles — all named Chinese bloggers/commentators). Layer 2 (quality control): seven deterministic Python engines in `engines/` that score the output on \"AI-taste\" dimensions and gate re-gener"
 },
 {
  "name": "Sahme115/ai-resume-humanizer-prompts",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. No scoring, no re-scoring, no iteration. The pipeline is: user pastes prompt + their text into an LLM chat → LLM returns rewrite → optionally run prompt 05 for ",
  "meaning_verification": "Partial, manual, and domain-specific. The KEY_FACTS allowlist in prompts 01–04 structurally prevents the LLM from inventing new entities, but only if the user correctly populates it. Prompt 05 asks th",
  "fact_preservation": "Strong for numbers — preventing invented metrics is the repo's central design principle. The KEY_FACTS allowlist bars the LLM from using any",
  "mechanism": "Pure prompt library — no code, no model, no automation. Five structured prompts (in Markdown, JSON, and XML variants) instruct any LLM (ChatGPT, Claude, Gemini) to rewrite resume text. The mechanism is: (1) a hard-banned vocabulary list of ~20 AI-tell words/phrases; (2) a KEY_FACTS allowlist the user supplies — the prompt instructs the LLM it may u"
 },
 {
  "name": "RnR-io/TrueText",
  "stars": 2,
  "category": "paraphrase-model",
  "detector_in_loop": "no — \"Analyze Text\" and \"Humanize\" are separate buttons with no shared code path. humanize_text() performs no scoring. The user must manually re-click Analyze after humanizing to see the new score.",
  "meaning_verification": "none",
  "fact_preservation": "none — Pegasus rewrites freely; the max_length=60 token cap will silently truncate or corrupt long sentences containing numbers or citations",
  "mechanism": "Single-pass Pegasus paraphrase (tuner007/pegasus_paraphrase). In humanize_text() (app.py), each sentence is independently fed to model.generate() with num_beams=10, temperature=1.5, max_length=60; the beam-search output replaces the original sentence. Detection uses roberta-base-openai-detector (detect_ai_content(), app.py): logits.argmax() per sen"
 },
 {
  "name": "humanizer-zh-update (Ttungx/humanizer-zh-update)",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — The audit step is two self-reflective LLM questions embedded in the prompt, not an external detector score. There is no score→rewrite→re-score→iterate loop of any kind.",
  "meaning_verification": "Minimal; instruction-only. The prompt contains an explicit \"no fabrication\" rule: the LLM must not add any facts, names, numbers, dates, or citations absent from the original, and must flag or ask the",
  "fact_preservation": "Explicit instruction-level protection: the \"no fabrication\" rule in the SKILL.md prohibits adding facts not present in the original, and the",
  "mechanism": "The entire product is a single SKILL.md file (~1000 lines of markdown with YAML frontmatter) that installs as a Claude Code skill/plugin. When invoked via /humanizer-zh-update, it loads as a system prompt into Claude. The LLM then applies a 33-pattern AI-tell catalogue to rewrite text in a 3-step internal workflow: (1) draft rewrite, (2) self-audit"
 },
 {
  "name": "humanizer-zh-pro",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — the LLM is told to \"self-check against 30-34 Chinese patterns\" in step 7 and do a \"final AI-feel audit\" in step 10, but both are the same LLM reasoning about its own output, not a separate detect",
  "meaning_verification": "none — step 3 of the 11-step process says \"preserve core meaning\" as a natural-language instruction to the LLM, but there is no mechanical check (no cosine similarity, NLI entailment, semantic-role ma",
  "fact_preservation": "none — numbers, citations, and quotes receive no explicit preserve-locking or byte-exact protection anywhere in the skill definition.",
  "mechanism": "Pure prompt/instruction set loaded as an AI assistant \"skill\" — no executable code. The repository consists entirely of Markdown files. SKILL.md encodes an 11-step rewriting procedure the host LLM follows: scan text against 34 named AI-tell patterns, rewrite to eliminate them, inject \"human authorship traces\" (concrete entry points, visible judgmen"
 },
 {
  "name": "JMok999/humanizer-zh-updated-june-26",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration. A single LLM rewrite pass is all that happens.",
  "meaning_verification": "none — the README lists \"保留核心含义\" (preserve core meaning) as a manual principle, but there is no automated semantic check, cosine similarity gate, NLI entailment, or any mechanical verification.",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or quotes. The guide is advisory text only.",
  "mechanism": "Pure prompt/skill file — no executable code. The entire implementation is SKILL.md (27 KB), a structured rule catalogue designed to be installed as a Claude Code / Codex / OpenCode skill. When invoked via `/humanizer-zh`, the host LLM reads the skill's instructions and performs a one-shot rewrite guided by 30 catalogued AI-writing patterns. Categor"
 },
 {
  "name": "deedeekong07-alt/fiction-humanizer-zh",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, no re-scoring, and no iteration. The skill performs a single-pass LLM rewrite guided by the prompt instructions in SKILL.md.",
  "meaning_verification": "none",
  "fact_preservation": "none — no explicit protection of numbers, citations, or quotes is mentioned anywhere in the skill instructions or references.",
  "mechanism": "Pure LLM prompt-guide / Claude Code skill. No executable code exists. The repository is a structured Markdown skill (SKILL.md) that instructs an LLM how to rewrite Chinese AI-generated fiction. The workflow (defined in SKILL.md) has 8 steps: determine task type, lock immutable content, diagnose AI patterns, rewrite scenes using \"setup/action/afterm"
 },
 {
  "name": "deedeekong07-alt/humanizer-zh",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "No. A 50-point self-assessment rubric (authenticity 10 + language naturalness 10 + stylistic personality 10 + structural coherence 10 + communicative warmth 10, pass threshold ≥40) is described in SKI",
  "meaning_verification": "None mechanically. The delivery checklist includes a prose instruction to \"preserve original meaning,\" but this is a prompt directive, not a cosine similarity check, NLI gate, or any programmatic veri",
  "fact_preservation": "None. No mention of number retention, citation locking, or quote preservation.",
  "mechanism": "Pure prompt guide packaged as a Claude Code skill. The entire repo is README.md + SKILL.md — no executable code. When invoked via `/humanizer-zh` in Claude Code, the SKILL.md instructions are loaded into Claude's context. Claude then rewrites the supplied Chinese text by applying a catalogue of 24 named AI-tell patterns (grouped into content, langu"
 },
 {
  "name": "shutcode/humanizer",
  "stars": 2,
  "category": "prompt-guide",
  "detector_in_loop": "no — the upstream pattern (blader/humanizer) is an LLM self-audit pass (the model asks itself \"what is still obviously AI?\" after a first draft), not an external detector scoring output and re-trigger",
  "meaning_verification": "none — no cosine similarity, NLI, or any automated check; the upstream skill instructs the model not to invent facts and not to drop information, but this is enforced only by the LLM's own compliance ",
  "fact_preservation": "prompt-instruction only — blader/humanizer's SKILL.md explicitly says \"Never invent facts… no fact, name, number, date, quote, or citation t",
  "mechanism": "The repo is a completely empty GitHub stub — no commits, no files, no source. Its description states the intent: combine blader/humanizer (33k stars, pure SKILL.md prompt guide for Claude Code) and op7418/Humanizer-zh (14k stars, Chinese translation of the same) into a bilingual Chinese-English skill. Neither upstream repo has any executable code; "
 },
 {
  "name": "ml-postech/Bias-Inversion-Rewriting-Attack",
  "stars": 1,
  "category": "adversarial-perturbation",
  "detector_in_loop": "No. The watermark detector is NOT in the rewrite loop. It runs once after rewriting as an evaluation step (attach_detectability in evaluation/tpr.py). The per-sample adaptive loop only monitors text d",
  "meaning_verification": "Evaluated post-hoc only, not as a gate. evaluation/evaluate_quality.py offers: SBERT cosine similarity (all-mpnet-base-v2), NLI entailment (cross-encoder/nli-deberta-v3-large via NLIEquivalenceScorePi",
  "fact_preservation": "None enforced mechanically. The system prompt passed to the rewriter LLM instructs \"Retain all specific details, numbers, names, and technic",
  "mechanism": "BIRA targets LLM watermarks (KGW, SIR, Unigram, UPV, EWD, DIP, EXP, and others), not general AI-text detectors. The transformation is a two-step logit-bias paraphrase. Step 1: a SelfInformationCalculator (attack_utils/attacks.py, class SelfInformationCalculator) runs the source text through a local causal LM (aux_model, default Llama-3.2-3B or the "
 },
 {
  "name": "SamCuipogobongo/bypass-aigc-skill",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector call anywhere. Text goes in, two prompt passes run sequentially, output comes out. No scoring, no re-scoring, no iteration. The original upstream project (chi111i/BypassAIGC)",
  "meaning_verification": "none automated. The prompts instruct Claude to preserve core logic, keep word-count within ±30 Chinese characters, and pass a self-check before outputting. No cosine similarity gate, no NLI entailment",
  "fact_preservation": "partial, prompt-only. Technical terms, code snippets, library names, API paths (e.g. Django, views.py, /accounts/api/token/refresh/) are pro",
  "mechanism": "A Claude Code skill (SKILL.md + prompt files + segmenter.py) that ports the core pipeline from chi111i/BypassAIGC. Text is first split into segments of ≤500 characters by segmenter.py (a faithful Python port of split_text_into_segments from the upstream ai_service.py). Each segment is then run through two sequential passes driven by prompt files: ("
 },
 {
  "name": "marynacleo/humanizer-plus",
  "stars": 1,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — but the detector is the regex scanner (ai_tells.py), not a statistical AI-text detector. The README flowchart shows Draft → Detector → Rewrite → Detector again → Read aloud → Publish. The loop i",
  "meaning_verification": "none — meaning preservation is a prompt instruction (\"Keep the information, drop the shape. Every claim in the source survives. Never invent a fact.\"), not a mechanical gate. No cosine similarity, no ",
  "fact_preservation": "Prompt-level instruction only: SKILL.md forbids introducing \"a fact, name, number, date or citation that was not in the source.\" The detecto",
  "mechanism": "Two-part system. (1) SKILL.md is a prompt-guide loaded by an LLM agent (Claude Code, OpenAI Codex): it instructs the model to rewrite prose against 33 numbered AI-tell patterns sourced from Wikipedia:Signs_of_AI_writing, plus French and Russian language-specific extensions. The rewriting is done entirely by the LLM — no code in the repo performs th"
 },
 {
  "name": "undetectable-ai-free/undetectable-ai",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "unclear — The README describes a detect-then-humanize flow and mentions \"multiple detection systems,\" but there is no code, so whether a real score-rewrite-rescore loop exists in the product cannot be",
  "meaning_verification": "none — README claims content preserves \"original meaning\" but provides no implementation. No similarity check, NLI gate, or any other meaning gate is described or coded.",
  "fact_preservation": "none — No mention of numeral protection, citation locking, or quote preservation anywhere in the README.",
  "mechanism": "No source code exists. The repo contains only a README.md (the entire repo is 4 KB, one file). The README is a marketing document for genspeed.ai, a closed-source commercial SaaS. It claims to run text through \"advanced algorithms,\" cross-validate with GPTZero, ZeroGPT, and Copyleaks, then apply unspecified \"humanization\" and \"paraphrasing.\" No fil"
 },
 {
  "name": "AI-Detection-Bypassers (d4r14h/AI-Detection-Bypassers)",
  "stars": 1,
  "category": "unicode-trickery",
  "detector_in_loop": "no — there is no detector call anywhere. The script takes text in and emits corrupted text out in a single pass. No scoring, no re-scoring, no iteration.",
  "meaning_verification": "none",
  "fact_preservation": "none — numerals and citations receive ZWJ insertion at the same 20% random rate as everything else; there is no preservation or exclusion lo",
  "mechanism": "Two-pass character-level corruption implemented in a single file, GPTZero-Bypasser.py. Function `replace_letters` substitutes 9 Latin letters (a, q, u, n, t, m, c, o, l) with visually indistinguishable Mathematical Monospace Unicode codepoints (e.g., 'a' -> U+1D68A range). Function `insert_zwj` then walks every character of the substituted text and"
 },
 {
  "name": "1003715231/humanize-ai",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — no code of any kind is present",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No source code exists. The repo contains a single README.md (two commits, last touched 2024-06-21) that is a marketing description for the commercial web service aihumanize.io. No transformation logic, no scripts, no dependencies — the \"tool\" is the closed-source SaaS it advertises."
 },
 {
  "name": "cammycurry/humanize-ai-text-pypi",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — the package makes a single API call and returns the result; there is no scoring, no iteration, and no feedback loop of any kind",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Pure thin HTTP wrapper. The entire implementation is one class, `HumanizedAI`, in `humanize_ai_text/humanized_ai.py`. Its `run()` method JSON-encodes the input text and POSTs it to `https://api.humanize-ai-text.ai/v1/humanize` with a Bearer token, then returns the parsed JSON response. There is no local transformation logic of any kind — all proces"
 },
 {
  "name": "ankshvayt/humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the self-audit in step 2 is the LLM re-reading its own draft against the pattern list, not an external detector scoring the output. There is no score, no threshold, no iteration triggered by a de",
  "meaning_verification": "none — the instructions say to \"preserve meaning\" but this is an unverified LLM-judgment instruction, not an automated check. No cosine similarity, no NLI entailment gate, no semantic-role matching, n",
  "fact_preservation": "none — numbers, citations, and quotes are not specifically protected. No byte-exact preserve-locking or any mechanism to flag invented or mu",
  "mechanism": "Pure prompt engineering via a single markdown file (SKILL.md). The file is pasted as a system prompt or skill definition into any LLM. The LLM then follows a three-step process when given text to humanize: (1) draft a rewrite that removes the 24 catalogued AI-tell patterns, (2) self-audit the draft by running through the pattern checklist, (3) outp"
 },
 {
  "name": "songkq/GPTs — Humanizer Pro.md",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — no scoring, no iteration, no re-check. One prompt → one rewrite, nothing else.",
  "meaning_verification": "none — the prompt says \"without changing its meaning\" but there is no programmatic gate, similarity check, or NLI step of any kind.",
  "fact_preservation": "none — no numeral retention, citation locking, or quote preservation logic anywhere in the prompt.",
  "mechanism": "Pure system-prompt instruction. The file `prompts/Humanizer Pro.md` is the leaked ChatGPT custom GPT configuration for \"Humanizer Pro\" by charly-ai.com. The system prompt tells the model to \"convert any AI generated content to human-like content without changing its meaning,\" \"mimic the user's previous writing style,\" and draw on four PDFs loaded i"
 },
 {
  "name": "geonwoo-jeong/japanese-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the rewrite_priority_score in profile-japanese-text.mjs is a weighted sum of rule-based style-issue hits (not an AI-text detector score), and there is no re-scoring or iteration loop of any kind.",
  "meaning_verification": "none — meaning preservation is enforced only via LLM prompt instructions (SKILL.md lists facts, numbers, dates, proper nouns, conditions, negations, and attribution as off-limits). There is no automat",
  "fact_preservation": "Partial, prompt-level only. The diagnostic script masks code blocks, table rows, blockquotes, URLs, inline code, and quoted spans (「…」 and \"",
  "mechanism": "Two-layer approach. Layer 1 is a Node.js static analysis script (`plugins/japanese-humanizer/skills/japanese-humanizer/scripts/profile-japanese-text.mjs`) that performs rule-based detection of Japanese-specific style issues on the input text: it masks protected spans (code blocks, tables, URLs, inline code, 「」/double-quoted direct quotes), then run"
 },
 {
  "name": "majiayu000/claude-skill-registry (via ToniPerea/humanizar-texto-es)",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the skill computes a \"humanity score\" (0-100) using a 5-criterion rubric that Claude applies to its own output, but no actual detector is invoked and there is no iterative rewrite-and-rescore loo",
  "meaning_verification": "none — the analysis output file shows before/after fragment pairs for human inspection, but there is no NLI gate, cosine similarity check, semantic-role match, or any automated meaning gate",
  "fact_preservation": "none — numbers, citations, and quoted material receive no explicit protection or byte-exact locking",
  "mechanism": "The registry (majiayu000/claude-skill-registry, 160,983 indexed skills) is a meta-catalog; the actual humanizer is ToniPerea/humanizar-texto-es, a Claude Code skill consisting entirely of a SKILL.md instruction file with no executable code. It instructs Claude to: (1) ask the user to pick \"informal/divulgative\" or \"academic/scientific\" mode; (2) de"
 },
 {
  "name": "ML-Watermarking/ada-llm-wm",
  "stars": 1,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes — the watermark detector is the optimization target; the attacker is tuned against detector scores, which is the core of the adaptive attack formulation (paper: arXiv:2410.02440, ICML 2025)",
  "meaning_verification": "none visible in the repo (README only); the paper likely reports quality metrics but these are not surfaced in the public repository",
  "fact_preservation": "none",
  "mechanism": "Preference-based optimization (analogous to DPO/RLHF-style tuning) is used to train paraphrasers that adaptively evade specific LLM watermarking detectors. The optimizer treats watermark robustness as an objective function and iteratively tunes the paraphraser against a target watermarking scheme. The paper reports that optimization transfers: a pa"
 },
 {
  "name": "pan-webis-de/pan24-generative-ai-authorship-verification",
  "stars": 1,
  "category": "research-code",
  "detector_in_loop": "no — there is no rewrite loop at all. The baselines score a text pair once and output a probability. The perturbators (T5MaskPerturbator) used by DetectGPT and DetectLLM generate internal candidate pe",
  "meaning_verification": "none — the repo contains no meaning-preservation check of any kind. It is a detection benchmark, not a text transformation pipeline.",
  "fact_preservation": "none — not applicable; the repo does not rewrite text.",
  "mechanism": "Shared-task infrastructure for a comparative authorship verification challenge. The task framing is Voight-Kampff style: given a pair of texts (text1, text2 — one human-written, one LLM-generated on the same topic), output a score in [0,1] indicating which is human. The repo contains three distinct layers. (1) Dataset generation (`pan24_llm_dataset"
 },
 {
  "name": "tsalkin/russian-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the repo explicitly and repeatedly states its goal is authenticity, not detector evasion; it calls optimizing for detector scores a \"Goodhart trap.\" No scoring loop of any kind is present.",
  "meaning_verification": "Described but not implemented in runnable code. SKILL.md Step 4 specifies bidirectional NLI entailment (\"does 'after' follow from 'before' and vice versa?\") as the primary check, with cosine similarit",
  "fact_preservation": "Explicitly required by SKILL.md Step 4: the LLM is told to verify that numbers, dates, names, negations, obligations, terms, and currency di",
  "mechanism": "Pure prompt/skill system — no executable code at all. The repo is a set of markdown files that constitute a Claude skill (SKILL.md) plus a standalone LLM prompt (prompt/standalone-prompt.md). The algorithm is a 4-step pipeline described in SKILL.md: (1) genre detection to set thresholds; (2) first pass against a catalogue of Russian machine-text ma"
 },
 {
  "name": "mrshibly/Humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step at any point. No detector is invoked before or after the rewrite. The pipeline is one-shot: instructions in, rewritten text out.",
  "meaning_verification": "none — no cosine similarity check, no NLI entailment gate, no semantic-role comparison. Meaning preservation is left entirely to the LLM following the prompt.",
  "fact_preservation": "none — no byte-exact preserve-locking, no numeral-retention check, no citation fencing. Numbers and quotes are rewritten along with everythi",
  "mechanism": "Pure prompt/instruction injection. The repo's core artifact is SKILL.md — a Markdown file containing an 8-step pipeline description and a 45-pattern AI-tell catalogue. There is no executable code. Users clone the repo and either register SKILL.md as a Claude Code skill (via the skills directory) or paste its contents into a system prompt. The LLM t"
 },
 {
  "name": "phamdo627/ai-detection-bypass-gptzero-turnitin",
  "stars": 1,
  "category": "other",
  "detector_in_loop": "no — README describes no scoring loop. The claimed workflow is paste → click Humanize → export, with no mention of any detector being queried at any stage.",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No source code exists. The repo ships only a precompiled Windows .exe inside a zip file (gptzero_bypass_turnitin_detection_ai_v3.4-beta.1.zip, ~465 KB). The README claims the binary applies vocabulary substitution (replacing \"AI-favoured words\" with synonyms), sentence-length variation, and paragraph reordering, but none of this can be verified fro"
 },
 {
  "name": "yaowen51888-rich/SayItBetter",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "yes — its own local pattern detector (pattern-detector.ts + statistics-detector.ts) runs after each rewrite iteration; the loop terminates when no P0 issues and at most one P1 issue remain, or the tim",
  "meaning_verification": "Rule-based only. fidelity-validator.ts runs three checks: (1) paragraph count falls within mode-specific bounds; (2) output/input length ratio meets a mode threshold (light: 0.85, platform: 0.7, deep:",
  "fact_preservation": "Yes, via extractProtectedFacts() in fidelity-validator.ts: protects HTTP/HTTPS URLs, PascalCase named entities and acronyms, decimal numbers",
  "mechanism": "Browser-based React/TypeScript frontend (Vite + Tailwind + Zustand) that implements a five-stage pipeline: (1) Detect — runs its own local pattern detector against the input; (2) Rewrite — calls an external LLM API (user-supplied key) with a platform-specific system prompt, generating 1–3 candidate rewrites for short texts or parallel chunks for lo"
 },
 {
  "name": "Sappymukherjee214/AI-Humanizer",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "yes — humanizeController.ts runs a while loop (MAX_ITERATIONS=3, threshold=15%) calling detectAI after each Gemini rewrite. However the detector is a single HF roberta model (not an ensemble), and on ",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role check, no hedge/numeral retention gate. The only meaning preservation is the Gemini prompt instruction to \"keep the core message identi",
  "fact_preservation": "none — no numeral locking, no citation byte-exact preservation, no special handling of quotes or statistics.",
  "mechanism": "Full-stack web app (React 19 + Express/Node.js). Rewriting is handled in backend/src/services/geminiService.ts: a large switch on \"toolId\" dispatches a system prompt to Gemini Flash (google/generative-ai SDK). The humanizer path uses a generic prompt: \"You are a world-class ghostwriter and expert linguistic editor. Transform the input text into hig"
 },
 {
  "name": "lakshitha-dev/ai-humanizer-skill",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — Claude cannot call any detector. The SKILL.md describes a manual \"detector-feedback workflow\" where the human closes the loop (run detector, paste highlights, Claude rewrites flagged sentences, r",
  "meaning_verification": "none — no automated check. SKILL.md warns \"Meaning drift is the real risk\" and instructs Claude to verify manually, but there is no cosine similarity gate, NLI entailment check, numeral-retention guar",
  "fact_preservation": "none — no byte-locking, no numeral retention guard, no citation preservation. The test-cases.md notes that invented personal details must be",
  "mechanism": "A Claude Code skill installed via npx that works by loading SKILL.md as a detailed system prompt into Claude. There is no model, no code that rewrites text — Claude itself does the rewriting using the instructions. The six documented \"levers\" are: (1) raise perplexity via specific word choice over model-default choices; (2) raise burstiness via del"
 },
 {
  "name": "ZyluxXD/zerobypass",
  "stars": 1,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — there is no scoring step and no iteration. The tool types text once with fake human rhythm and stops.",
  "meaning_verification": "none — text is passed through byte-for-byte (modulo the self-corrected typos); no semantic check is performed.",
  "fact_preservation": "none — the text is not altered, so numbers and citations survive trivially, but there is no explicit protection mechanism.",
  "mechanism": "Behavioral keystroke mimicry, not text transformation. The tool opens a Chromium browser via Playwright (`playwrighter.py`), waits for the user to navigate to a target page (e.g., GPTZero's submission field), then types the user's clipboard text character-by-character (`algorithm.py`) with human-like timing: random inter-keystroke jitter (35–100 ms"
 },
 {
  "name": "Mohit1053/Humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step at any point. The workflow is: CSV row → single Ollama generate() call → write output. Users are instructed to manually copy results to GPTZero and ZeroGPT and fill in a ",
  "meaning_verification": "none. The prompt says \"Preserve EXACT emotional meaning\" but no automated check is performed — no cosine similarity, NLI, semantic-role matching, or any other gate. Meaning preservation is entirely de",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special treatment. There is no preserve-locking, no numeral-retention check, no regex guard ",
  "mechanism": "Single-pass LLM rewriting via prompt engineering. The core transform is in humanize_v2.py::humanize() and humanize_csv.py::humanize_text(): the input text is formatted into a prompt template and sent to Llama3:8b running locally via Ollama. The prompts instruct the model to add contractions, filler words (\"honestly\", \"like\", \"you know\", \"I mean\"), "
 },
 {
  "name": "rohitverma8253/writing-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no external detector. The only feedback loop is a prompted self-review pass in which Claude evaluates its own output against the pattern checklist. No score-then-rewrite-then-rescore ite",
  "meaning_verification": "none — \"preserve core meaning\" is one of six bullet-point instructions in the prompt, with no automated semantic check (no cosine similarity, no NLI, no paraphrase comparison). Compliance depends enti",
  "fact_preservation": "none — numbers, citations, and quoted text receive no special treatment. No preserve-locking or byte-exact protection is mentioned or implem",
  "mechanism": "The repo contains exactly one functional file: skills/writing-humanizer/SKILL.md — a Claude Code skill (prompt) that instructs Claude to act as a Chinese-language editor. It defines 24 named AI-tell patterns (overuse of specific zh-TW vocabulary, formula structures such as three-part lists and negation parallels, dash overuse, emoji, boilerplate cl"
 },
 {
  "name": "jheaatabraizz-code/deai-document-rewriter",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — surface_scan.py does regex/keyword matching only, not a trained detector. There is no score-rewrite-rescore cycle. The strict mode's limited loop is a document quality control checklist (DQC), no",
  "meaning_verification": "Partial. verify_constraints.py checks literal presence of explicitly declared locked spans and P0 key terms, plus token-count comparison for numbers, dates, and citations between original and candidat",
  "fact_preservation": "Yes, explicitly and structurally. SKILL.md declares legal clauses, tender terms, patent claims, dates, amounts, tables, numbering, citations",
  "mechanism": "A Codex Skill (LLM prompt package) that instructs an LLM to rewrite Chinese text by operating on the \"expression packaging\" layer while preserving the factual and argumentative skeleton. The core transformation logic lives in SKILL.md and five reference documents: a 6-dimension AI trace catalog (information specificity, discourse structure, syntax/"
 },
 {
  "name": "AmyIvan/humanizer-zh",
  "stars": 1,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes, but offline — the AIGC detection report (PDF from CNKI 知网 or 维普) is a required input and drives which paragraphs get rewritten; there is no automated re-score-after-rewrite loop. The detector out",
  "meaning_verification": "Substantial. `freeze_object_extractor.py` extracts seven categories of must-preserve objects from source text (paren_terms, abbreviations, factual_numbers, units, policy_ids_or_doc_ids, structural_ord",
  "fact_preservation": "Strong and explicit. `factual_restore_patcher.py` specifically targets factual_numbers (quantities + units like \"50 mM\", \"10例\", 4-digit year",
  "mechanism": "DOCX-native, report-driven pipeline for Chinese academic papers. Workflow: (1) `document_parser.py` parses the user-supplied CNKI/Weipu AIGC detection report PDF, extracting per-paragraph AI-ratio scores from the detection table; (2) segments are classified high/medium/low risk (>=70%/>=40% thresholds); (3) `freeze_range_resolver.py` assigns a 4-ti"
 },
 {
  "name": "spuvr/humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no feedback loop, no re-score-and-iterate. The skill is invoked once; the LLM rewrites in one pass and stops.",
  "meaning_verification": "none — no automated gate of any kind. The skill instructs the LLM to \"keep the meaning\" and to \"read it out loud when you're not sure,\" but these are heuristic reminders to the model, not mechanical c",
  "fact_preservation": "none — no numeral retention, no citation locking, no preserve-locking of any kind. Numbers and quotes pass through whatever the model produc",
  "mechanism": "A Claude Code skill delivered entirely as a single SKILL.md file with YAML frontmatter. It loads into whatever LLM agent reads Claude skills and provides: (1) a core philosophical frame (\"human writing trusts the reader; AI writing doesn't\"), (2) a ~30-pattern AI-tell catalogue organized into five failure-shape groups (trust/rhythm/abstraction/mark"
 },
 {
  "name": "uaandesign/bragi-ai-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the lint script (ai-tells-lint.sh) is run manually before and after to observe density change; there is no automated score-rewrite-rescore loop and no detector integration whatsoever",
  "meaning_verification": "none automated. SKILL.md defines a \"five essentials\" checklist (what it is / how it works / validated output / progress / next-step support needed) that must survive every rewrite, and structural elem",
  "fact_preservation": "strong editorial intent but no mechanical enforcement. The core rule \"不造料，只逼真料\" (don't fabricate, only force real material) means numbers an",
  "mechanism": "A Claude Agent Skill (installed via `npx skills add`) that gives Claude a structured prompt-guide workflow. Transformation happens in three layers, all enforced by LLM instruction rather than code: (1) subtract AI-characteristic patterns catalogued in `skills/bragi/references/ai-tells.md` (40+ patterns across 7 categories, including a \"Layer 2\" of "
 },
 {
  "name": "AtharvaVavhal/ghostwriter-guard",
  "stars": 1,
  "category": "other",
  "detector_in_loop": "no — the repo contains only a detector; there is no rewrite step, no evasion loop, and no feedback between detection and any output text",
  "meaning_verification": "none — not applicable; the tool produces scores, not rewritten text",
  "fact_preservation": "none — not applicable",
  "mechanism": "This repo is a pure AI-text DETECTOR with zero text-transformation capability. It scores input text sentence-by-sentence using three signals fused as ML×0.70 + Perplexity×0.20 + Burstiness×0.10. The ML component (backend/pipeline/classifier.py: predict_score) runs 8 handcrafted linguistic features (unique-word ratio, avg word length, burstiness, pu"
 },
 {
  "name": "kevin0818-lxd/Humanizer",
  "stars": 1,
  "category": "back-translation",
  "detector_in_loop": "no — the reranker uses ten quality heuristics (semantic_fidelity_score, keyword_overlap, entity_match_score, relation_preservation_score, repetition_score, awkward_collocation_score, human_likeness_sc",
  "meaning_verification": "Partial heuristics only: the reranker feature set includes keyword_overlap (Jaccard over content tokens), entity_match_score (named entity and numeral retention), and relation_preservation_score (conn",
  "fact_preservation": "Partial: entity_match_score in the reranker counts capital-letter tokens and numeric strings from the source that survive into the candidate",
  "mechanism": "Two-stage back-translation pipeline: each semantic chunk (built by `build_semantic_chunks()` in `apps/api/app/services/pipeline.py`, max ~420 chars) is first translated to Chinese via `translate_to_chinese()` (calling an OpenAI-compatible endpoint, either a local DeepSeek client or an HTTP model server). The Chinese intermediate is then fed to a fi"
 },
 {
  "name": "dandepan303/AiDetectionBypass",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — the script submits text and accepts the first result unconditionally; there is no scoring step before or after rewriting",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes pass through unmodified only if undetectable.ai happens to preserve them; the script has no awareness ",
  "mechanism": "Selenium browser automation against the undetectable.ai web UI. The single file `paraphraser.py` does the following in a loop over 250-word chunks: (1) generate a throwaway email via `tempmail-python` and a random password, (2) register a fresh free account on undetectable.ai via Selenium clicks, (3) verify the account by following the link extract"
 },
 {
  "name": "bunnyscholar/bunnyscholar-api-skill",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — single-shot API call only. The response returns the rewritten text and a billing amount (usage.amount); no detection score is returned, no re-scoring step exists, no iteration is implemented anyw",
  "meaning_verification": "none — the personalTerms parameter lets callers nominate specific terms to preserve during rewriting, but that is term-pinning, not meaning verification. No cosine similarity, NLI entailment, semantic",
  "fact_preservation": "none automatic — personalTerms[] can pin user-specified tokens (useful for proper nouns, formula variables, institution names), but number a",
  "mechanism": "Pure documentation + agent-skill wrapper around BunnyScholar's closed commercial API. The repo contains no transformation code. Its core deliverable is SKILL.md — a structured prompt file that teaches AI agents (Claude Code, Codex, Copilot, etc.) to call two HTTP endpoints: POST /api/open/academic-rewrite (paraphrase, 3 strength levels) and POST /a"
 },
 {
  "name": "humantone-node",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — detect() and humanize() are independent one-shot calls; the SDK does not connect them into a score→rewrite→re-score loop. A caller could build such a loop themselves, but nothing in the SDK does ",
  "meaning_verification": "none — the SDK returns whatever the API responds with; no cosine similarity, NLI, hedge-preservation, or any other semantic gate is applied locally or enforced.",
  "fact_preservation": "none — text is passed verbatim to the remote API; no numeral locking, citation preservation, or quote protection is performed client-side.",
  "mechanism": "Pure HTTP client wrapper around the closed HumanTone SaaS API. The repo contains one source file (index.js, class HumanTone) with three methods: humanize() POSTs to /v1/humanize, detect() POSTs to /v1/detect, account.get() GETs /v1/account. No text transformation happens locally; all humanization logic is server-side and opaque. The SDK handles API"
 },
 {
  "name": "bunnyscholar/bunnyscholar-sdk",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — single-pass, fire-and-forget. There is no scoring step, no iteration, and no re-submission logic anywhere in the client code.",
  "meaning_verification": "none — the client returns the raw API response text without any cosine similarity check, NLI entailment, or any other meaning gate.",
  "fact_preservation": "none on the client side. The personalTerms parameter passes a word list to the remote API as a soft preservation hint, but there is no byte-",
  "mechanism": "Pure thin HTTP client wrapper around BunnyScholar's closed SaaS API. The entire SDK is two files: python/bunnyscholar/client.py and node/src/index.ts. Both expose two methods — academic_rewrite (POST /api/open/academic-rewrite) and humanize (POST /api/open/humanizer) — that serialize the caller's text to JSON, attach a Bearer token, and return what"
 },
 {
  "name": "ByPassEngine",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — the detector tab opens four websites in a browser window and nothing more; no scores are read back, no rewriting loop exists",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Three-tab Windows desktop GUI (Python/CustomTkinter). The humanizer tab is a thin wrapper around the cleverhumanizer.ai API: CleverHumanizerAPI.process_chunk() fetches the homepage to harvest a CSRF token, posts to /fingerprint-init with a random visitor UUID, then POSTs the text to /rewrite with a style param (casual/academic/formal). All actual r"
 },
 {
  "name": "HumanTone/humantone-n8n",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — Detect and Humanize are two independent node operations. A user could manually wire detect→humanize→re-detect as an n8n workflow loop, but the node ships no such loop, no iteration logic, and no ",
  "meaning_verification": "none — the node passes text in and text out with zero semantic checks. No cosine similarity, no NLI, no anything.",
  "fact_preservation": "none — no numeral retention, no citation locking, no quote preservation. The node passes the full text to the API as-is and returns whatever",
  "mechanism": "Thin n8n community node (TypeScript) that delegates entirely to the closed-source HumanTone API via the `humantone` npm SDK. The node itself (`nodes/HumanTone/HumanTone.node.ts`) marshals three parameters — text, level (standard/advanced/extreme), outputFormat — and calls `client.humanize(...)`. No rewriting logic exists in this repo; all transform"
 },
 {
  "name": "Sumit12312299/SkyScan-AI",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — humanization and detection are completely separate operations; there is no score→rewrite→re-score cycle anywhere in the codebase",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Three-tier pipeline, all in App.jsx and server/main.py. (1) Live mode: thin wrapper around Undetectable AI's commercial REST API — submits text to `https://humanize.undetectable.ai/submit`, polls `https://humanize.undetectable.ai/query` up to 30 times (3 s apart), and returns the result verbatim. (2) Self-hosted mode: POSTs to local FastAPI (`/api/"
 },
 {
  "name": "ljmor/my-humanizer-es-en",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the loop is human-mediated, not automated. The user submits the document to Compilatio/Turnitin/GPTZero externally, receives a PDF report, hands that PDF to the skill, and the skill targets the s",
  "meaning_verification": "none. There are no cosine similarity, NLI, hedge-preservation, or semantic-role checks. The only constraint is rule-based: the SKILL.md prompt instructs Claude never to alter citations, numbers, dates",
  "fact_preservation": "Partial — rule-based, not mechanical. SKILL.md states as a hard constraint that citations, numbers, dates, and the reference list must never",
  "mechanism": "A Claude skill (SKILL.md prompt catalogue + four Python helper scripts). The core transformation is an LLM rewrite guided by a hand-written rulebook in SKILL.md. The rulebook targets structural AI tells: colon-introduced elaborations (\"Claim: detail\"), symmetric enumerations (\"three fronts:\"), uniform sentence length, em-dashes, and a vocabulary bl"
 },
 {
  "name": "zkrun/academic-humanizer-zh",
  "stars": 1,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — score.py scores before and after each rewrite pass; SKILL.md explicitly instructs \"if a paragraph is still medium-high/high, revise it again rather than reporting it as done.\" However the detect",
  "meaning_verification": "Qualitative Claude-side checklist in SKILL.md: did the rewrite preserve the original argument? Were data or methods invented? Does the style match the discipline? No automated cosine similarity, NLI e",
  "fact_preservation": "Explicit and non-negotiable in SKILL.md: preserve facts, data, citations, author positions, methods, and conclusions. Forbids inventing expe",
  "mechanism": "Two-track pipeline targeting Chinese academic prose. Track 1: scripts/score.py applies nine deterministic regex pattern detectors (S1-S9) to each paragraph — template openings (\"随着…发展\"), parallel structures, hollow closings, vague attribution, high-frequency filler words, unsafe data claims, elevation contrasts, parallel-list density, and multi-par"
 },
 {
  "name": "Dex719/humanizer-extended",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — no external detector scores the output between passes. The two-pass structure (draft → self-audit → final) is entirely in the LLM's own chain-of-thought. The skill explicitly says it targets \"rem",
  "meaning_verification": "Instruction-level only, not automated gates. The Process step \"Fact lock\" tells the model to inventory claims and lock numbers, dates, URLs, names, negations, and modality, then run immutable_token_au",
  "fact_preservation": "Yes, instruction-level with eval support. The SKILL.md Non-negotiable constraints section forbids adding statistics, dates, names, instituti",
  "mechanism": "A Claude Code / OpenCode skill: the transformation is done entirely by the LLM following instructions in SKILL.md. The skill gives the model a compact pattern registry of 73 numbered AI-writing tells to detect and fix (patterns 1–73 in SKILL.md). Execution is a two-pass process: a draft rewrite, an internal \"What makes this obviously AI-generated?\""
 },
 {
  "name": "TEARYU-Humanizer (The-Lazy-Dragon/TEARYU-Humanizer)",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector call at any point. Detection awareness is baked into the prompt as natural-language rules describing what Turnitin (standard + August 2025 bypasser mode), GPTZero, Copyleaks,",
  "meaning_verification": "none — the instruction \"never add ideas not in the original; never remove substance\" appears in Step 5 as a rule to Claude, but there is no cosine similarity gate, NLI entailment check, semantic-role ",
  "fact_preservation": "none programmatic — the academic rewrite rules say \"Do not change facts, data, citations, or the author's interpretive claims\" and the techn",
  "mechanism": "A single Markdown skill file (Humaniser.skill, formerly SKILL.md) that Claude Code loads and executes when invoked via /tearyu-humanizer. There is no code, no runtime, no external dependencies. The mechanism is entirely prompt-guided LLM rewriting: the file encodes a numbered pipeline (Steps 0–6) that tells Claude how to (1) classify content type a"
 },
 {
  "name": "ghost-prose-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no. There is no detector of any kind invoked. The \"self-check\" in Step 5 is a mental checklist Claude reviews about its own output (e.g., \"every is not converted to isn't?\"), not a call to any scoring",
  "meaning_verification": "none. The instructions say \"Do not produce garbled nonsense — preserve core meaning even while distorting style,\" which is an instruction to the LLM, not a gate. No cosine similarity, no NLI entailmen",
  "fact_preservation": "none. Numbers are explicitly treated as targets for inconsistency injection (evasion Rule 21: \"Mix numeral and word number formats... Use ro",
  "mechanism": "Pure prompt-engineering framework delivered as a Claude skill. The repo contains no executable code whatsoever — only markdown instruction files. At runtime, Claude loads three reference files (skill/SKILL.md, skill/references/evasion-rules.md, skill/references/aiisms.md) and applies them to rewrite text or generate new text in a single pass. The t"
 },
 {
  "name": "openclaw-skill-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no. The Python script produces a JSON warnings report, but there is no re-scoring after the LLM rewrites. The workflow is strictly single-pass: script -> LLM prompt. No external detector is invoked at",
  "meaning_verification": "none. SKILL.md instructs the LLM to \"preserve meaning\" as a natural-language directive, but there are no mechanical gates: no cosine similarity check, no NLI entailment, no semantic-role comparison, n",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special protection. The curly-quote fix actively touches quotation marks (replacing typograp",
  "mechanism": "Two-phase. Phase 1: `scripts/humanize.py` (stdlib-only Python) applies regex substitutions: replaces em/en dashes with commas, curly quotes with straight quotes, ~20 filler phrases (e.g. \"in order to\" -> \"to\"), sycophantic openers, chatbot artifacts (\"I hope this helps\"), and editorializing phrases. It also runs detection-only passes (no auto-fix) "
 },
 {
  "name": "reallygood83/kimera",
  "stars": 1,
  "category": "api-wrapper",
  "detector_in_loop": "no — The modal's `runFullHumanize()` in `src/ui/AnalysisModal.ts` calls `aiProvider.humanize()` once, then calls `aiProvider.analyze()` on both the original and the result purely for a side-by-side di",
  "meaning_verification": "none — The prompt instructs the LLM to \"preserve meaning 100%\" but the code performs no programmatic check (no cosine similarity, no NLI entailment, no semantic-role match, no back-translation). The u",
  "fact_preservation": "none — No byte-exact locking of numbers, citations, or quotes. The humanize prompt contains no special instruction about numerals or citatio",
  "mechanism": "Two-layer approach. (1) Local heuristic analysis (no API): `src/analyzers/LocalAnalyzer.ts` computes seven metrics from raw text — a word-frequency-based perplexity approximation (`calculatePerplexity`), burstiness via sentence-length coefficient of variation (`calculateBurstiness`), TTR, sentence-length variance, AI-pattern regex matching (`detect"
 },
 {
  "name": "gitryson/allhereai-humanizer-skill",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no re-scoring, and no iteration. The skill is a single-shot prompt with a final self-check checklist the LLM is instructed to run, but no external detector is ever consu",
  "meaning_verification": "The SKILL.md ends with a checklist the LLM is instructed to apply after rewriting: verify every number, proper noun, citation, and URL still comes from the original; verify Markdown structure is intac",
  "fact_preservation": "Yes, explicitly. The skill's first section is titled \"inviolable constraints\" and lists: no invented facts, numbers, names, dates, quotes, c",
  "mechanism": "Pure prompt-guide packaged as an OpenAI Agents SDK / Codex skill. The entire transformation logic lives in SKILL.md — a system prompt in Chinese that instructs whatever LLM the agent framework supplies to: (1) diagnose eight categories of AI-flavor patterns (empty openers, marketing jargon, abstract noun stacks, mechanical 3-part structure, bureauc"
 },
 {
  "name": "acchuang/zh-tw-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no detector, no iteration. The skill does a single rewrite pass guided by human-readable pattern rules. No score-then-rewrite loop of any kind.",
  "meaning_verification": "A no-fabrication rule is stated explicitly: \"rewrites never add facts, names, dates, or citations that aren't in the source text.\" The skill's four-step procedure includes a self-check question asking",
  "fact_preservation": "Numbers and citations are covered only by the no-fabrication rule in the prompt — no byte-exact preserve-locking, no numeral-retention score",
  "mechanism": "A plain-Markdown agent skill (SKILL.md, ~500 lines) that instructs a backing LLM to perform rule-based rewriting of Traditional Chinese text. The transformation logic is entirely in the prompt: 33 named AI-writing patterns (split into Content, Language, Taiwanese-style, and Communication categories), a 48-entry cross-strait vocabulary substitution "
 },
 {
  "name": "dbhosbu-dotcom/renwei",
  "stars": 1,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — partial. `renwei_lint.py` is the in-loop scorer for deterministic metrics (dash frequency, colon usage, parallelism density, anthropomorphism count). AGENTS.md explicitly says \"when numerical ju",
  "meaning_verification": "none for rewriting itself. The macro rules include a long-context validation stage (数据对账 — cross-chapter figure reconciliation, 承诺对账 — claimed scope vs. actual content, metaphor coherence, hallucinati",
  "fact_preservation": "yes — numbers and factual data are protected by the deterministic rule layer rather than model inference. The \"dosage figures across chapter",
  "mechanism": "Dual-channel pipeline: (1) a local Python CLI linter (`cli/renwei_lint.py`) that scans text with `text.count()` literal matches, `re.findall()` regex counts, and sentence-level `re.search()` calls against patterns loaded from `metrics.json` and profile overrides — no external calls, exit 0/1 for CI; (2) four markdown rule files (`rules-scan.md` rul"
 },
 {
  "name": "Joozozhao/renhua-skill",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no score, and no iteration. The rewrite is one-shot, driven entirely by the LLM following the pattern list. The only \"loop\" is a single self-check step baked into the prompt",
  "meaning_verification": "The prompt includes a hard \"no fabrication\" rule: the rewrite must not introduce any fact, name, number, date, quotation, or citation not present in the source text. The agent self-checks this before ",
  "fact_preservation": "The \"no fabrication\" rule explicitly prohibits adding specific details (facts, names, dates, citations) unless they come from the original t",
  "mechanism": "Pure Markdown prompt loaded into an LLM agent. SKILL.md contains YAML frontmatter and a ~450-line Chinese-language system prompt that instructs the agent to scan input text against a 33-pattern AI-tell catalogue and rewrite it. The LLM does all the work; there is no transformation code. The workflow is: read input → identify pattern violations from"
 },
 {
  "name": "qi531416/novel-humanize",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "No. Zhuque is named as the detection target and the diagnosis step includes an initial \"Zhuque risk scan,\" but there is no code that calls Zhuque, captures a score, and iterates. The loop is manual/si",
  "meaning_verification": "Partial, rule-based only. The workflow locks the \"motivation-obstacle-action-result chain\" before rewriting and enforces a hard constraint of ±20% length change and \"no OOC\" (do not alter plot facts, ",
  "fact_preservation": "None for numbers in the sense untell uses. Technique 25 (\"数字体感化\") deliberately eliminates precise numbers and replaces them with subjective ",
  "mechanism": "Pure LLM skill (prompt ruleset loaded as an agent skill). No executable code is shipped. The repo contains SKILL.md plus five reference markdown files (diagnosis.md, techniques.md, checklist.md, banned-words.md, roles.md) that together define a multi-step rewriting workflow: (0) read and lock the plot/motivation chain; (1) diagnose AI-tells using a"
 },
 {
  "name": "reubenzhuyt-cloud/aigc-humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector scoring step anywhere in the workflow. The skill does not score output, does not iterate based on detection probability, and has no re-score loop. It is a single-pass rewrite",
  "meaning_verification": "none — the prompt instructs sub-agents \"不改数据、专有名词、事实\" (do not change data, proper nouns, facts) but this is a natural-language instruction to the rewriting LLM with no automated check. Position tags (",
  "fact_preservation": "prompt-only instruction — sub-agents are told not to alter numbers, proper nouns, or facts, but there is no byte-exact locking, no pre/post ",
  "mechanism": "A single SKILL.md (375 lines) for the OpenCode AI coding assistant. The orchestration is entirely prompt-driven, with no executable code. The main agent (1) asks the user for an optional writing sample and extracts a Voice Profile from it (sentence length, tone, vocabulary, rhythm, formality), (2) reads the user's OpenCode config to discover availa"
 },
 {
  "name": "kongzichixiangjiao/qu-ai",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "No. There is no detector integrated at any point. The structured output block in SKILL.md asks the LLM to produce a before/after comparison (50-character excerpts) and a tally of 19 transformation cat",
  "meaning_verification": "None in any computational sense. The Language-Only mode instructs the LLM to preserve facts and citations as a prompt constraint (\"不改事实\"), and the 13-point output checklist asks it to confirm voice co",
  "fact_preservation": "Partial instruction-level only. The Language-Only mode explicitly tells the LLM not to alter data or citations, and \"不改事实\" (preserve facts) ",
  "mechanism": "Pure prompt engineering packaged as a Claude Code SKILL.md skill. No executable code exists — the entire repo is Markdown instruction files. The core SKILL.md describes a four-dimensional transformation framework (language layer, narrative layer, detail layer, style layer) applied by instructing the LLM to: (1) eliminate ~30 blacklisted Chinese AI-"
 },
 {
  "name": "KyrieHuang/humanizer-zh-speech",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no AI-text detector score integrated into the rewrite cycle. The SKILL.md has a 5-dimension self-assessment checklist (fluency, rhythm, connectors, warmth, anti-AI residue) but this is C",
  "meaning_verification": "Lightweight. The skill's Protection Barriers section explicitly locks technical content, numbers, proper nouns, and quoted material before transformation, and the SKILL.md states \"never alter meaning,",
  "fact_preservation": "Yes — numbers, proper nouns, technical terms, and quoted material are identified as protected zones before any pattern is applied. The commi",
  "mechanism": "A Claude Code skill (SKILL.md) that loads a 12-pattern rule set into Claude's context. Claude then applies them sequentially: sentence fragmentation to breath units of ≤10 characters, formal-to-colloquial connector substitution (然而→但是, 因此→所以), nominalization reduction (進行討論→聊一下), subject ellipsis after topic establishment, second-person pronoun inj"
 },
 {
  "name": "ispacekid/agentizer-zh",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring, no detector, no rewrite loop of any kind. It is a one-shot prompt template applied by the underlying LLM.",
  "meaning_verification": "none",
  "fact_preservation": "none — no protection for numbers, citations, or quotes whatsoever",
  "mechanism": "Pure prompt-engineering skill file — no code, no model, no API calls of its own. The repo ships a SKILL.md that instructs an LLM (Claude Code or OpenCode) to apply a rigid 5-step \"AI-ification\" template to every response: (1) empathetic opener (\"I'm here, not hiding, not dodging…\"), (2) false promise of directness, (3) vocabulary-injected over-expl"
 },
 {
  "name": "KyrieHuang/humanizer_zh",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring loop, no re-scoring after rewrite. The process is one-shot: user submits text, LLM rewrites, done. A 5-dimension quality rubric (directness, rhythm, reader trust,",
  "meaning_verification": "none — no cosine similarity gate, no NLI entailment check, no semantic-role match, no automated meaning gate of any kind. Quality assurance is the LLM asking itself \"what still reads as obviously AI-w",
  "fact_preservation": "none — no byte-exact locking of numbers or citations. Pattern #4 (vague attribution) instructs the LLM to replace vague sourcing (\"analysts ",
  "mechanism": "Pure prompt-engineering skill with zero code. The repo contains two files: README.md (documentation) and SKILL.md (the operative instruction set). When a user runs `/humanizer-zh` in Claude Code or OpenCode, the LLM loads SKILL.md and executes a fixed two-phase pipeline: (1) scan the input against 27 named Traditional Chinese AI-writing patterns (e"
 },
 {
  "name": "tentenco/Better-Humanizer-zh-TW",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector of any kind. The skill is a single-pass rewrite. No score-then-rewrite-then-rescore loop exists. The 60-point rubric is self-evaluated by the rewriting LLM as a final quality",
  "meaning_verification": "none automated. Section 12.1 of SKILL.md says direct quotations and legal/original text must not be changed, but this is a prose rule for the LLM, not a semantic-similarity check or NLI gate. No cosin",
  "fact_preservation": "none automated. The prompt includes guidance on when numbers should be Arabic vs Chinese characters, and a rule to leave direct quotations v",
  "mechanism": "Entirely a Claude Code skill shipped as a single instruction file (SKILL.md). No executable code exists in the repo — the transformation is performed by Claude following a detailed system prompt. The prompt instructs Claude to act as a veteran Taiwan publishing editor and execute a fixed seven-step pipeline: (1) infer genre from seven presets, (2) "
 },
 {
  "name": "KMYy7/Academic_Humanizer",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, no iteration. One-shot prompt, one-shot rewrite, done.",
  "meaning_verification": "none automated. The prompt instructs the LLM not to fabricate data and to stay within ±10% of the original word count, but these are natural-language instructions to the model, not code-enforced gates",
  "fact_preservation": "Prompt-level only. Constraint #1 states \"absolutely prohibit fabricating or imagining data; must reconstruct 100% from the user-supplied ori",
  "mechanism": "Single LLM system prompt stored in aigc.md. The entire repo is three files: a README, a LICENSE, and one markdown file containing a Chinese-language role-play system prompt. You paste the prompt into any LLM chat interface, then send your draft text. The prompt instructs the model to act as a senior CS conference scholar and rewrite the text in sev"
 },
 {
  "name": "jefeerzhang/academic-humanizer-zh",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the audit phase has Claude itself identify AI-tell patterns from its own catalogues, but no external or local AI-text detector is called, scored, and iterated against. There is no score-rewrite-r",
  "meaning_verification": "none in the automated sense. The verification step is a Claude self-instruction to confirm no number or citation was altered, but there is no cosine similarity check, NLI entailment gate, semantic-rol",
  "fact_preservation": "Stated rule in SKILL.md: \"Never invent, drop, or alter a number, equation, or citation.\" The verification phase instructs Claude to confirm ",
  "mechanism": "A Claude Code skill — purely markdown, no executable code. The pipeline is defined in SKILL.md as natural-language instructions Claude follows. Four sequential phases: (1) Audit: Claude reads the manuscript and any author writing samples to identify document type and venue. (2) Pattern scan: checks text against six layered catalogues of AI tells (i"
 },
 {
  "name": "elongatus-sn/humanizer-ja",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "No. Step 4 of the process (\"再スキャン — 書き直し後に残存パターンがないか確認する\") asks Claude to self-check the rewrite for residual pattern matches, but this is the same LLM re-reading its own output against the pattern li",
  "meaning_verification": "None. No cosine similarity, NLI entailment, semantic-role comparison, or any automated check that meaning survived. The skill instructs Claude to \"preserve the meaning and information of the sentence\"",
  "fact_preservation": "None. Numbers, citations, and quoted text receive no special treatment. No preserve-locking, no numeral-retention check, no mention of these",
  "mechanism": "A single SKILL.md file (~14 KB) that is the entire implementation — no executable code whatsoever. It instructs Claude (via the Claude Code skill system, installed with `npx skills add`) to: (1) scan input text for 24 named Japanese-specific AI writing patterns, (2) produce a severity table per pattern, (3) rewrite the full text removing those patt"
 },
 {
  "name": "DSasakiBusiness/humanizer-ja",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — the three-step post-write self-check is the same Claude model reviewing its own output in-context, guided by an 11-item checklist. There is no separate detector model producing a numeric score, n",
  "meaning_verification": "none automated. The pre-write quality gates (AskUserQuestion for abstract metrics, vague dates, redacted nouns, unsourced claims) ask the user to clarify rather than checking semantic equivalence algo",
  "fact_preservation": "partial and manual. Quality gates prompt the user to supply concrete numbers and source citations when the input contains vague claims (\"sig",
  "mechanism": "A Claude Code / OpenCode skill — the core artifact is SKILL.md, a structured system prompt that instructs Claude to (1) run pre-write quality gates using AskUserQuestion to surface vague claims, (2) rewrite Japanese text in one of five tone modes (business, standard, note-style, casual, technical-neutral), detecting and eliminating 33 regex-catalog"
 },
 {
  "name": "matsutouya/humanizer-ja",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scorer, no re-scoring step, and no iteration. The SKILL.md describes a single-pass rewrite. The CHANGELOG explicitly notes that numerical benchmarks and evaluation method",
  "meaning_verification": "none — no cosine similarity, no NLI check, no semantic-role comparison, no automated gate of any kind. The only quality check is a manual heuristic called the \"humanizer trophy effect\" — a self-check ",
  "fact_preservation": "none — no byte-exact preserve-locking, no numeral retention check, no citation protection. Numbers, quotes, and citations receive no special",
  "mechanism": "A Claude Code skill (SKILL.md) that acts as a structured prompt/style guide. It instructs Claude to scan Japanese AI-generated drafts for 40 categorized patterns across three tiers (structural #1-10, vocabulary #11-27, formatting/rhythm #28-40), then rewrite using a two-layer model: a fixed \"identity layer\" (first-person pronoun, avoided endings, t"
 },
 {
  "name": "matsuikentaro1/humanizer-japanese",
  "stars": 1,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no detector, and no iteration. It is a single-pass rewrite guided by the skill prompt.",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers, citations, and quotes receive no special protection",
  "mechanism": "A Claude Code skill implemented as a single SKILL.md file placed in ~/.claude/skills/. When invoked via /humanizer-japanese, it instructs Claude to scan the submitted Japanese text for 33 labelled AI-tell patterns across six categories (symbols/formatting, rhythm/structure, introduction/progression, content/stance, communication artifacts, and \"sou"
 },
 {
  "name": "liam-nextdomain/humanizer-kr",
  "stars": 1,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no AI-text detector score driving the loop. Step 5 re-scans output for residual instances of the 23 catalogue patterns and may loop back to Step 3, but this is rule-pattern auditing by t",
  "meaning_verification": "constraint-based but no formal gate. The Writing Profile (speech level, vocabulary register, sentence length, tone, anchor-noun list, metaphor spans) is locked in Step 1 and asserted as binding guards",
  "fact_preservation": "partial. Anchor nouns (title/heading nouns, definitional-sentence subjects, central concept nouns) are explicitly exempted from vocabulary-d",
  "mechanism": "A Claude skill (packaged as SKILL.md + reference files + one Python helper) that drives Claude through a five-step orchestration loop to remove Korean AI-writing patterns. Step 1 detects essay vs. academic style and locks a Writing Profile (speech level, vocabulary register, sentence length, tone, anchor nouns). Step 2 runs a catalogue scan of 23 n"
 },
 {
  "name": "XuandongZhao/WatermarkAttack",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist, cannot be assessed",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Repository does not exist. GitHub API returns 404. A full enumeration of XuandongZhao's public repos (50 listed) contains no repo named WatermarkAttack. The nearest repos are WatermarkAttacker (NeurIPS 2024, removes invisible image watermarks via generative AI — image domain, not text) and Unigram-Watermark (ICLR 2024, builds robust text watermarks"
 },
 {
  "name": "wrightprosper7-bit/paper-humanizer",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — no code exists",
  "meaning_verification": "none — no code exists",
  "fact_preservation": "none — no code exists",
  "mechanism": "No code exists. The repository has exactly one commit and two files: a stock Node.js .gitignore template and a README.md whose entire content is the Chinese description \"中文论文自然化修改网站\" (Chinese paper naturalization modification website). There are no source files, no functions, no transformation logic of any kind to describe."
 },
 {
  "name": "gtskevin/ob-paper-humanizer",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — no external detector is queried at any point; pattern recognition is performed by the same LLM that does the rewriting; no numerical score, no rewrite-rescore iteration",
  "meaning_verification": "none mechanically — the quality checklist instructs the LLM to verify \"Technical accuracy preserved\" and the prompt warns against meaning drift with a revert instruction, but there are no cosine-simil",
  "fact_preservation": "instructions only — Category 15 (False Precision) explicitly prohibits fabricating statistics, effect sizes, or p-values; Common Trap 9 proh",
  "mechanism": "Structured LLM system-prompt delivered as SKILL.md. The file instructs the host LLM to act as an OB writing coach: scan submitted text for 17 named AI-pattern categories (hollow framing, transition monotony, hedging overload, generic contribution claims, sentence-length uniformity, formulaic section patterns, post-hypothesis summaries, methods with"
 },
 {
  "name": "aken1023/academic-humanizer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — the detector (detect_ai_patterns.py) and the rewriter (humanize_text.py) are completely separate scripts with no shared code or IPC. The user must run them manually in sequence. There is no score",
  "meaning_verification": "none — no cosine similarity, no NLI, no semantic-role check, no back-translation. The README explicitly tells users to check accuracy themselves after rewriting.",
  "fact_preservation": "none — the replacement loop runs str.replace() over the entire raw text with no exclusion zones. Numbers, inline citations, quoted strings, ",
  "mechanism": "Pure dictionary substitution on Traditional Chinese text. `scripts/humanize_text.py` defines a hardcoded `REPLACEMENTS` dict of ~200 Chinese phrases, each mapped to a list of alternatives (e.g., \"然而\" → [\"但是\",\"不過\",\"話說回來\",…]). The `humanize_text()` function iterates the dict, counts occurrences of each key in the input, replaces 80% of them (95% in -"
 },
 {
  "name": "gitoneuunn/Paper_Master_Humanization",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — no code exists to inspect",
  "meaning_verification": "none — repo is empty",
  "fact_preservation": "none — repo is empty",
  "mechanism": "The repository exists on GitHub but contains no files whatsoever — no README, no source code, no commits. The repo was created on 2026-06-21 and has a description reading \"this is a skill for paper humanization\" but was never populated. There is nothing to analyze."
 },
 {
  "name": "tyreekb046-ux/paper-humanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the pipeline is strictly one-way: user text in, Claude rewrite out. No detector is called at any stage, no score is computed, and no iteration occurs.",
  "meaning_verification": "none — the rewritten text is returned directly to the user with no cosine similarity check, NLI gate, numeral comparison, or any other semantic guard.",
  "fact_preservation": "none — there is no byte-locking of citations, numbers, or quotes. The system prompt says \"preserve all original information\" as a prose inst",
  "mechanism": "Single-pass rewrite via the Anthropic Claude API (claude-opus-4-6 model) with a hardcoded system prompt. The prompt instructs Claude to vary sentence length, add natural transitions, insert subtle hedges, and replace formal vocabulary with conversational alternatives. The rewritten output is streamed directly to the client browser. All logic is in "
 },
 {
  "name": "academic-humanizer-kr",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "yes, partially — but the \"detector\" is Claude itself, not an external AI-text classifier. In Strict mode, `academic-tell-detector` scores the rewritten text for residual AI tells and re-triggers `acad",
  "meaning_verification": "yes — two layers. (1) Deterministic Python: `tests/preservation_lint.py` uses regex to extract numbers, citation signatures (3 patterns covering Korean 홍길동(2020), parenthetical, and Western author-yea",
  "fact_preservation": "strong relative to repo class. Three distinct protections: numbers and statistics extracted by regex and multiset-compared (NUMBER_DRIFT, NU",
  "mechanism": "Entirely a Claude Code skill/plugin — no standalone executable or fine-tuned model. The transform is driven by a structured prompt pipeline whose instructions live in `skills/humanize-academic/SKILL.md`. Two paths exist. Fast path (≤5 000 chars): a single Claude agent (`agents/academic-humanize-monolith.md`) performs detect + rewrite + self-verify "
 },
 {
  "name": "academic-humanizer/academic-humanizer",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo not found",
  "meaning_verification": "none — repo not found",
  "fact_preservation": "none — repo not found",
  "mechanism": "Cannot be determined — the repository returns HTTP 404 from both the GitHub REST API and direct web fetch. No source code, README, or any file was accessible."
 },
 {
  "name": "leike0813/paper_humanizer",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no AI-text detector at any stage. Pattern recognition is performed by the LLM agent against the 39-category rule catalogue in SKILL.md and diagnostic-guidance.md. No score is computed an",
  "meaning_verification": "Full-mode verification runs a three-part LLM-driven check: bidirectional information mapping (every claim in the source must survive in the output and vice versa), structural integrity, and style cons",
  "fact_preservation": "Yes, and more thoroughly than most. document_pipeline.py byte-protects non-prose content — LaTeX environments and commands, inline/display m",
  "mechanism": "A Claude Agent Skill (prompt-instruction package, not executable code at its core). The primary deliverable is SKILL.md — a rule document that instructs an LLM agent to identify and rewrite 39 catalogued AI-tell patterns (significance inflation, stock vocabulary, copula avoidance, nominalizations, uniform rhythm, vague attribution, etc.). Three ope"
 },
 {
  "name": "ubturja/Research-Paper-Humanizer-Claude-Skill",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, and no iteration. The workflow is a single two-pass editorial rewrite driven by prompt instructions; no feedback from any detection system.",
  "meaning_verification": "none mechanically. The skill instructs Claude to \"Preserve substance: Never alter data, findings, statistics, or citations\" and to \"Justify every change,\" but these are prompt-level directives to the ",
  "fact_preservation": "Prompt-instruction only: the skill text explicitly says \"Never alter data, findings, statistics, or citations.\" There is no byte-exact prese",
  "mechanism": "Single-file Claude skill (scholarly-prose.md, 51 KB) that encodes editorial rules as prompt instructions. When loaded as a Claude skill, it directs Claude to perform a two-pass rewrite: Pass 1 tightens claims, calibrates hedging, improves cohesion, and varies sentence structure; Pass 2 audits for clarity. The patterns addressed are numbered (e.g., "
 },
 {
  "name": "JerryWang1234/AIGC-academic-paper-evasion",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo not accessible",
  "meaning_verification": "none — repo not accessible",
  "fact_preservation": "none — repo not accessible",
  "mechanism": "Cannot be determined — both the GitHub user JerryWang1234 and the repository AIGC-academic-paper-evasion return HTTP 404. The repo does not exist or has been deleted/made private."
 },
 {
  "name": "yangsijie666/aigc-detection-bypass-academic",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo not found",
  "meaning_verification": "none — repo not found",
  "fact_preservation": "none — repo not found",
  "mechanism": "Repository does not exist. The GitHub user yangsijie666 has 60 public repositories; none is named aigc-detection-bypass-academic. GitHub search for that name returned zero results."
 },
 {
  "name": "zh-academic/aigc-detector-bypass",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Cannot determine — repository returns HTTP 404 from both the GitHub REST API and direct WebFetch. No organization named zh-academic exists on GitHub, and no repository with this path was found via GitHub search."
 },
 {
  "name": "AsdethPrime/mastra-humanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "unclear — the description says \"iteratively rewrites them to bypass AI detection using Winston AI\", which implies a score→rewrite→re-score loop, but there is no source code to confirm whether Winston ",
  "meaning_verification": "none — no source code exists; no meaning-preservation mechanism is described anywhere in the repo.",
  "fact_preservation": "none — no source code exists.",
  "mechanism": "Cannot be determined — the repository was created 2026-05-27 and has never received a single commit. Size is 0, no files, no README, no source tree. The only technical signal is the repo description: \"An AI agent built with Mastra that generates essays and iteratively rewrites them to bypass AI detection using Winston AI.\" From that description alo"
 },
 {
  "name": "yanaiela/Robust-GPT-Detector",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Cannot determine — the repository does not exist. The user yanaiela has 34 public repos on GitHub; none is named Robust-GPT-Detector or any close variant. A GitHub repo search for \"Robust-GPT-Detector\" returns no match under that owner."
 },
 {
  "name": "WannabeSmith/drp",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo not found",
  "meaning_verification": "none — repo not found",
  "fact_preservation": "none — repo not found",
  "mechanism": "Cannot be determined — the repository returns HTTP 404 from both the GitHub REST API and direct web access. No source files, README, or metadata are reachable."
 },
 {
  "name": "ieee-acm-paper-writing",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, no re-scoring, and no iteration. The humanize pass is a single LLM call. The skill has no awareness of any AI-text detector.",
  "meaning_verification": "Rule-based prohibition encoded in the system prompt, not a post-hoc semantic check. SKILL.md and manuscript-structure-style.md state that humanize must never change \"technical claims, numbers, units, ",
  "fact_preservation": "Explicit and enforced by instruction: SKILL.md hard-prohibits changing numbers, units, citations, equations, figure and table labels, scope ",
  "mechanism": "A structured skill (SKILL.md + five reference markdown files) that installs into Claude Code or Codex as a slash/at-command. It exposes nine modes routed by a dispatch table in SKILL.md. The \"humanize\" mode is the relevant one: it applies a catalog of ~12 AI-prose patterns documented in skills/ieee-acm-paper-writing/references/manuscript-structure-"
 },
 {
  "name": "B00409227/MSc-AI-Detection",
  "stars": 0,
  "category": "research-code",
  "detector_in_loop": "no — attacks are batch-processed once offline; evaluation is a separate notebook step run afterwards. There is no score-rewrite-rescore loop of any kind.",
  "meaning_verification": "none — no cosine similarity check, no NLI gate, no hedging or numeral retention check. The project measures attack success rate as a research metric after the fact, but imposes no quality gate on indi",
  "fact_preservation": "none — numbers, citations, and named entities receive no special treatment; the 150-word truncation in the ChatGPT attack may drop them enti",
  "mechanism": "Three one-shot paraphrase attacks, each implemented as a standalone script in src/. (1) Pegasus attack (run_pegasus_attack.py): splits text into sentences, paraphrases each with tuner007/pegasus_paraphrase via beam search (num_beams=10, max 6 sentences, 60-token cap per sentence), then recombines. (2) QuillBot-style attack (run_quillbot_style_attac"
 },
 {
  "name": "lynxthecoder10/RESEARCH-PAPER-HUMANIZER",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — The Python backend runs a heuristic AI-risk score (sentence variance + type-token ratio) and a plagiarism check, but these are independent features surfaced in a separate \"plagiarism\" UI route. T",
  "meaning_verification": "none",
  "fact_preservation": "none — The PDF route prompt says \"preserving all citations and technical accuracy\" as a plain-English LLM instruction, but there is no progr",
  "mechanism": "Single-shot LLM prompt via the Groq API (llama3-8b-8192) with a Gemini 1.5 Flash fallback. The prompt is literally \"Rewrite this academic text to be more human, clear, and professional.\" Input is truncated to 4000 characters. The PDF variant (`src/app/api/humanize-pdf/route.js`) is identical but Groq-only and instructs the model to preserve citatio"
 },
 {
  "name": "yuvraj-rag/adversarial-ai-text-attack",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "yes, partially — GPT-2 perplexity (GPTZero-compatible) is the internal scoring signal. After each vector the pipeline re-scores and stops early if both doc-PPL and per-line PPL clear the human thresho",
  "meaning_verification": "cosine similarity via all-MiniLM-L6-v2 sentence embeddings (verifier.py). Sentence-level threshold 0.88 for V1/V3 token and verb swaps; paragraph-level threshold 0.85 for V2 structural changes. Every ",
  "fact_preservation": "none — no numeral retention, no citation locking, no quote preservation. Numbers embedded in text can be freely substituted by V1/V3 if they",
  "mechanism": "Three independent attack vectors applied sequentially with re-scoring between them. V1 (disrupt_token_probabilities in attacker.py): identifies the highest-probability content-word tokens via GPT-2 per-token log-probs (computed in scorer.py), then replaces them using BERT/RoBERTa fill-mask or GloVe/Word2Vec/FastText embedding neighbors — only accep"
 },
 {
  "name": "noisyboy/zerow",
  "stars": 0,
  "category": "unicode-trickery",
  "detector_in_loop": "no — no detector is consulted at any stage; the tool is a one-shot encoder/decoder with no scoring or iteration",
  "meaning_verification": "none — the visible carrier text is never altered, so meaning preservation is not a concern this tool addresses",
  "fact_preservation": "none — numbers, citations, and quotes in the carrier string pass through unmodified (because nothing in the carrier is touched), but this is",
  "mechanism": "Pure zero-width Unicode steganography. The single-file client-side tool (index.html) has two JS functions: encodeData() converts each payload character to its 8-bit binary representation via charCodeAt(), then maps each bit to either U+200B (zero-width space, = 0) or U+200C (zero-width non-joiner, = 1), and splices the resulting invisible character"
 },
 {
  "name": "JonathanZha47/PadBen-Paraphrase-Attack-Benchmark",
  "stars": 0,
  "category": "research-code",
  "detector_in_loop": "no — detectors are evaluated after paraphrasing is complete, not consulted during generation. The Type 5 iteration depth (1/3/5 rounds) is a fixed parameter, not driven by detector scores. There is no",
  "meaning_verification": "Jaccard similarity (n-gram overlap via `data_quality_assurance/metrics.py: JaccardSimilarityCalculator`), self-BLEU, GPT-2 perplexity, difflib `SequenceMatcher` ratio for early-stop in `type5_generati",
  "fact_preservation": "none — no numeral retention logic, no citation locking, no byte-exact preserve mechanism identified in any source file",
  "mechanism": "PADBen is a benchmark dataset + evaluation harness, not an active evasion tool. It creates five text categories offline and then evaluates detectors against them. Text transformation happens in two generation scripts: `data_generation/type4_generation.py` (single-pass LLM paraphrase of human text) and `data_generation/type5_generation.py` (iterativ"
 },
 {
  "name": "berdnikovartem/undetectableAIText",
  "stars": 0,
  "category": "unicode-trickery",
  "detector_in_loop": "no — single-pass character replacement; no scoring, no feedback, no iteration of any kind",
  "meaning_verification": "none — text is never read semantically; only individual characters are swapped",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling; they pass through unmodified only because they happen not to contain the ",
  "mechanism": "Pure homoglyph substitution — no rewriting, no LLM, no paraphrase. The C version (UNDETECTABLE AI.c, function replace_letters) replaces three ASCII characters in-place: 'o' → Cyrillic 'о', 'e' → Cyrillic 'е', 'p' → Cyrillic 'р'. The Java version (UNDETECTABLEAI/src/undetect/MainActivity.java, method replaceLetters) reads a text file, applies a larg"
 },
 {
  "name": "PSS-Watermark-Detection",
  "stars": 0,
  "category": "detector-with-evasion",
  "detector_in_loop": "no — The paraphrasing pipeline in src/paraphrasing/paraphraser.py applies 8 rounds of LLM rewriting with no watermark detection scores consulted between rounds. Each round simply paraphrases the prior",
  "meaning_verification": "none — The paraphraser has a length check (allows up to 20% deviation from the original length, retries up to 3 times at adjusted temperatures if violated) but no semantic similarity gate, no NLI enta",
  "fact_preservation": "none — Numbers, citations, and quoted strings receive no special handling in the paraphraser. They pass through or get rewritten along with ",
  "mechanism": "This is primarily a watermark DETECTOR, not a humanizer. The repo ships two distinct components: (1) a greenlist watermarking scheme (vocabulary partitioned by gamma/delta parameters) applied at generation time; and (2) an iterative paraphrasing attack pipeline (D1-D8 rounds) using instruction-tuned 7B models (Mistral-7B-Instruct, Qwen2-7B-Instruct"
 },
 {
  "name": "rexcoleman/llm-watermark-robustness",
  "stars": 0,
  "category": "research-code",
  "detector_in_loop": "no — the project measures detection signal before and after paraphrasing, but paraphrasing and scoring are entirely separate steps. There is no score-then-rewrite feedback loop. Detection runs as a po",
  "meaning_verification": "none — the paraphrase prompt includes the phrase \"while preserving the meaning\" but no programmatic check (cosine similarity, NLI, BLEU, etc.) is applied to verify meaning survived.",
  "fact_preservation": "none — no special handling of numbers, citations, or quotes anywhere in the codebase.",
  "mechanism": "Cross-model paraphrasing as a watermark-scrubbing attack. The repo implements the real Kirchenbauer (2023) green-list watermark in `src/kirchenbauer_watermark.py` using GPT-2 with logit access: for each token position it hashes the previous token to partition the vocabulary into green/red lists (50/50 split, configurable gamma), then adds a delta b"
 },
 {
  "name": "M1noa/ai-detection-bypass",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "unclear — repo does not exist; cannot verify",
  "meaning_verification": "none — repo does not exist; cannot verify",
  "fact_preservation": "none — repo does not exist; cannot verify",
  "mechanism": "Cannot be determined — the repository returns HTTP 404 from the GitHub API and raw content endpoint. No source code or README was retrievable."
 },
 {
  "name": "o2161405/GPTZero-Bypasser",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist; cannot verify",
  "meaning_verification": "none — repo does not exist; cannot verify",
  "fact_preservation": "none",
  "mechanism": "Cannot be determined — the repository returns HTTP 404 from both the GitHub REST API and direct URL fetch. No source code or README was retrievable."
 },
 {
  "name": "CoalElderLobby/ai-detection-bypass-gptzero-turnitin",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — there is no code at all, let alone a detector loop",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No transformation mechanism exists. The repo contains only a README.md and a license.txt file — no source code of any kind. The README is a warez/crackware distribution page: it claims to distribute a \"pre-activated full build\" of unnamed AI-detection-bypass software and links out to a Telegra.ph URL (https://telegra.ph/Download-06-01-11) for a bin"
 },
 {
  "name": "ai-paraphrase-rewrite-to-bypass",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — the README describes a one-shot rewrite with adjustable \"intensity\" sliders; there is no mention of scoring output, feeding scores back, or iterating. The tool applies its transformation once and",
  "meaning_verification": "none — the README says the tool \"performs best when the original meaning remains clear\" and instructs users to review output manually. No automated semantic gate of any kind is described or evidenced.",
  "fact_preservation": "none — numbers, citations, and quotes are not mentioned anywhere in the README. No preserve-locking mechanism is described.",
  "mechanism": "Closed-binary Windows GUI application distributed as a compiled ZIP (paraphrase_to_bypass_ai_rewrite_v1.8.zip, ~465 KB). No source code is present anywhere in the repo. The README describes two techniques: \"Structure Adjustment\" (varying sentence lengths and patterns) and \"Vocabulary Enhancement\" (synonym substitution). These are classic rule-based"
 },
 {
  "name": "nicojan/humanize-text-prompt",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no feedback loop, and no iteration. The user pastes prompt + text once and receives a single edited output.",
  "meaning_verification": "none — the prompt contains no instruction to check that meaning survived the edit, and no automated gate of any kind.",
  "fact_preservation": "none — no mention of protecting numbers, citations, or quoted material. The prompt rules operate purely at the stylistic/structural level.",
  "mechanism": "A single markdown file (PROMPT.md) containing a structured editing prompt the user pastes into any LLM. No code executes. The LLM reads six layers of rules — vocabulary (replace AI-overused terms, unpack nominalizations, vary hedging density), structure (widen sentence-length variance, eliminate em-dashes and antithesis frames), tone (allow negativ"
 },
 {
  "name": "DevIsper/Prompts-ChatGPT",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the prompt is single-pass. The user submits text, ChatGPT rewrites it once. There is no scoring step, no feedback from any detector, and no iteration.",
  "meaning_verification": "none — the system prompt says \"without changing its meaning\" as an instruction to the LLM, but there is no automated gate, similarity check, NLI step, or any other verification that meaning was actual",
  "fact_preservation": "none — no number retention, no citation locking, no quote preservation mechanism of any kind. Numbers and citations are rewritten freely alo",
  "mechanism": "The repo is a collection of leaked system prompts for OpenAI custom GPTs (the \"GPTs\" product). The humanizer-relevant entry is \"Humanizer Pro.md\" (by charly-ai.com), which contains the verbatim system prompt extracted from the Humanizer Pro GPT. The system prompt instructs ChatGPT to rewrite submitted text to be \"human-like\" and bypass detectors, d"
 },
 {
  "name": "cammycurry/humanize-ai-text-npm",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector, no scoring, no iteration, no re-scoring. Single-shot API call, single response.",
  "meaning_verification": "none — the SDK passes text to the remote API and returns the response verbatim. No similarity check, no NLI gate, no semantic comparison of any kind.",
  "fact_preservation": "none — no numeral retention check, no citation locking, no quote preservation. The API response is returned as-is.",
  "mechanism": "Thin TypeScript/JavaScript class (`HumanizedAI` in `src/index.ts`) that makes a single POST request to `https://api.humanize-ai-text.ai/v1/humanize` with the raw text in the body, bearer-token authenticated. The `run()` method fires and returns whatever the closed-source commercial service sends back. No local processing of any kind — the SDK has z"
 },
 {
  "name": "nolan-vale/humanizer-ru",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist; cannot determine",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Repository does not exist. GitHub API returns HTTP 404 for https://github.com/nolan-vale/humanizer-ru. No source code, README, or any other content is available to inspect. A search for \"humanizer-ru\" on GitHub returns roughly a dozen repositories by other authors (smixs, ilyautov, Vladimir-Human, thevseprod, yasikvlad, etc.), none of which are att"
 },
 {
  "name": "humanizador-de-texto/humanizar-texto-ia",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no evidence of any detector, scoring step, or iterative loop. The site copy makes no mention of detection evasion or re-scoring.",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No source code exists. The GitHub account (created 2025-06-13) contains a single public repo with one file: a 195-byte README.md that names the service and links to humanizador.org. The website describes itself as using a large language model trained on text data to \"analyze AI writing patterns\" and apply sentence-structure variation, vocabulary si"
 },
 {
  "name": "menpente/humanizador",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no. The tool computes an \"IA Score\" heuristic (`calcularScoreIA`) that counts occurrences of a short list of formal Spanish transition words and vocabulary (13 patterns total, weighted 0.5-0.9), displ",
  "meaning_verification": "none. No cosine similarity, NLI, semantic-role, or entailment check is run. The mT5 paraphrase output is accepted if `parafraseado.length > 5` and it differs from the input string—a purely surface-lev",
  "fact_preservation": "Partial, for citations and acronyms only. `protegerContenido` replaces APA-style citations `(Author, YYYY)`, numeric references `[N]`, seque",
  "mechanism": "Single self-contained HTML file with all logic in inline JavaScript. The `HumanizadorTexto` class applies a sequential pipeline of regex-based rule passes to Spanish text: (1) placeholder-locking of APA citations, numeric references, acronyms, and URLs; (2) formal-word substitution via a hardcoded ~15-entry dictionary (`reducirFormalidad`); (3) for"
 },
 {
  "name": "humanizar-texto-ia",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "unclear — the marketing site claims detection bypass against named detectors but gives zero implementation detail; no loop architecture can be verified because there is no code",
  "meaning_verification": "none — no code exists to inspect; marketing copy makes no mention of meaning preservation",
  "fact_preservation": "none — no code exists to inspect",
  "mechanism": "No source code exists. The GitHub user account (github.com/humanizar-texto-ia, created 2024-10-03) has zero public repositories. A separate account (humanizador-de-texto/humanizar-texto-ia) has exactly one file — a 195-byte stub README with no code whatsoever. The associated commercial site (humanizar-texto.net) describes \"advanced machine learning"
 },
 {
  "name": "bazil3814/humanize-ai-text",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — source inaccessible",
  "meaning_verification": "none — source inaccessible",
  "fact_preservation": "none — source inaccessible",
  "mechanism": "Cannot determine. All access attempts returned HTTP 401 Unauthorized (Space page, HF API, raw file endpoints) or HTTP 404 (live Gradio app URL). The Space is either private, deleted, or never publicly deployed."
 },
 {
  "name": "khrisat/text-humanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "No. src/services/detector.py returns round(random.uniform(20, 90), 2) — entirely fabricated random numbers. The humanizer.py does record a score_before and score_after, but both come from this fake st",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Repository returns HTTP 404 and is confirmed deleted. Evidence reconstructed from fork yo-steven/text-humanizer-exploration-20260613 (upstream commit 5c3712147408c5a2826d0eec2f1d56046fe828e3). Mechanism: single-pass LLM rewrite via OpenAI-compatible API (default: DeepSeek). src/services/humanizer.py calls generate() via an AsyncOpenAI client; src/s"
 },
 {
  "name": "joanPlepi/watermark-spoofing",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo is absent; cannot inspect code",
  "meaning_verification": "none — repo is absent",
  "fact_preservation": "none — repo is absent",
  "mechanism": "Cannot be determined — the repository does not exist. The GitHub user joanPlepi exists (NLP researcher at CAISA Lab / Mirelo AI, University of Bonn) and has 17 public repos, none of which is watermark-spoofing. No redirect, fork, or rename is present."
 },
 {
  "name": "TokenFool/TokenFool",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo not found",
  "meaning_verification": "none — repo not found",
  "fact_preservation": "none — repo not found",
  "mechanism": "Cannot be determined — the repository does not exist. GitHub API returns 404 for TokenFool/TokenFool. A search for \"TrustThink/tokenfool\" surfaces an unrelated image-transformer attack toolkit with no connection to AI-text detection. No arXiv paper titled \"TokenFool\" about token-level perturbations for detector evasion could be found."
 },
 {
  "name": "jkodner05/AuTexTification-system",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Cannot be determined — the repository does not exist. `gh api repos/jkodner05/AuTexTification-system` returns HTTP 404, and a full enumeration of the account's 30 public repositories (`gh api users/jkodner05/repos`) contains no repo by that name or any name matching AuTexTification, humaniz, or detector-evasion. The account (Julie Kodner, NLP/lingu"
 },
 {
  "name": "grad-escape/GradEscape",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "unclear — repo does not exist; the claimed description implies yes (queries a surrogate detector for directional perturbations), but this cannot be verified",
  "meaning_verification": "none — unverifiable",
  "fact_preservation": "none — unverifiable",
  "mechanism": "Cannot be determined — the repository does not exist. The GitHub user/org \"grad-escape\" returns 404 and no repo named \"GradEscape\" under that account was found. A broad GitHub search for the name returns only two unrelated \"grade escape\" game repos (2020, 2025). No alternative location was discovered."
 },
 {
  "name": "Symanto-Research/autextification",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist to inspect",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Repository does not exist. GitHub returns HTTP 404 for https://github.com/Symanto-Research/autextification. The symanto-research org has 18 public repos, none named autextification. The AuTexTification 2023 shared task evaluation code is actually hosted at autextification/AuTexTificationEval (a separate GitHub user account), which contains only an "
 },
 {
  "name": "BunsenFeng/machine-generated-text",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "unclear — The repo does not exist to inspect. The related YichenZW/Robust-Det runs detectors over attacked outputs for evaluation but the attacks themselves are applied open-loop (no iterative rescore",
  "meaning_verification": "none — the repo cannot be verified",
  "fact_preservation": "none",
  "mechanism": "Repository does not exist. BunsenFeng (Shangbin Feng) is a co-author of the ACL 2024 \"Stumbling Blocks\" paper, but the code for that paper is at YichenZW/Robust-Det, not this URL. That paper's repo implements post-generation attacks (typo insertion, homoglyph substitution, synonym substitution, span perturbation, paraphrasing), pre-generation attac"
 },
 {
  "name": "yushengzh/TSAPA",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — the watermark detector is not queried during the evolutionary loop. PLL (Pseudo-Log-Likelihood) computed by a surrogate LM is the only signal used to identify suspicious tokens and drive fitness.",
  "meaning_verification": "Cosine similarity via sentence-transformers enforces a feasibility floor of 0.75 and a soft target band of [0.80, 0.92]; candidates outside the band are penalised in the multi-objective ranking but no",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or named entities. The catastrophe rewrite prompt contains a natural-language instructio",
  "mechanism": "Multi-objective genetic/evolutionary algorithm targeting LLM watermark removal. Two core files implement the attack: ga_brief_v2.py (article continuation) and ga_qa.py (QA). Each generation: (1) batch_find_suspicious_words_v2 identifies the top-8 lowest Pseudo-Log-Likelihood (PLL) tokens — treated as likely watermark-carrying positions — and constr"
 },
 {
  "name": "igotyabingo/wmstealing",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — the KGW watermark detector (WatermarkDetector / detect_wm_KGW) is called once per sample after generation to record the p-value for evaluation metrics. It does not gate rewriting or trigger addit",
  "meaning_verification": "none — no cosine similarity, NLI entailment, or semantic-role checks. A GPT-based quality prompt (scoring style, consistency, and accuracy 1-10 for spoofing; 1-100 quality for scrubbing) is run post-h",
  "fact_preservation": "none — the GPT evaluation prompt includes an \"Accuracy\" criterion that penalizes invented facts, but this is purely a post-hoc measurement; ",
  "mechanism": "Implements two published watermark-stealing baseline attacks against KGW-style token-distribution watermarks: JSV (Jovanovic et al., ICML 2024) and DeMark (Chen et al., arXiv 2410.13808). Both attacks have scrubbing and spoofing modes. For JSV scrubbing: uses DIPPER paraphraser (dipper-paraphraser-xxl) as a surrogate, guided by a SpoofedProcessor l"
 },
 {
  "name": "JunchuanYu/AuthorMist",
  "stars": 0,
  "category": "fine-tuned-model",
  "detector_in_loop": "yes — but only during training, not inference. Commercial detector APIs serve as the RL reward signal during GRPO fine-tuning. At inference time the trained model runs once with no re-scoring loop.",
  "meaning_verification": "E5-small embedding cosine similarity between original and paraphrased text; paper reports median above 0.94 across all models. No NLI gate, no hedge or numeral checks. KL divergence penalty in GRPO im",
  "fact_preservation": "none — the paper does not describe any mechanism for preserving numerals, citations, or specific factual content. Semantic similarity via em",
  "mechanism": "The paper (arXiv 2503.08716, Isaac David & Arthur Gervais, March 2025) describes GRPO (Group Relative Policy Optimization) RL fine-tuning of a 3B-parameter language model. Six separate detector-specific models are trained, each using a single commercial detector API (GPTZero, Winston.ai, Sapling, Originality.ai, HelloSimpleAI, or OpenAI RoBERTa) as"
 },
 {
  "name": "kinit-sk/mdok",
  "stars": 0,
  "category": "fine-tuned-model",
  "detector_in_loop": "no — mdok IS the detector; it contains no rewrite loop, no evasion feedback, and no scoring of its own output. It is purely a classifier that other tools would need to evade.",
  "meaning_verification": "none — it is a classifier, not a rewriter; meaning preservation is irrelevant to its function.",
  "fact_preservation": "none",
  "mechanism": "QLoRA fine-tuning of Qwen3-14B (binary) and Qwen3-4B (multiclass) for sequence classification as an AI-text detector. The core robustness trick lives in get_data() in mdok-binary.py: during training, 10% of machine-generated texts are passed through a homoglyph() function that applies 5% per-character Unicode confusable substitutions plus 5% per-po"
 },
 {
  "name": "pan25-generative-ai-authorship-verification",
  "stars": 0,
  "category": "research-code",
  "detector_in_loop": "no — DIPPER paraphrase and prompt obfuscation are one-shot steps used to build the benchmark dataset; the baselines are scored after the fact by the evaluator. No feedback loop exists anywhere in the ",
  "meaning_verification": "none — neither the DIPPER paraphrase path nor any prompt-obfuscation template applies a semantic gate. Meaning degradation from Yoda grammar or random-word injection is deliberate and unmonitored.",
  "fact_preservation": "none — no numeral retention, citation preservation, or factual consistency check anywhere in the codebase.",
  "mechanism": "This is a competition evaluation framework, not a humanizer. It has two distinct sides. Detection side: three baselines — (1) Binoculars, a perplexity/cross-perplexity ratio detector using LLaMA-3.1-8B as observer and LLaMA-3.1-8B-Instruct as performer (binoculars.py, _get_score_impl computes -(log_ppl / x_ppl)); (2) PPMd compression-based cosine s"
 },
 {
  "name": "ii5/Humanizer_transformers",
  "stars": 0,
  "category": "paraphrase-model",
  "detector_in_loop": "no — _calculate_ai_probability() computes an internal heuristic score (sentence-length consistency, transitional-phrase density, contraction absence, formal-word density, lexical diversity) but no ext",
  "meaning_verification": "weak partial: _is_quality_paraphrase_enhanced() accepts a T5 output only when MiniLM cosine similarity to the original falls in the 0.7–0.95 window, filtering out degenerate paraphrases. No NLI entail",
  "fact_preservation": "none — no logic locks numbers, citations, or quoted strings before transformation",
  "mechanism": "transformer/app.py::AdvancedAcademicTextHumanizer._apply_ai_avoidance_techniques() runs a sequential pipeline: (1) heuristic pre-processing — contraction injection, sentence-structure diversification, formality reduction via _add_natural_variations() and _reduce_formality(); (2) sentence-level T5-small / T5-base paraphrasing via StateOfTheArtHumani"
 },
 {
  "name": "ksanyok/texthumanize-extension",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no. The detector (engine/detector.js) produces a score and verdict shown in the UI but is not wired into a rewrite-score-rewrite cycle. The test suite confirms the AI probability drops after one pass ",
  "meaning_verification": "none formally. The naturalizer uses \"exact-case dictionary hits only\" and blocklists to avoid preposition/complementizer swaps, and the tests assert a change ratio below 12% and that human text is bar",
  "fact_preservation": "URLs, email addresses, and code blocks are explicitly preserved (confirmed in tests). No byte-exact locking of numerals or citations — numbe",
  "mechanism": "Five sequential passes in engine/naturalizer.js, all offline JavaScript: (1) _injectBurstiness — splits uniform-length sentences at conjunctions or commas, occasionally merges short ones; (2) _replaceAiConnectors — swaps \"Furthermore/Moreover\" etc. with language-pack alternatives, deletes connectors 45% of the time; (3) _deNominalize — converts hea"
 },
 {
  "name": "omtoi101/human-typer",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — there is no detector of any kind in the pipeline. No content scoring, no feedback loop, no re-evaluation. The text passes through byte-for-byte; only the delivery mechanism (timing of keystrokes)",
  "meaning_verification": "none — the text content is never modified at all, so meaning preservation is trivially satisfied but also trivially irrelevant: the tool adds nothing and removes nothing from the text.",
  "fact_preservation": "none — not applicable. Text is not modified.",
  "mechanism": "Behavioral input simulation, not text transformation. The tool takes text that already exists (written by an LLM or a human) and replays it into any target application by simulating keystrokes one character at a time, mimicking a human typist. Core logic is in human_typer.py / class HumanTyper. It randomizes per-character delay based on a configura"
 },
 {
  "name": "HumanizerBench/humanizerbench",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — detectors (GPTZero, Originality.ai, Copyleaks, Winston AI, ZeroGPT) are used to score humanizer outputs after the fact, but there is no feedback loop that triggers further rewrites. Detector verd",
  "meaning_verification": "Cosine embedding similarity per test, stored pre-computed in tests.json as meaning_preservation. scoring.js rescales raw cosine from [0.75, 1] to [0, 1] for the sub-score. Severe drift (raw cosine bel",
  "fact_preservation": "none",
  "mechanism": "This repo does NOT transform text. It is a public audit record and benchmark data repository for monthly evaluations of commercial AI humanizer tools. The actual humanization runs happen in a private repo via manual operation of commercial SaaS tools (WriteHuman, HIX Bypass, Humbot, Undetectable.ai, etc.) on their most aggressive settings. The publ"
 },
 {
  "name": "pandeyvishwas51-oss/open-humanizer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "yes — the Critic (Agent B) is an LLM called with an aggressive AI-detector persona that returns a structured JSON score. The pipeline re-runs the Writer only when overall_ai_percentage > target_score.",
  "meaning_verification": "none mechanically. The Writer system prompt says \"Retain 100% of the original factual accuracy, numbers, names, claims\" but this is a soft LLM instruction with no post-hoc check. There is no cosine si",
  "fact_preservation": "Prompt instruction only. The Writer system prompt (config.HUMANIZER_RULES) says \"Preserve all facts, names, numbers, and SEO keywords\" and t",
  "mechanism": "Adversarial Writer/Critic loop over any OpenAI-compatible chat API. Agent B (Critic, pipeline.py `_scan`) calls the LLM with a ZeroGPT/GPTZero-persona prompt (config.py DETECTOR_RULES) to score text as JSON (overall_ai_percentage + per-sentence flags). Agent A (Writer, `_mutate`) calls the LLM again with a style-guide system prompt (config.HUMANIZE"
 },
 {
  "name": "Venigalla/abridged-language-models",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — repo does not exist",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Repository does not exist. The GitHub user venigalla is real (account created 2013, 4 public repos: AutoFramework, docker-cheat-sheet, easy-peasy-bot, jenkins) but has never had a repo named abridged-language-models. A global GitHub search for that name returns zero results. The repo cannot be profiled."
 },
 {
  "name": "finestructure-ai/humanizer-multilingual",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — workflow is audit → rewrite in a single pass; no scoring model is invoked and no re-score loop exists",
  "meaning_verification": "none automated — SKILL.md instructs Claude to \"preserve meaning, facts, names, numbers, and the author's voice\" but there is no cosine similarity gate, NLI check, or any mechanical verification that m",
  "fact_preservation": "Instruction-level only. SKILL.md says \"Never change the facts. Do not invent numbers, quotes, company names, dates, or claims.\" No byte-exac",
  "mechanism": "Pure prompt/instruction package installed as a Claude Code plugin (skill). The entire transformation logic lives in Markdown files: SKILL.md drives the workflow; method.md defines five families of AI-tells (calque, imported punctuation/typography, register flattening, English syntax showing through, empty scaffolding) with severity grades P0/P1/P2 "
 },
 {
  "name": "rayfok/paraphrase-to-evade",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo does not exist; cannot determine",
  "meaning_verification": "none — repo does not exist",
  "fact_preservation": "none — repo does not exist",
  "mechanism": "Repository does not exist. GitHub API returns HTTP 404 for rayfok/paraphrase-to-evade, and an enumeration of rayfok's public repositories confirms no such repo is present. No archived snapshot was accessible either."
 },
 {
  "name": "AmenRa/rank_bm25",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — BM25 is a retrieval scorer, not a rewriter; no loop of any kind",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Repository does not exist. AmenRa is a real GitHub user focused on information retrieval (ranx, retriv), but no repo named rank_bm25 is present under that account. The well-known rank_bm25 Python package belongs to dorianbrown, not AmenRa. Even if the attribution were corrected, BM25 is a lexical document-ranking algorithm — it scores and orders ex"
 },
 {
  "name": "Vicky-Cr/paraphrase-any-sentence",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo not accessible",
  "meaning_verification": "none — repo not accessible",
  "fact_preservation": "none — repo not accessible",
  "mechanism": "Cannot determine — the repository does not exist. The GitHub user Vicky-Cr exists (account created 2019, 0 public repos) but the repo paraphrase-any-sentence returns HTTP 404. It was never public, was deleted, or was made private."
 },
 {
  "name": "Janabi/undetectable-ai",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the package calls only detection endpoints and returns a score; there is no rewrite step and no loop of any kind",
  "meaning_verification": "none — no rewriting occurs, so no meaning check is possible or attempted",
  "fact_preservation": "none — no rewriting occurs",
  "mechanism": "Does not transform text at all. It is a thin TypeScript singleton class (src/undetectableApi.ts, class UndetectableApi, methods detectHumanText and detectIndividualHumanText) that POSTs raw text plus an API key to two endpoints on aicheck.undetectable.ai (/detect and /detectIndividual) and returns the JSON response {human: number}. All computation "
 },
 {
  "name": "hopo84/humbot-humanizer-skill",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "No. The API response includes a \"detection score\" (0-100) as a passive output metric, but nothing in the repo re-submits the output for re-scoring or iterates until a threshold is met. It is a strict ",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Thin two-script OpenClaw skill wrapper around the commercial Humbot API. `scripts/create_task.py` POSTs text to `https://humbot.ai/api/humbot/v1/create` with an API key and a model-tier flag (Quick/Standard/Advanced), receiving a task ID. `scripts/retrieve_result.py` polls `https://humbot.ai/api/humbot/v1/retrieve` every 2 seconds until the task co"
 },
 {
  "name": "StealthGPT/zapier-stealthgpt",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — The API response includes a howLikelyToBeDetected integer field (0–100) that is surfaced as a Zapier output field, but the integration makes exactly one API call and returns. There is no re-scori",
  "meaning_verification": "none — No cosine similarity, NLI entailment, hedge preservation, semantic-role matching, or any other meaning gate exists anywhere in the repo. Text goes out and comes back; no local check confirms th",
  "fact_preservation": "none — No logic in this repo preserves numbers, citations, or quoted material. The entire text is sent to the remote API without any byte-ex",
  "mechanism": "Pure thin wrapper around the StealthGPT cloud REST API. The repo itself contains no text-transformation logic. src/creates/humanize-text.ts POSTs the input text to /api/stealthify/humanize-runs with three tunable parameters (qualityMode: quality|fast, model: heavy|lite, outputFormat: text|markdown) and waits for a webhook callback. src/creates/gene"
 },
 {
  "name": "wordpress-stealthgpt",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — The API response includes a `howLikelyToBeDetected` integer field that the plugin stores as post meta (`_stealthgpt_detection_score`), but this is a single score returned alongside the completed ",
  "meaning_verification": "none — The WordPress plugin has no meaning verification. Whether the StealthGPT backend does any is not visible from this repo.",
  "fact_preservation": "none — The plugin strips all tags from source text before sending (`wp_strip_all_tags`) and applies only basic UTF-8 and control-character s",
  "mechanism": "Pure API wrapper. The plugin sends text to the StealthGPT commercial API endpoint (`POST /api/stealthify/runs`) with `text`, `qualityMode` (fast/quality), `model` (heavy/lite), and `outputFormat: markdown`. All humanization logic is closed-source server-side. The WordPress plugin (open-source) does nothing but: collect the text from the WordPress e"
 },
 {
  "name": "rephrasyai/rephrasy-skills",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — The bypass-turnitin SKILL.md describes \"iterative refinement\" as advice to the human user (edit, then self-check, then optionally use Rephrasy). The detect-ai-text-self-check skill is a heuristic",
  "meaning_verification": "none — SKILL.md guardrails say \"Humanizing changes how it reads, never what it asserts\" and \"Do not invent claims, stats, or quotes,\" but these are prompting instructions, not mechanical gates. No cos",
  "fact_preservation": "The seo-keyword-preserving-rewrite skill explicitly protects target keywords verbatim in anchor positions (title, opening paragraph, H2 subh",
  "mechanism": "Two distinct pathways. (1) Prompt-guide path — 17 SKILL.md files that instruct an LLM agent to follow a heuristic rewriting method. The core humanize-ai-text skill catalogues 8 \"AI tells\": uniform sentence length, hedging scaffolding (delete on sight), rule-of-three overuse, empty transitions (Moreover/Furthermore/Additionally), over-explanation, s"
 },
 {
  "name": "rephrasyai/rephrasy-mcp",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the two tools are independent. The README suggests a manual workflow (\"humanize, then run detect on the result\") but there is no automated score-rewrite-rescore loop anywhere in the code.",
  "meaning_verification": "none",
  "fact_preservation": "none — no mention of numbers, citations, or quotes in the code or documentation",
  "mechanism": "Thin MCP wrapper around Rephrasy's closed cloud API. All transformation is entirely server-side; the repo contains zero local text-processing logic. Two tools are defined in src/index.ts: (1) \"humanize\" — POSTs to https://v2-humanizer.rephrasy.ai/api/ via callRephrasy() with fields text, model (default \"v3\"), optional style ID, optional language; r"
 },
 {
  "name": "n8n-nodes-stealthgpt",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — The completed humanization response includes a `howLikelyToBeDetected` field (0–100, higher = more human), but the node code never reads that value to drive iteration. It is a single-pass fire-an",
  "meaning_verification": "none — No cosine similarity, NLI, semantic-role check, or any other meaning gate exists in this repo. All verification, if any, is entirely opaque inside StealthGPT's backend.",
  "fact_preservation": "none in this repo — Content generation exposes an `enableFactCheck` boolean that is forwarded to the API, but the node itself performs no nu",
  "mechanism": "Pure HTTP API wrapper around StealthGPT's closed-source cloud service. The node posts to two remote endpoints: `/api/stealthify/runs` for text humanization and `/api/stealthify/agent/runs` for the full content pipeline (research → draft → humanize). It then polls the status endpoint until the run reaches a terminal state. All transformation logic i"
 },
 {
  "name": "mcpflow/Text2Go_ai-humanizer-mcp-server",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — detection only, no rewrite step and no iteration. The server exposes one tool (\"detect\") and nothing else.",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Despite the \"AI Humanizer\" name, the entire implementation is a single `detect` tool in `src/index.ts`. It POSTs the user's text to `https://api.edgeshop.ai/rewrite/text-detection` with a chosen detector type (COPYLEAKS or HEMINGWAY) and returns the JSON score. The tool description instructs the host LLM to construct a URL `https://pre-www.text2go."
 },
 {
  "name": "BACH-AI-Tools/bachai-humanizer-apis",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no scoring, no rewrite loop, no iteration of any kind; the server makes one API call and returns the response",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Zero local transformation logic. The repo is an auto-generated MCP server (created by a \"MCP factory\" tool from an OpenAPI spec) that proxies all requests to the closed-source commercial API at `humanizer-apis.p.rapidapi.com` via RapidAPI. `server.py` loads `openapi.json`, creates an async httpx client pointed at that host, injects `X-RapidAPI-Key`"
 },
 {
  "name": "jhubbardsf/rephrasy-skills",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no. A detect-and-iterate loop was sketched in earlier versions (--verify/--iterate/--threshold workflow mentioned in commit history) but was removed in v0.1.2 when the bundled Rephrasy detector was dr",
  "meaning_verification": "none. The API returns a Flesch readability score (`new_flesch_score`), which the skill surfaces to the user but which measures prose difficulty, not meaning preservation. There is no cosine similarity",
  "fact_preservation": "none. No numeral retention, no citation locking, no quote preservation. Numbers and citations pass through the Rephrasy API unchanged only i",
  "mechanism": "Thin wrapper around the commercial Rephrasy Humanizer API. The core logic lives in `plugins/rephrasy/scripts/rephrasy_humanize.py`: its `call_api()` function constructs a JSON payload (text, model, style, language) and POST-requests it to Rephrasy's cloud endpoint with bearer-token auth. Default is model v3 + professional style. The Claude Code ski"
 },
 {
  "name": "nestswordengine/ai-detection-bypass-gptzero-undetectableai",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — there is no code of any kind, let alone a scoring loop",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No text transformation mechanism exists. The repository contains exactly two files — README.md (1361 bytes) and license.txt (1144 bytes) — and zero source code. The README points users to an external download hosted on telegra.ph (a Telegram-affiliated anonymous publishing service). The download is described using crackware vocabulary: \"pre-activat"
 },
 {
  "name": "Jaffdino/ghostwriter-extension",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — The client-side detectAIScore() function in lib/humanizer.js counts regex pattern hits and displays a 0–100 score in the popup UI before and after rewriting, but this score is never fed back into",
  "meaning_verification": "none — The system prompt instructs Claude to \"preserve all core meaning,\" but this is an LLM instruction, not an automated gate. No cosine similarity check, no NLI entailment, no semantic-role match, ",
  "fact_preservation": "none — Numbers, citations, and quoted strings pass through Claude with no byte-exact locking, no preserve-list, and no post-hoc audit. The s",
  "mechanism": "Single-pass LLM call. The entire transformation is one request to claude-opus-4-5 via the Anthropic Messages API. The technique is a ~2000-token system prompt encoding 29 named AI-writing anti-patterns (AI vocabulary words, copula avoidance, em-dash overuse, rule-of-three, superficial -ing endings, inflated significance phrases, chatbot artifacts)."
 },
 {
  "name": "nice811/bypassai-frontend",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no scoring step at any point. The pipeline is: POST /api/humanize -> single LLM call -> return output. No detector is queried before or after rewriting, and there is no retry or re-score",
  "meaning_verification": "none in production. The system prompt includes the instruction \"Keep the original meaning, facts, and core information COMPLETELY unchanged\" but no code enforces or checks this. The offline test file ",
  "fact_preservation": "none. No byte-exact locking of numbers, citations, or quotes. The system prompt says to preserve facts but there is no regex extraction, pre",
  "mechanism": "Single-pass prompt-based rewriting via a third-party LLM API. The backend (server.py, function llm_stream_generator) sends user text to SiliconFlow's chat completions endpoint using the deepseek-ai/DeepSeek-V3 model (configurable via MODEL_NAME env var) with a fixed system prompt (DEFAULT_SYSTEM_PROMPT in server.py) that instructs the model to vary"
 },
 {
  "name": "writeflow-chrome-extension",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no. A \"humanized score\" percentage (30–100%) is shown in the UI, but it is computed entirely locally in background.js:calcHumanizedScore() using a crude word-frequency and transition-word heuristic (p",
  "meaning_verification": "none. No cosine similarity, no NLI, no semantic-role matching, no back-translation round-trip. The output of the first (and only) DeepSeek call is surfaced directly to the user.",
  "fact_preservation": "none. Numbers, citations, and quotes receive no special treatment. They flow through the LLM prompt unchanged or altered at the model's disc",
  "mechanism": "Single-pass prompt-to-LLM rewrite via the DeepSeek API (model: deepseek-chat). Implemented in background.js:rewriteText(). The user picks one of four style modes (Formal, Simple, Creative, Academic); each maps to a fixed system prompt instructing DeepSeek to rewrite the pasted text in that register at temperature 0.7, max 2048 tokens. The result is"
 },
 {
  "name": "yufe99/bypass-ai-source",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the AIDetector component (src/components/AIDetector.tsx) POSTs to /score and displays the result as a standalone UI widget. The HumanizeInput component calls /humanize and then shows the aiScore ",
  "meaning_verification": "none visible in this repo. The academic page claims citation and technical-term preservation via placeholder locking, and the how-it-works page lists \"citations, numbers, units, equations, quoted mate",
  "fact_preservation": "Claimed for the academic mode: citation markers ([12], Smith et al. 2023), technical abbreviations (PCSK9, LDL-C), numbers, units, and quote",
  "mechanism": "This repo is purely the Next.js 15 / TypeScript marketing and UI frontend for BypassAI.online, deployed as a static export to Cloudflare Pages. All actual rewriting happens in a closed Cloudflare Worker at https://bypass-ai-api.yufe99.workers.dev — that backend is not in this repo. The frontend's HumanizeInput.tsx (src/components/HumanizeInput.tsx)"
 },
 {
  "name": "nacmonad/NewsGooner",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the pipeline is strictly linear: rewrite once, then score once. The score (human 0–100 from undetectable.ai) is persisted to the database for editorial review, but nothing reads it back to trigge",
  "meaning_verification": "none — the only post-rewrite check is a minimum-length guard (rewritten content must be at least 30% of input length and at least 50 chars). No cosine similarity, no NLI entailment, no semantic-role m",
  "fact_preservation": "none — citation markers that Perplexity appends (`[1]`, `[2]`, etc.) are actively stripped by the rewriter (`replace(/\\[\\d+\\]/g, '')`). Numb",
  "mechanism": "Single-pass LLM paraphrase via Perplexity AI \"sonar\" model (OpenAI-compatible API). The system prompt in `backend/src/rewriter/rewriter.service.ts` (`rewriteContent` method) instructs the model to rewrite in \"a professional journalistic tone, in the voice of a superior, skeptical, yet fair crypto journalist\" with \"varied syntax and concrete, idiosy"
 },
 {
  "name": "stasiiakondratiuk/humanizer",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, no re-score loop. Single-pass rewrite only.",
  "meaning_verification": "Instruction-only: SKILL.md tells Claude to preserve facts and not invent numbers/cases/names, and includes a self-check bullet (\"Сохранён ли смысл и все факты? Ничего не выдумано?\"). The eval runner (",
  "fact_preservation": "Weak string-match gate via evals/check.py \"keep\" field — verifies specific substrings (numbers, key nouns) survive in the output. The SKILL.",
  "mechanism": "Pure prompt-guide / rule-based rewriter delivered as a Claude Code skill. The transformation is performed entirely by Claude itself following written rules in SKILL.md and three language reference files (references/ru.md, references/uk.md, references/en.md). The skill instructs Claude to: (1) auto-detect language (RU/UK/EN), (2) detect format/regis"
 },
 {
  "name": "pengchenghaoche51/turnitin0_skill",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — The optional `--ai-report-id` flag can pass a report ID from a prior Turnitin check to the humanizer endpoint (presumably so the server can use it to guide rewriting), but there is no client-side",
  "meaning_verification": "none — No cosine similarity, NLI entailment, semantic-role matching, hedge preservation, or any other meaning gate appears anywhere in the client code.",
  "fact_preservation": "none — No numeral retention, byte-exact locking of citations, or any protection of numbers or quotes in the client code.",
  "mechanism": "Pure REST API wrapper around the closed commercial turnitin0.com backend. The client skill (`skills/turnitin0/scripts/humanize.py`) does three things: uploads text or a .docx/.txt file to `/api/documents/from-text` or via a presigned OSS URL (`/api/oss/presign_upload` + `PUT` + `/api/oss/confirm_upload`), creates a task with `POST /api/humanizer/ta"
 },
 {
  "name": "TrophySecure/ai-content-humanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the README describes a straight single-pass rewrite with no scoring, re-scoring, or iteration. There is no mention of a detector anywhere in the documentation.",
  "meaning_verification": "none — the README claims \"Maintain Meaning: Keep your original message\" as a marketing bullet, but no automated gate (cosine similarity, NLI, SRL, etc.) is described or evidenced anywhere in the docs ",
  "fact_preservation": "none — no mention of numeral retention, citation locking, or quote preservation anywhere in the README.",
  "mechanism": "Single-pass LLM rewrite via Mistral Large (mistral-large-latest). User sends text; the Apify actor calls the Mistral API with a humanization prompt; the rewritten text is returned. The README describes the flow as: input text → Mistral Large analyzes and transforms → humanized output. No source code is present in the repository (the repo contains o"
 },
 {
  "name": "granitevolition/text-humanizer-api",
  "stars": 0,
  "category": "fine-tuned-model",
  "detector_in_loop": "no — no detector is instantiated or called at any point in the pipeline; there is no score-rewrite-rescore loop",
  "meaning_verification": "none — the select_best_sentence prompt asks for \"almost the same meaning\" and clean_text is told not to add content, but both are soft natural-language instructions with no automated gate (no cosine s",
  "fact_preservation": "The clean_text GPT-4o system prompt says \"You must ensure all intext citations are correctly present in the rewritten text,\" but this is a p",
  "mechanism": "Three-stage sentence-by-sentence pipeline, all in humanizer.py. (1) extract_sentences() splits on punctuation with abbreviation-protection placeholders. (2) process_sentence() fires two parallel asyncio calls to a private fine-tuned GPT-3.5-turbo (ft:gpt-3.5-turbo-0125:personal::9hpCfvVt) with two system-prompt slots that are, in the shipped code, "
 },
 {
  "name": "EpicSaber/ai-text-humanizer-api",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "yes, but only for before/after reporting — not iterative rewriting. The README flowchart shows: score input → apply 12 passes → re-score output → return both scores. There is no loop that feeds the po",
  "meaning_verification": "none. The API accepts a preserveMeaning: true flag but the README says nothing about how meaning is checked. No cosine similarity, NLI entailment, semantic-role matching, or any other gate is describe",
  "fact_preservation": "none. No mention of protecting numbers, citations, or quoted strings anywhere in the README or API schema.",
  "mechanism": "README-only repo — there is no source code whatsoever. The repository contains a single file (README.md), created in one commit on 2026-05-21. The actual implementation is a closed-source Apify actor (george.the.developer/ai-text-humanizer-api). The README describes a claimed 12-pass rule-based pipeline: Phase 1 (passes 1-3) replaces 40+ AI transit"
 },
 {
  "name": "aditya123-ask/ai-humanizer",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the \"self-improving\" loop does not call any AI-text detector. LearningEngine.analyze_performance() is a hand-written heuristic that counts phrase occurrences and sentence-length ratios; it never ",
  "meaning_verification": "none — the heuristic tracks technical-term retention (regex over common suffixes and acronyms) but there is no cosine similarity gate, NLI entailment check, or any semantic comparison against the orig",
  "fact_preservation": "none — numbers and citations receive no special handling; there is no byte-exact preserve-locking or even a regex pass to check that numeral",
  "mechanism": "Single-shot prompt engineering via Google Gemini API (gemma-3-4b-it via generativelanguage.googleapis.com). The prompt instructs the model to write as an \"experienced engineering project lead\" in a conversational meeting tone, with explicit rules for sentence length variety, informal transitions, first-person perspective, and \"controlled redundancy"
 },
 {
  "name": "alas-m/Mimesis-AI-Text-Humanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no scoring step, no iteration, and no re-scoring loop described anywhere in the README or visible in the repo",
  "meaning_verification": "none",
  "fact_preservation": "none — no mention of numeral retention, citation locking, or quote preservation",
  "mechanism": "The repo is a storefront README only — no source code is publicly available (the two files present are README.md and LICENSE). Based on the README: (1) User exports their Telegram chat history as JSON; (2) A pipeline (src/1_ingestion.py → 2_cleaning.py → 3_profiling.py) parses and labels messages into Chatting/Formal/Academic styles and produces a "
 },
 {
  "name": "StealthHumanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "yes — the internal heuristic detector (lib/detector.ts, 12-metric heuristic covering perplexity, burstiness, vocabulary diversity, AI phrase density, passive voice, transition word frequency, sentence",
  "meaning_verification": "partial — lib/server/humanization-governance.ts implements a regression guard (applyRewriteRegressionGuard) that computes unigram, bigram, and trigram Jaccard overlap between original and candidate, c",
  "fact_preservation": "Structural regions (URLs, markdown links, code blocks, inline code, blockquotes, email addresses, @-mentions, #-hashtags) are placeholder-lo",
  "mechanism": "4-layer pipeline implemented in TypeScript/Next.js. Layer 1 (lib/humanizer.ts, lib/prompts.ts): sends text to a user-supplied LLM (any of 13 providers) using anti-detection system prompts that instruct the model to maximize burstiness, inject perplexity, avoid AI-typical phrases, and vary sentence length and paragraph structure. Layer 2 (lib/postpr"
 },
 {
  "name": "karthiksreenivasanp/Grammer-AI",
  "stars": 0,
  "category": "fine-tuned-model",
  "detector_in_loop": "no — there is no detector call anywhere in the codebase. The word \"detector\" only appears in the CLI banner (\"ANTI-DETECTOR V2\") and in the README marketing copy. There is no scoring, no iteration, an",
  "meaning_verification": "none — no cosine similarity, no NLI, no semantic-role check, no back-translation comparison. The prompt says \"keep the technical meaning accurate\" but nothing in the code verifies that it did.",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special handling. They pass through the LLM call like any other token and can be si",
  "mechanism": "Single-pass LLM paraphrase via llama-cpp-python running Qwen-2.5-3B-Instruct (Q4_K_M GGUF, ~2.3 GB). The entire logic is in main.py: `process_full_text` splits input on double-newlines into paragraph blocks, then calls `humanize_chunk` on each block sequentially. `humanize_chunk` builds a one-shot chat-completion prompt with a hardcoded system inst"
 },
 {
  "name": "macrochen/remove-ai-flavor-skill",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration. The LLM rewrites once based solely on the prompt. No re-scoring or convergence criterion exists.",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role matching, no round-trip check of any kind.",
  "fact_preservation": "none — numbers and citations receive no special treatment. Mode B actively encourages the LLM to *invent* specific numbers where the origina",
  "mechanism": "Pure prompt engineering — the entire implementation is SKILL.md, a system-prompt file consumed by Gemini CLI. No code exists. The prompt instructs the LLM to: (1) classify input as emotional/narrative (\"Mode A\") or science/explanation (\"Mode B\"); (2) apply mode-specific rewriting rules (Mode A: concretise abstractions, inject subjectivity, fragment"
 },
 {
  "name": "2JIHAN/doc-human-tation",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no trained detector in the loop. The pre/post scoring (`metrics_en.py`, `metrics.py`) is hand-coded rule counting (regex + heuristic counters), not a trained classifier. The verify_gates",
  "meaning_verification": "Structural and surface-level only; no semantic/embedding check. verify_change_rate.py enforces a character-level edit-distance cap (WARN ≥30%, ABORT ≥50%). verify_gates.py adds three more axes: P1 che",
  "fact_preservation": "Yes, at multiple levels. checks.py has a `quote_altered` gate (direct quotes must remain verbatim) and a `number_injected` gate (any numeric",
  "mechanism": "Claude Code skill/plugin (installs via `npx skills add` or `/plugin marketplace add`). The host LLM (Claude itself) rewrites text directly from bundled rule prompts; no separate model is called. Two language tracks, each with its own rule catalogue and Python verification scripts (stdlib only):\n\nEnglish: `skills/doc-human-tation/references/language"
 },
 {
  "name": "LocalWritingHumanizer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — the iterative retry loop scores chunks on preservation criteria (numbers preserved, proper noun overlap ≥ 0.90, no drift/fallback warning fragments) via score_chunk_attempt(). No AI-text detector",
  "meaning_verification": "partial — rapidfuzz token_set_ratio < 45 raises a large-drift warning (evaluate_drift_warnings); Counter equality on extracted numbers flags mismatches; proper noun set-overlap ratio tracked. No NLI e",
  "fact_preservation": "numbers: Counter equality check between original and rewritten, warns if mismatched; proper nouns: set-overlap ratio (warns below 0.70, \"unr",
  "mechanism": "Single file: humanizer.py (~870 lines). Two engines. Rules engine (apply_rule_based_rewrite): (1) double-quoted strings are token-substituted before rule passes and restored after (protect_double_quotes/restore_tokens); (2) ~6 phrase replacements via PHRASE_REPLACEMENTS dict (\"it is important to note that\" → \"notably,\" etc.); (3) 4-pattern AI-hedgi"
 },
 {
  "name": "passeth/obsidian-ai-humanizer",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector invoked at any stage. The pipeline applies the three transformation modules once and outputs the result. There is no scoring step, no re-score, and no iterative convergence l",
  "meaning_verification": "none — the system prompts instruct the GPT model to \"preserve the core meaning\" in natural language, but there is no programmatic check (no cosine similarity, no NLI, no semantic-role comparison) that",
  "fact_preservation": "none — numbers, citations, and quoted strings receive no special handling. No preserve-locking, no numeral retention check.",
  "mechanism": "Three-module pipeline implemented in TypeScript as an Obsidian plugin, ported from a Korean \"GPT Checker\" Python backend. (1) StructureTransformer (src/structure-transformer.ts): sentences are split by a KoreanNLP regex splitter, then each sentence is stochastically routed — randomly split via openai.splitSentence(), merged with the next sentence v"
 },
 {
  "name": "dmazumdar186/humanizer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no detector anywhere in the codebase. The pipeline is a single forward pass: pre-pass → LLM rewrite → post-process. No scoring step, no iteration, no re-scoring.",
  "meaning_verification": "none mechanically. The system prompt instructs the LLM \"Keep ALL meaning. Never add new claims or facts not in the original\" but there is no cosine similarity check, NLI entailment gate, semantic-role",
  "fact_preservation": "none. The system prompt says \"Never add new claims or facts\", but there is no byte-exact preserve-locking of numbers, citations, or quotes. ",
  "mechanism": "4-stage pipeline in humanizer.py. Stage 1 (_rules_pre_pass): deterministic regex pre-pass strips opening fluff (RE_OPENING_FLUFF: \"Certainly!\", \"I'd be happy to\", etc.), closing fluff (RE_CLOSING_FLUFF: \"I hope this helps!\", \"Let me know if you have questions\"), replaces em-dashes, flags banned vocab from DEFAULT_BANNED_VOCAB (16 words: delve, leve"
 },
 {
  "name": "marsoyang1/ai-naturalizer-free",
  "stars": 0,
  "category": "adversarial-perturbation",
  "detector_in_loop": "no — the pipeline is a fixed four-stage transform; no detector is queried, no score is computed, and there is no iterate/re-score loop",
  "meaning_verification": "none — this is an image tool; the concept of semantic meaning preservation does not apply",
  "fact_preservation": "none — image domain only; numbers, citations, and quotes are not relevant",
  "mechanism": "Four-stage pixel-level image manipulation pipeline implemented entirely in `app/naturalize.ts` running client-side via the browser Canvas API. Stage 1: screen texture overlay — alternating-row subtraction/addition of B/G channel values (offset = 12.75 * strength). Stage 2: lens chromatic aberration — diagonal one-pixel R/B channel displacement with"
 },
 {
  "name": "Bishek-lpu/Hummanize",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — detection and humanization are independent API actions; the humanize path never scores its own output, never re-scores after rewriting, and contains no iteration loop of any kind",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role matching, no hedge retention check; the rewrite output is returned as-is after Pass 2",
  "fact_preservation": "none — no numeral retention, no byte-exact locking of citations or quotes; numbers and named facts can be silently altered by the rewrite pr",
  "mechanism": "Two-pass \"Ghost Mode\" prompt pipeline wrapping Claude Sonnet 4.6 via the Anthropic API. Both passes live in `src/app/api/process/route.ts`, function `handleHumanize`. Pass 1: calls Claude with `thinking: {type: 'adaptive'}` (extended/adaptive thinking mode) to forensically analyse AI fingerprints and emit a JSON strategy object containing `fingerpr"
 },
 {
  "name": "Carrie2026-ai/preschool-human-writing--skill",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no rewrite loop, and no detector of any kind mentioned or implied",
  "meaning_verification": "none — the guide instructs the writer not to invent child speech or emotions from photos, and to use hedged language (\"appears to,\" \"may indicate\") for inferences, but these are authorial discipline r",
  "fact_preservation": "none mechanically — the four-tier risk-level taxonomy (A–D) is an instruction to the writer to stay within factual bounds at the strictest l",
  "mechanism": "Pure prompt/style guide — no code. SKILL.md is a textual instruction set that tells an LLM to: (1) identify which of eight named \"writing channels\" the task falls into (Administrative, Formal Elegant, Clear Friendly, Warm Nurturing, Modern Elegant, Classical Poetic, Celebratory Solemn, Professional Restrained); (2) suppress a catalogue of named AI "
 },
 {
  "name": "gheetdufa/ai-humanizer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — analyze() provides an output-side tell-score but the tool does not feed that score back into another rewrite pass. The pipeline is: clean → LLM rewrite → display score. There is no iterate-until-",
  "meaning_verification": "none — the LLM system prompt says \"same facts\" and \"output ONLY the rewritten text,\" but there is no cosine similarity check, NLI entailment, hedge preservation test, semantic-role comparison, or any ",
  "fact_preservation": "none — numbers and citations are explicitly not validated. The README and source both acknowledge no factual verification. Quotes receive fo",
  "mechanism": "Two-stage pipeline in a single index.html (no build step). Stage 1 (offline, \"Quick Clean\"): stripHidden() removes zero-width joiners, narrow no-break spaces, BOMs, and soft hyphens; straighten() converts curly quotes and em/en dashes to ASCII; replacePreserving() swaps ~150 buzzword phrases (PHRASES, WORDS, CONTRACTIONS arrays); forceHumanRhythm()"
 },
 {
  "name": "mabdullahab614-alt/phantom-writer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — there is no scoring loop of any kind. The app produces one output and stops. The trained ML model is never invoked by the application at all.",
  "meaning_verification": "none",
  "fact_preservation": "none — the five replacement rules could corrupt numbers or citations that happen to follow a trigger phrase (e.g., \"In conclusion, see Table",
  "mechanism": "All transformation logic is browser-side JavaScript in index.html. The \"forensic scan\" (runForensicScan) checks for the words \"furthermore\"/\"moreover\" and uses text.length thresholds to hard-code three possible AI probability outputs (12%, 31%, 84%); burstiness and TTR figures shown in the UI are Math.random() calls. The \"humanizer\" (applyHumanizer"
 },
 {
  "name": "ghostwriter-tool",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — single pass only; the \"AI Risk: Original: High → After: Low\" badge in the output is Gemini self-reporting inside the structured prompt format, not a real detector score; there is no external dete",
  "meaning_verification": "none — the SYSTEM_PROMPT instructs Gemini to \"Preserve 100% of original meaning and facts\" but there is no automated check; no cosine similarity gate, no NLI entailment, no semantic-role matching",
  "fact_preservation": "none — no byte-exact locking, no numeral retention check; the prompt verbally asks for fact preservation but provides no enforcement mechani",
  "mechanism": "Single-pass prompt engineering via Gemini API. The entire logic lives in a 260-line SYSTEM_PROMPT constant inside index.html. The prompt instructs Gemini to (a) hard-ban 50+ named AI-fingerprint words/phrases (\"leverage\", \"delve\", \"tapestry\", etc.), (b) vary sentence length aggressively (burstiness injection), (c) start sentences with conjunctions "
 },
 {
  "name": "Carrie2026-ai/preschool-human-writing",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the skill document contains no mention of any detector, scoring mechanism, or iterative rewrite loop. There is no score-then-rewrite cycle of any kind.",
  "meaning_verification": "none — no semantic similarity check, NLI step, or any automated meaning gate. The guide has editorial rules (don't invent child dialogue, don't delete substantive content) but these are LLM instructio",
  "fact_preservation": "The guide instructs the LLM to preserve dates, numbers, children's names, and policy language, and to avoid inventing facts. These are promp",
  "mechanism": "Pure prompt / style guide. The repo contains a single large Chinese-language Markdown file (学前教育行业去AI味写作_SKILL.md, 43 KB) packaged as a Claude \"skill\". It instructs the LLM to classify the writing task into one of nine preschool-document types (parent notice, activity report, family-school communication, official document, honor announcement, multi"
 },
 {
  "name": "n9rrrx/TruthAI",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no. DetectorService.php runs a weighted ensemble of four commercial APIs (Sapling 35%, Writer.com 30%, GPTZero 20%, Copyleaks 15%) and stores a consensus score, but there is no code path that feeds a ",
  "meaning_verification": "none. No cosine similarity, NLI entailment, semantic-role check, or any other meaning gate exists anywhere in the codebase. The Groq prompt says \"maintain meaning\" as a vague instruction to the LLM, b",
  "fact_preservation": "none. The local synonym pass does blind case-insensitive replacement over the entire text and would corrupt numbers that happen to contain m",
  "mechanism": "Two-path rewrite. Primary path (app/Services/GroqService.php): calls Groq API with Llama-3.3-70b-versatile. The prompt instructs the model to \"completely rewrite this text as if YOU personally wrote it from scratch\" with explicit rules: start sentences with \"I think\"/\"Honestly\"/\"Look,\", use contractions everywhere, vary sentence lengths, add 1-2 in"
 },
 {
  "name": "a6828464/yimai-humanizer-v4",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — SKILL.md explicitly states \"目标是编辑质量，不是欺骗检测器\" (goal is editing quality, not deceiving detectors). There is no score-rewrite-rescore loop, no external detector call, and no AI-probability threshold",
  "meaning_verification": "Prompt-only checklist (L0–L4 in references/eval.md). L0 (highest priority) forbids adding or altering any figure, date, location, case, quote, feature, or sensory detail not present in the original. L",
  "fact_preservation": "Explicit prohibition on fabrication: numbers, cases, locations, experiences, quotations, features, and user feedback must not be invented. T",
  "mechanism": "A Hermes Agent skill file (SKILL.md) — not executable code. The transformation is done entirely by the host LLM following structured natural-language instructions. Three modes: detect (list AI-tell evidence only, no edits), light-edit (default: minimal deletions/substitutions using \"minimal effective modification\" — prefer deleting one filler word "
 },
 {
  "name": "Kloue/Ai-Human-Typer",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — there is no detector, no scoring, no rewrite step, and no iteration of any kind. The tool simply types the user-supplied text and stops.",
  "meaning_verification": "none — not applicable; text content is never modified.",
  "fact_preservation": "none — not applicable; text content is never modified.",
  "mechanism": "Keystroke simulation, not text transformation. The Chrome extension (`extension/content/content-typer.js`, `DirectTyper` class) takes the user's text unchanged and replays it character-by-character into the active DOM element (textarea, input, or contenteditable). `calculateDelay()` randomizes inter-key timing around a configurable WPM (default 60-"
 },
 {
  "name": "marcoaiwithfefe-hub/humanizer-hk",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no feedback loop, and no iteration. The workflow is: paste text → LLM rewrites once → done. No detector is consulted at any stage.",
  "meaning_verification": "none mechanically. The prompt instructs the model to \"preserve facts, stance, and information density\" and explicitly forbids adding new facts or escalating cautious judgments to absolute ones, but th",
  "fact_preservation": "none automated. The rules say not to invent facts and not to change the author's epistemic strength, but there is no byte-exact locking of n",
  "mechanism": "Pure prompt engineering — no executable code exists at all. The repo ships three Markdown files: SKILL.md (a Claude Code skill loaded as system instructions), paste-prompt.md (a standalone copy-paste prompt for any LLM), and patterns.md (a 13-category catalogue of AI tells in Chinese text). When invoked, the LLM is told to apply eight core rules (s"
 },
 {
  "name": "lijunyu726/humanizer-zh-plus",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the 50-point self-assessment (across directness, rhythm, reader trust, authenticity, conciseness) is the LLM grading its own output, not a separate AI-text detector scoring and triggering re-writ",
  "meaning_verification": "none — the no-fabrication rule is a prompt-level constraint (\"never add facts, names, numbers, dates, or citations absent from the original\"), not a cosine-similarity check, NLI entailment gate, or an",
  "fact_preservation": "Instruction-level only: the SKILL.md includes a no-fabrication rule prohibiting the LLM from inventing numbers, names, dates, or citations. ",
  "mechanism": "Pure Markdown instruction set (no executable code). The repository contains two files: README.md and SKILL.md. When invoked as a Claude Agent Skill via `/humanizer-zh-plus`, the LLM (Claude) reads the 32-pattern catalogue and applies the rules directly to the input text in a single pass. Pattern catalogue is split across six categories: content (6 "
 },
 {
  "name": "dake2482/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. The 50-point quality rubric is a manual post-hoc checklist that the same agent fills out after rewriting; it is not wired to any classifier and does not trigger",
  "meaning_verification": "none — no cosine similarity, NLI, semantic-role matching, or any automated meaning gate. The rubric includes a \"conciseness\" and \"authenticity\" dimension but these are subjective judgements made by th",
  "fact_preservation": "none — no byte-locking, no numeral retention check, no citation protection of any kind.",
  "mechanism": "A SKILL.md prompt file — a curated catalogue of 39 Chinese AI-writing tells (15 Chinese-specific, plus content/language/style/communication categories) with rewrite instructions and a 50-point scoring rubric. No code executes; the file is loaded into an AI agent (Claude Code via ~/.claude/skills/, Hermes via ~/.hermes/skills/) which then applies th"
 },
 {
  "name": "Moyouchi/humanizer-zh-pro",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "yes — but only nominally. Step 6 re-runs the same six-category scoring heuristic inside the host LLM (not an external detector model) and demands a ≥30% drop. If the drop is not achieved after two cor",
  "meaning_verification": "Partial. The fidelity contract (5 rules) prohibits logic drift, silent deletion, attribution mutation, and numeric-context scrambling. It instructs the LLM to extract logical connectors (因为/所以/但是) fro",
  "fact_preservation": "Yes, by prompt rule: numbers must stay bound to the objects they modify; proper nouns are untouched; quoted text boundaries are locked; no i",
  "mechanism": "The repo is a single SKILL.md prompt file (300+ lines) loaded into AI agent platforms (Claude Code, OpenCode, Trae, Codex, Coze). It instructs the host LLM to follow a seven-step pipeline: (0) input validation; (1) six-category AI-flavor diagnostic scan with a weighted scoring formula (per-100-word hit rate x weight x 20 = 0-100 score); (2) fidelit"
 },
 {
  "name": "QuintinQiu2003/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector at any stage. The skill is a single-pass rewrite prompt. There is no scoring before rewriting, no re-scoring after, and no iteration loop of any kind.",
  "meaning_verification": "none — the skill instructs Claude to \"保留含義\" (preserve meaning) as a prose instruction, but no semantic similarity score, NLI check, cosine similarity gate, or any other automated check is implemented ",
  "fact_preservation": "none — numbers, citations, and quotes receive no special handling. The skill treats \"模糊歸因\" (vague attribution) as a pattern to fix (replace ",
  "mechanism": "A Claude Code skill (SKILL.md + README.md only — no executable code whatsoever). When the user invokes /humanizer-zh inside Claude Code, Claude reads the SKILL.md prompt and applies it to the supplied text in a single LLM pass. The prompt enumerates 24 named AI-writing patterns (e.g. \"過度強調意義\" — over-emphasis on significance; \"AI 詞彙\" — AI vocabulary"
 },
 {
  "name": "1430327769/Humanizer-zh",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — no code exists",
  "meaning_verification": "none — no code exists",
  "fact_preservation": "none — no code exists",
  "mechanism": "The repository is an empty shell. It was created on 2026-07-21 and has never received a commit. No files, no README, no source code, no description, and no topics are present. There is nothing to analyze mechanically."
 },
 {
  "name": "syw2039/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no score, and no iteration. The skill performs a single-pass rewrite; there is no feedback loop of any kind.",
  "meaning_verification": "Prompt-instruction only. Step 2 of the workflow (\"建立事实锁 / establish fact lock\") asks the model to record names, numbers, proportions, prices, dates, citations, causal relations, and negations, then st",
  "fact_preservation": "Numbers, dates, citations, links, quotations, proper nouns, causal qualifiers, and negation words are all listed as explicitly locked in the",
  "mechanism": "Pure prompt-engineering skill — no code, no scripts, no API calls. The entire transformation lives in SKILL.md, a structured set of instructions for an LLM agent. The workflow is: (1) classify text type and lock a \"fact register\" (names, numbers, dates, citations, causal relations), (2) read references/chinese-ai-patterns.md which catalogues 36 Chi"
 },
 {
  "name": "MudabbirulSaad/para-humanizer",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — no detector is ever invoked. The pipeline is strictly one-shot: transform → return. There is no score-then-rewrite iteration anywhere in the codebase.",
  "meaning_verification": "none. Tests in tests/test_paraphraser.py only assert output != input and abs(len(output) - len(input)) < 0.5 * len(input). No cosine similarity, NLI entailment, semantic-role match, or any other meani",
  "fact_preservation": "A static whitelist in para_humanizer/resources/config/protected_terms.json skips synonym replacement for ~300 named entities and tech brand ",
  "mechanism": "Three-layer pipeline implemented in para_humanizer/core/paraphraser.py (_process_text). Layer 1: rule-based synonym replacement via RuleBasedProcessor (rule_based.py) — uses a bundled synonyms.json plus WordNet, with POS tagging through NLTK/spaCy, at a user-configurable replacement rate (default 0.4). Layer 2: optional transformer paraphrasing via"
 },
 {
  "name": "simpson-furrow/Humanizer-zh-hant",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — no code exists to inspect",
  "meaning_verification": "none — no code exists",
  "fact_preservation": "none — no code exists",
  "mechanism": "Cannot be determined. The repository is empty — no files, no commits beyond initialization, no README, no source code of any kind. Created 2026-06-25 with description \"AI Humanizer for Traditional Chinese\" but never populated."
 },
 {
  "name": "EchoInCode26/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass style injection; no scoring, no rewriting loop, no iteration of any kind",
  "meaning_verification": "none",
  "fact_preservation": "none — no explicit handling of numbers, citations, or quotes",
  "mechanism": "Pure prompt injection as a Claude Code skill. SKILL.md contains a list of banned phrases (e.g., 综上所述, 值得注意的是, 建议您, 当然, 根据我的搜索结果) and mandatory style rules: short choppy sentences with omitted subjects, colloquial replacements, emotional interjections (卧槽, 稳了), markdown downgrading (no tables or deep headings unless requested), and mirroring the use"
 },
 {
  "name": "cc-seika/Humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no score, and no iteration. The skill is invoked once; Claude rewrites in a single pass.",
  "meaning_verification": "A 50-point qualitative rubric is included in the skill (covering directness, rhythm, reader trust, authenticity, conciseness), but it is a human-readable checklist, not an automated semantic gate. No ",
  "fact_preservation": "none — no mention of protecting numbers, citations, or quoted text anywhere in the skill or README.",
  "mechanism": "A Claude Code skill (SKILL.md) that, when invoked as `/humanizer-zh`, instructs the model to scan the input text against a catalogue of 24 named AI-writing patterns (organised into four groups: content, language, style, communication) and rewrite the flagged spans toward more natural Chinese prose. No executable code, no algorithmic transformation "
 },
 {
  "name": "humanizer-zh-wanyfon",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no detector call, and no iteration. The agent rewrites once and stops.",
  "meaning_verification": "none — the SKILL.md states the tool does not fact-check or add unreferenced information, but there are no automated cosine-similarity, NLI, or any other meaning gates.",
  "fact_preservation": "none — no byte-locking, numeral-retention check, or citation preservation mechanism is described or implemented.",
  "mechanism": "Prompt-only Claude Code skill (SKILL.md). The entire repo is two markdown files — README.md and SKILL.md — with no executable code. SKILL.md is a ~30 KB instruction document that an LLM agent follows: it carries a catalogue of roughly 40 named Chinese AI-writing patterns (content patterns such as \"overstated significance,\" language patterns such as"
 },
 {
  "name": "ksufer/humanizer-zh-skill",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — no detector is invoked at any point. The only evaluation is a self-assessment rubric the LLM fills in internally (6 dimensions × 10 points each). No external scoring, no iteration based on a dete",
  "meaning_verification": "none mechanically enforced. The skill asks the model to (a) compile a \"core fact list\" of numbers, names, dates, and quotes before rewriting and (b) check the list afterwards, but this is a prompt ins",
  "fact_preservation": "Partial and rule-based by prompt: three explicit \"red lines\" — (1) quoted speech must be reproduced verbatim or converted to indirect speech",
  "mechanism": "Pure LLM agent skill — no code, no scripts, no model calls of its own. The repo contains SKILL.md (a structured system-prompt persona) that instructs a host model (Claude Code, OpenCode, etc.) to act as a senior Chinese editor. The rewrite pipeline is entirely prompt-driven: (0) detect one of four text registers (blog/news/official-document/essay),"
 },
 {
  "name": "humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration; the skill fires once and returns a rewrite",
  "meaning_verification": "none automated; discipline-guardrails.md instructs the LLM to verify that rewrites did not alter factual meaning, claim strength, citations, formulas, or technical terms, but this is a prompt instruct",
  "fact_preservation": "described as a rule in discipline-guardrails.md (citations, formulas, dataset/model names, and domain terminology must remain unchanged; no ",
  "mechanism": "Pure prompt/instruction set with no executable code. Three markdown files define the transformation: SKILL.md contains the rewrite workflow and priorities; references/academic-patterns.md catalogs seven Chinese AI-tell patterns (inverted argument structure, connector overuse like \"对于……来说\", vague power words like \"赋能\", numbered-list templates, infla"
 },
 {
  "name": "humanizer-zh-tw-pro",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — Step 3 is lexical pattern-matching (banned word scan, simplified-character scan, AI-phrase scan), not a trained detector. There is no score→rewrite→re-score loop.",
  "meaning_verification": "none mechanically. The \"dual-lens check\" (Step 4) asks the LLM to self-evaluate readability and business consistency, but there is no cosine similarity gate, NLI model, or any numerical threshold.",
  "fact_preservation": "partial and informal. The banned-word list includes \"虛構數字 / 假數字\" (fabricated numbers) as a prohibited output, but there is no byte-exact loc",
  "mechanism": "A Claude Code skill defined entirely in SKILL.md (no executable code, language=null). The 4-step pipeline is: (1) identify AI-flavor patterns via a hardcoded list of ~10 banned sentence structures; (2) inject a rewriting prompt into the underlying LLM instructing it to shorten sentences, inject Taiwanese oral particles (啊/喔/耶/嘛/欸), and use Traditio"
 },
 {
  "name": "xiaoluo-creative/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the 50-point rubric it ships (5 dimensions × 10 points: directness, rhythm variation, reader trust, authenticity, conciseness) is a manual human-readable checklist, not a call to any AI-text dete",
  "meaning_verification": "none — the only check is \"ensure natural sound when read aloud,\" which is a subjective, manual step, not an automated semantic gate.",
  "fact_preservation": "none — no mention of protecting numerals, citations, or quoted strings.",
  "mechanism": "A Claude Code skill (SKILL.md) that instructs Claude to identify and rewrite 24 catalogued AI writing patterns across four categories: content patterns (over-emphasis on significance, vague attribution, shallow -ing analysis, etc.), language patterns (AI vocabulary overuse, negation parallelism, rule-of-three forcing, synonym cycling), style patter"
 },
 {
  "name": "slivenred/humanizer-zh-TW-Pro",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration. The skill is a one-shot editorial prompt. validate_repo.py and validate_forward_cases.py check SKILL.md structure and JSON schema of the t",
  "meaning_verification": "none — the test cases carry human-authored must_preserve / must_avoid / success_checks fields, but validate_forward_cases.py only checks that those fields are non-empty strings with correct schema; it",
  "fact_preservation": "Rules in SKILL.md instruct the LLM to keep prices, dates, URLs, sources, limitations, code blocks, tables, and SEO keywords unchanged. This ",
  "mechanism": "A SKILL.md prompt document (not executable code) that instructs Claude Code, Codex, or OpenCode agents on how to rewrite Traditional Chinese text. The agent reads the skill at invocation time; the skill lists 33 labelled AI-tell patterns (inflated significance, vague attribution, triad padding, chatbot residue, Taiwan-specific mainland-Chinese calq"
 },
 {
  "name": "Humanizer-zh (Aether-Lumine/Humanizer-zh)",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite only; there is no scoring step, no re-scoring after rewrite, and no iteration of any kind",
  "meaning_verification": "none — the SKILL.md states \"保留含义\" (preserve meaning) as a guiding principle but provides no automated check; no cosine similarity, NLI, semantic-role, or any other gate is implemented",
  "fact_preservation": "none — numbers, citations, and quotes receive no special treatment; no byte-exact locking, no numeral-retention check",
  "mechanism": "A Claude Code skill packaged entirely in skills/humanizer-zh/SKILL.md. No executable code exists in the repo. The SKILL.md instructs the Claude Code model to scan input text for 24 named AI-writing patterns (grouped into four categories: content patterns, language/grammar patterns, style patterns, and communication/filler patterns) and rewrite offe"
 },
 {
  "name": "whjstc/skills (humanizer-zh)",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite only; no scoring step, no re-score, no iteration",
  "meaning_verification": "none automated. SKILL.md verbally instructs the LLM to preserve meaning and information density, and the two-pass process asks the LLM to self-ask \"where does this still sound like AI?\" but there are ",
  "fact_preservation": "none automated. The skill instructs the LLM to prefer concrete numbers, named sources, and specific details over abstractions, and to retain",
  "mechanism": "A SKILL.md system-prompt that instructs an LLM (Claude Code, OpenCode, or Codex) to act as a Chinese text editor and apply a 39-pattern AI-tell catalogue. No code executes — the LLM is the engine. The skill defines detection patterns and rewrite rules for Chinese-specific AI tells (宣传腔 promotional tone, 意义拔高 meaning inflation, 纠正式对照 corrective cont"
 },
 {
  "name": "JLWY2233/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — no detector is invoked at any stage; there is no scoring, re-scoring, or iteration loop of any kind",
  "meaning_verification": "none (automated). The instruction files instruct the LLM to preserve original facts, technical terms, citations, numbering, paragraph order, and causal relations, but this is a prompt constraint, not ",
  "fact_preservation": "Instruction-level only: SKILL.md and rewrite-rules.md both state hard constraints against modifying technical terms, code identifiers, API p",
  "mechanism": "A Codex skill (three instruction files: SKILL.md, references/rewrite-rules.md, agents/openai.yaml) that drives an OpenAI model through a two-round prompt-guided rewrite pipeline. Round 1 (SKILL.md) normalises Chinese vocabulary and syntax toward a natural expert-explainer register: specific word substitution tables (e.g. 管理→开展管理工作, 交互→进行交互), adding"
 },
 {
  "name": "songxrui/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the only automated check is a PowerShell grep for a static banned-word list; there is no score-then-rewrite-then-rescore iteration and no local or remote detector is queried",
  "meaning_verification": "none automated — the Step 5 checklist includes a word-count deviation check (±20%) and a manual re-read, but no cosine similarity, NLI entailment, semantic-role matching, or any programmatic meaning g",
  "fact_preservation": "none — SKILL.md instructs Claude not to fabricate details (\"不编造细节\"), but there is no byte-exact locking of numbers, citations, or quotes; no",
  "mechanism": "A Claude Code skill (SKILL.md) that instructs Claude to identify and rewrite Chinese AI-tell patterns. No executable code. The skill defines a 5-step manual workflow: (1) identify target platform (WeChat public account, Xiaohongshu, Douyin), (2) run a 5-dimensional diagnostic rubric scoring sentence structure/vocabulary/structure/emotion/specificit"
 },
 {
  "name": "jiachengyu80/my-humanizer-zh",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — repo is empty, no implementation present",
  "meaning_verification": "none — no code exists to examine",
  "fact_preservation": "none — no code exists",
  "mechanism": "The repository exists on GitHub but is completely empty — no files, no README, no source code, no commits beyond the initial creation. Nothing can be determined about its mechanism."
 },
 {
  "name": "liuxuheng-1/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, scorer, or iteration loop of any kind. Single-pass prompt rewrite only.",
  "meaning_verification": "none — the README states \"保留核心含义\" (preserve core meaning) as a human principle, but there is no automated gate, similarity check, NLI test, or any other programmatic verification.",
  "fact_preservation": "none — no numeral retention, no citation locking, no quote preservation. Numbers and citations are rewritten at the LLM's discretion.",
  "mechanism": "Pure prompt guide installed as a Claude Code skill. The repo contains only README.md and SKILL.md (plus .gitignore and LICENSE — no executable code at all). SKILL.md is a Chinese-language markdown file that instructs Claude to scan text for 24 labelled AI-writing patterns across four categories (content, language/grammar, style, communication) and "
 },
 {
  "name": "tianpeng-dev/Humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no. The \"second audit\" in step 5 of the workflow is the same LLM self-reviewing its own draft (it literally asks itself \"What obvious AI patterns remain?\"), not an external detector scoring the output",
  "meaning_verification": "none. No cosine similarity, NLI entailment, semantic-role match, or any automated semantic check. The self-critique pass is a style audit, not a meaning audit.",
  "fact_preservation": "none. No byte-exact locking, no numeral retention check, no citation preservation rule. The Chinese punctuation preservation rule (。，、《》 vs ",
  "mechanism": "Pure prompt-guide (zero executable code). The repo is a Claude Code \"Agent Skill\" — a set of Markdown files (SKILL.md + eight references/ files) that instruct an LLM agent how to identify and rewrite Chinese AI writing patterns. SKILL.md defines a two-pass workflow: (1) classify patterns, load only the relevant references/ file, fix P0→P1→P2 severi"
 },
 {
  "name": "lulu1373/shuorenhua-humanizer-zh",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — no code exists to inspect",
  "meaning_verification": "none — no code exists",
  "fact_preservation": "none — no code exists",
  "mechanism": "The repository exists on GitHub but is completely empty — no commits, no files, no README, no source code of any kind. It was created on 2026-04-30 and nothing has ever been pushed to it. There is nothing to profile."
 },
 {
  "name": "script-humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector call, no score, and no iteration. Single-pass rewrite only.",
  "meaning_verification": "none — SKILL.md instructs the LLM to \"protect the core point before editing\" and prohibits inventing facts, but this is a prompt instruction, not a mechanized check. No cosine similarity, NLI, or any ",
  "fact_preservation": "Light prompt-level only: SKILL.md says \"do not invent facts, examples, data, personal experience, prices, results, or claims\" and \"mark unce",
  "mechanism": "A Claude/Codex installable skill (structured system prompt, no executable code). SKILL.md instructs the host LLM to scan the draft for six named Chinese AI-tone patterns — speaking for the reader (很多人觉得...), correction framing (不是X而是Y), teacher-like self Q&A, lowering the reader (普通人真正该...), report-style analysis, and generic evaluation without a s"
 },
 {
  "name": "zhangjinglsc-alt/a5l-skill-humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no score, and no iteration. The workflow is strictly one-shot: scan → rewrite → deliver. The SKILL.md five-step workflow (Pattern Identification, Fragment Rewriting, Meaning",
  "meaning_verification": "none mechanically. \"Meaning Preservation\" is listed as step 3 of the workflow but is implemented only as a natural-language instruction to the LLM. There is no cosine similarity gate, no NLI entailmen",
  "fact_preservation": "none. The skill mentions maintaining \"core message integrity\" as a general instruction but there is no byte-exact locking, no numeral-retent",
  "mechanism": "Pure prompt-instruction skill. The entire implementation is SKILL.md — a structured natural-language prompt that instructs an LLM (via the A5L agent framework) to: (1) scan input text for 24 catalogued AI writing patterns (em-dash overuse, \"not only...but also\" parallelism, excessive bolding, chatbot artifacts like \"Hope this helps!\", knowledge-cut"
 },
 {
  "name": "chenboyo/humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, and no iteration. The skill is a single-pass rewrite. No score is computed before or after the rewrite.",
  "meaning_verification": "none — the instructions say \"preserve meaning\" and \"preserve core information\" as a human-readable directive to the LLM, but there is no automated semantic check (no cosine similarity, no NLI entailme",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or quotes. The instructions mention preserving core information as a soft rule, but noth",
  "mechanism": "Pure prompt-guide delivered as a Hermes Agent skill (SKILL.md). No executable code whatsoever — the entire repo is two markdown files and one example. The skill instructs an LLM to scan for 23+ named AI-writing patterns across five categories (content, linguistic, style, communication, filler) and rewrite offending passages. It also includes a \"sou"
 },
 {
  "name": "ddcarolyn/writing-humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite only; no detector is invoked, no score is computed, no iteration loop exists",
  "meaning_verification": "none — the SKILL.md says \"preserve meaning\" as an instruction to the model but there is no automated cosine similarity, NLI entailment, or any other programmatic gate",
  "fact_preservation": "none — no byte-exact locking, no numeral retention check, no citation guard mentioned anywhere",
  "mechanism": "A Claude Code skill consisting of a single SKILL.md prompt file. When invoked via the /humanizer-zh slash command, it instructs Claude to scan Chinese text for 24 named AI-writing patterns across four categories — content (overemphasis on legacy, shallow -ing analyses, vague attribution), language (high-frequency AI vocabulary: 此外/furthermore, 至关重要"
 },
 {
  "name": "humanizer-zh-technical",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no scoring step, no iterative loop, and no detector referenced anywhere in the file",
  "meaning_verification": "none",
  "fact_preservation": "Partial: the skill explicitly instructs preservation of code blocks, structured tables, RFC 2119 compliance language, and appropriate techni",
  "mechanism": "A single SKILL.md file defining a named-pattern catalogue of seven Chinese-language AI tells to detect and eliminate in technical documentation: negation-contrast structures (\"不是...而是...\"), self-referential pronouns (\"本方案\", \"本文\"), bloated causal language, transition padding (\"这意味着\"), trailing negations (\"而不是\"), title comparisons, and verbose conclu"
 },
 {
  "name": "nieyuann99-ship-it/humanize-writing",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "unclear — no source exists to inspect",
  "meaning_verification": "none — no source exists",
  "fact_preservation": "none — no source exists",
  "mechanism": "Cannot be determined. The repository was created on 2026-08-04 and is completely empty — no files, no README, no commits beyond the repo initialisation itself. The GitHub API confirms size=0 and returns 404 on the contents endpoint."
 },
 {
  "name": "xyy54323/BypassAIGC",
  "stars": 0,
  "category": "back-translation",
  "detector_in_loop": "no — PDF reports from PaperPass / SpeedAI / Vipu can be imported and parsed to show the user which segments were flagged, but the rewriter does not receive those scores and does not re-run; the loop i",
  "meaning_verification": "none",
  "fact_preservation": "none — no numeral locking, citation preservation, or hedge protection found in any source file",
  "mechanism": "Two-stage LLM pipeline via an OpenAI-compatible API. Stage 1 (\"polish\"): vocabulary substitution, verb-phrase expansion, improved logical connectors — `ai_service.py::get_default_polish_prompt()`. Stage 2 (\"enhance\"): for English text, the prompt instructs the model to translate to Chinese, optimize the Chinese, then mechanically back-translate to "
 },
 {
  "name": "transiglobal/elatia-humanizer-zh",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the two \"passes\" are both LLM rewrites driven by the same prompt; there is no detector score, no threshold, and no re-score cycle",
  "meaning_verification": "none — the prompt includes the instruction \"preserve meaning\" and \"keep the core message intact,\" but there is no automated gate (no cosine similarity, no NLI, no semantic-role check)",
  "fact_preservation": "none — numbers, citations, and quoted text get no special handling; the prompt says \"use specific details over vague claims\" but that is a r",
  "mechanism": "Pure LLM prompt-guide. The entire \"code\" is SKILL.md — a markdown file with YAML frontmatter that Claude Code or OpenCode reads as a skill definition. No executable code runs. The LLM is instructed to apply a 29-pattern catalogue of AI-writing tells (drawn from Wikipedia's \"Signs of AI writing\" / WikiProject AI Cleanup) in a two-pass process: (1) i"
 },
 {
  "name": "rAIn0x7/shuorenhua",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — check.py is a standalone scorer separate from the LLM rewriting skill; the user must manually run the checker and re-prompt. No automated score→rewrite→rescore→repeat cycle exists.",
  "meaning_verification": "none — check.py only counts regex pattern occurrences; no cosine similarity, no NLI entailment, no semantic-role matching, no meaning gate of any kind. SKILL.md includes a human self-check heuristic (",
  "fact_preservation": "none — no byte-exact locking, no numeral retention check, no citation preservation. Numbers and quoted material are not specially handled by",
  "mechanism": "Two-component tool for Chinese-language AI-flavor removal. (1) SKILL.md is a writing instruction set (prompt guide) that instructs any LLM (Claude, ChatGPT, Kimi, etc.) to avoid specific Chinese AI-flavor patterns: dash overuse (——), \"不是X而是Y\" (not-X-but-Y) symmetric constructions, \"首先…其次…最后\" enumeration, platform jargon (宝子们, 姐妹们冲), marketing emoji"
 },
 {
  "name": "ai-skill-uploader-2026/ai-humanizer-zh",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — there is no detector anywhere in the codebase; the pipeline is input → API call → poll → output, single pass with no scoring or iteration",
  "meaning_verification": "none",
  "fact_preservation": "none automated; the brandRequirements parameter lets the user manually list items to preserve, but the API does whatever it wants with them ",
  "mechanism": "The repo is a one-day auto-generated catalog entry for the ai-skills.ai commercial platform. run.py uses urllib to POST the user's text to https://ai-skills.ai with an API key, then polls up to 180 times (2 s intervals) for a completed/failed status — all rewriting logic is in the closed backend. The skill.json explicitly cites op7418/humanizer-zh "
 },
 {
  "name": "zh-humanizer-best-practice",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the repo explicitly rejects putting a detector in the loop. audit_ai_flavor.py is described as a \"lightweight hint\" that \"does not prove writing is human-written\" and must not be used as a rewrit",
  "meaning_verification": "none automated. SKILL.md Step 7 lists a manual self-check the LLM is instructed to perform before delivering: (1) fact fidelity — did numbers, subjects, sources, conclusions drift; (2) register consis",
  "fact_preservation": "Instructional only, not technical. references/protected-spans.md is a detailed markdown specification telling the LLM what categories to tre",
  "mechanism": "Pure prompt/instruction package for LLM agents. No text transformation code runs outside the LLM. The skill consists of a structured SKILL.md (step-by-step editorial workflow for the LLM) plus six reference markdown files: patterns.md (Chinese AI-tell catalogue), protected-spans.md (what not to change), scene-playbooks.md (per-genre strategies), ou"
 },
 {
  "name": "aken1023/academic-humanizer-web",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no — detect_ai_patterns() is called before and after the single rewrite pass to show before/after scores, but there is no iterative loop: it rewrites once and stops regardless of the resulting score",
  "meaning_verification": "none",
  "fact_preservation": "none — numbers and citations pass through unchanged only because they are not keys in the replacement dictionary, not due to any explicit pr",
  "mechanism": "Pure dictionary-based synonym substitution for Traditional Chinese text. The core is a hardcoded REPLACEMENTS dict (~60 entries) mapping common AI-sounding academic transition phrases (此外, 然而, 因此, 綜上所述, etc.) to lists of more colloquial alternatives. `humanize_text()` in app.py iterates through the dict and calls `str.replace()` with a randomly cho"
 },
 {
  "name": "Awzs/linggan-ai-thesis-rewrite",
  "stars": 0,
  "category": "other",
  "detector_in_loop": "no — no code of any kind; the commercial service behind it may use a detector loop but nothing is disclosed or open-sourced here",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "No transformation mechanism exists in this repository. The repo contains zero source code (GitHub reports language: null, size: 28 KB). It is a pure SEO/GEO marketing documentation kit for a closed-source commercial Chinese SaaS called 零感AI (Linggan AI, linggantext.com). Contents are entirely markdown files: keyword matrices, route indexes, FAQ, pl"
 },
 {
  "name": "fantay0312/BypassAIGCdocker",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — the service does not call any detector at any point. Evasion is entirely achieved by prompting the LLM to mimic human writing styles; there is no score-rewrite-rescore loop anywhere in the codeba",
  "meaning_verification": "none — no cosine similarity, no NLI entailment, no semantic-role check, no hedge/numeral retention test. The only quality bookkeeping is storing a before/after text diff in the database for human audi",
  "fact_preservation": "none — no number-locking, no citation preservation, no byte-exact protect list. Numbers and citations pass through the LLM rewrite unchanged",
  "mechanism": "Two-stage LLM prompt pipeline, entirely via external API calls (OpenAI-compatible endpoints; Gemini 2.5 Pro recommended). Stage 1 (\"polish\", `ai_service.py` `polish_text`): academic prose expansion — verb phrases padded out, parentheticals integrated, formal vocabulary substituted. Stage 2 (\"enhance\", `ai_service.py` `enhance_text`): a \"style mimic"
 },
 {
  "name": "Ran350/humanizer-ja",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector of any kind. The skill performs one rewrite pass guided by prompt rules and stops. No score, no re-score, no iteration.",
  "meaning_verification": "none — the SKILL.md states \"never alter facts, figures, URLs, or quoted material\" as a prompt-level instruction, but there is no mechanical check (no cosine similarity, no NLI, no semantic-role compar",
  "fact_preservation": "Prompt-level instruction only: \"never alter facts, figures, URLs, or quoted material\" and \"preserving facts, numerical values, proper nouns,",
  "mechanism": "A Claude Code skill (SKILL.md prompt file) containing a taxonomy of 23 Japanese-language \"AI-tell\" patterns across 7 categories (boilerplate openings/closings, padding/overstatement, redundancy, vagueness, vocabulary/tone, formatting, argumentation). When installed as a Claude Code plugin, the LLM reads the SKILL.md instruction set and performs a s"
 },
 {
  "name": "kupuuu/BypassAIGC-main",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite only; no scoring step, no re-score, no iteration of any kind",
  "meaning_verification": "none programmatically; the system prompt instructs the LLM to preserve \"core logic, causal relationships, and functional description\" and keep word count within ±30 characters of the original, but no ",
  "fact_preservation": "Technical terms, proper nouns, code snippets, library names, config keys, and API paths are listed as protected by prompt instruction (e.g. ",
  "mechanism": "Single-pass LLM rewrite driven by a large bilingual \"Style Mimicry Expert\" system prompt. Text is split into ~500-character segments (split_text_into_segments in package/backend/app/services/ai_service.py), then each segment is sent to any OpenAI-compatible /chat/completions endpoint with the enhance prompt as the system message. Strategy A (Chines"
 },
 {
  "name": "kgraph57/humanizer-ja",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the optional AI-smell score (0–100) is generated by the LLM itself from the same prompt; no external detector is called, and there is no re-score-then-rewrite loop of any kind.",
  "meaning_verification": "none — SKILL.md contains a 厳守ルール (strict rules) section instructing the model not to alter facts or add information not present in the source, but no automated semantic check (cosine similarity, NLI, ",
  "fact_preservation": "none — prompt instructions say not to introduce new numbers or proper nouns, but there is no byte-exact preserve-locking of citations or num",
  "mechanism": "Single-pass LLM rewrite driven entirely by a prompt-instruction file (SKILL.md, 12 KB). There is no executable code. The SKILL.md defines a 4-step workflow: (1) receive text (direct paste, file path via Read, or URL via WebFetch), (2) diagnose it against 6 categories and 30 named patterns of Japanese AI-tell, (3) rewrite the whole text in one pass "
 },
 {
  "name": "bluesky-firmament/humanizer-ja",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the \"Final AI Check\" step is a self-interrogation prompt to the rewriting LLM, not a call to any detector model or scoring function. There is no external detector, no score, and no iteration cond",
  "meaning_verification": "none in the automated sense. The skill instructs contextual judgment: \"判断基準は『この表現が文脈上自然かどうか』\" (the criterion is whether the expression feels contextually natural). No cosine similarity, NLI entailment",
  "fact_preservation": "none for numbers. For vague attributions (\"専門家によると\"), the skill instructs conversion to specific sources — but this risks LLM hallucination ",
  "mechanism": "Pure LLM prompt/skill document — no executable code. The repo contains README.md, SKILL.md, and LICENSE only. SKILL.md is a natural-language instruction set for an LLM (Claude, Gemini, etc.) to follow a six-step pipeline: (1) register detection (academic/business/essay/technical/casual/fiction), (2) pattern scanning against 26 documented patterns a"
 },
 {
  "name": "tmknzz/Humanizer-ja",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no score, and no iteration. A single LLM pass rewrites the text and optionally outputs a 5-dimension quality score (direct, rhythm, trust, authenticity, polish) as a self-as",
  "meaning_verification": "none — the prompt instructs Claude to \"preserve core information\" but no automated check (cosine similarity, NLI, semantic-role match, etc.) is applied. Meaning preservation is entirely at the LLM's d",
  "fact_preservation": "none — no byte-exact locking of numbers or citations. The rewrite examples actually encourage adding specific numbers where the original was",
  "mechanism": "Pure LLM-instruction rewriter. The entire tool is a single SKILL.md prompt file. It instructs Claude to (1) scan input text against a catalogue of 20 AI-tell patterns, (2) rewrite flagged passages in-place, (3) apply a caller-specified tone (plain/blog/business/academic). No code executes. The pattern catalogue and rewrite examples live entirely in"
 },
 {
  "name": "harupan119/claude-skills-ja (humanizer-ja-pro)",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — The ai-score-rubric.md defines a weighted tell-density score (strong weight 3 / medium weight 2 / weak weight 1, aggregated per 1000 characters of body text), and the main skill calls for a self-",
  "meaning_verification": "none automated — The skill explicitly forbids \"just deleting\" connectives or transitions without replacement (e.g., bare-deleting a conjunction must not leave a dangling sentence break), and the proce",
  "fact_preservation": "none — The skill encourages replacing vague authority references (\"according to experts\") with concrete numbers and proper nouns, and it not",
  "mechanism": "Pure prompt-guide skill invoked inside Claude Code. The repo contains no executable transformation code — only Markdown instruction files that Claude reads and follows. The skill implements a 3-layer detection + rewrite pipeline entirely through natural-language rules: Layer 1 catches symbol/format residue (full-width dashes, Markdown asterisks, em"
 },
 {
  "name": "TJ-kr/humanize-helper",
  "stars": 0,
  "category": "api-wrapper",
  "detector_in_loop": "no — no evidence of any detection scoring step; the binary makes a single Claude API call and returns the result",
  "meaning_verification": "none",
  "fact_preservation": "none",
  "mechanism": "Ships precompiled binaries (Windows, macOS, Linux) that invoke the Claude CLI with the `-p` (pipe/prompt) flag — confirmed by the release note \"claude -p 단일 바이너리\" (\"claude -p single binary\"). The binary forwards Korean AI-generated text to Claude with a humanization instruction so that the style becomes natural-sounding while the content stays the "
 },
 {
  "name": "npced16/im-not-ai-web",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "yes, but only in the Claude Code strict-mode pipeline, and only with the internal taxonomy-based detector (ai-tell-detector agent), not an external statistical detector like GPTZero or Originality.ai.",
  "meaning_verification": "Partial. The four commandments encoded in the skill state \"수치·주장·고유명사·직접 인용 100% 원문 보존\" (numbers/claims/proper nouns/direct quotes preserved verbatim). In strict mode the content-fidelity-auditor agen",
  "fact_preservation": "Stated as a hard rule in the skill spec (수치·고유명사·직접 인용 100% 원문 보존), enforced by the content-fidelity-auditor agent in strict mode. The web a",
  "mechanism": "Two delivery surfaces, one shared knowledge base. (1) Browser web app (web/app.js + api/humanize.js, Vercel-deployed): user pastes Korean text, selects provider (OpenAI/Gemini/Claude), supplies their own API key, and submits. api/humanize.js splits input into ≤4 000-char paragraph-boundary chunks (splitIntoChunks), sends each to the chosen LLM with"
 },
 {
  "name": "ptec07/humanize-korean",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — SKILL.md explicitly states \"It is an editing skill, not a detector-evasion tool\" and the README repeats \"Do not claim the output defeats AI detectors.\" There is no scoring step, no re-score after",
  "meaning_verification": "Prompt-level self-audit only. SKILL.md has a checklist the LLM must run before finalising: facts/numbers/dates/names/URLs/quotes preserved, genre/register unchanged, no new claims added. It also enfor",
  "fact_preservation": "Explicit and detailed. Protected spans are marked before any edit: direct quotes, numbers, dates, units, URLs, code blocks, law articles, pr",
  "mechanism": "Pure Hermes Agent skill package — no executable code whatsoever. The entire repo is SKILL.md (the prompt entrypoint, 12 KB) plus seven reference Markdown files. Transformation is done by the LLM at inference time, guided by: (1) a tiered AI-tell taxonomy covering 10 Korean-specific pattern classes (translationese particles, English pronoun literal "
 },
 {
  "name": "diuzhev26-glitch/humanizer-ru",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the skill contains no scoring step; it is a single-pass rewrite driven by a static rule catalogue. There is no re-score or iterate cycle.",
  "meaning_verification": "Minimal: SKILL.md includes a pre-delivery checklist (naturalness, sentence variety, specificity, meaning preservation, voice presence) that is itself just a prompt instruction to Claude. No cosine sim",
  "fact_preservation": "none — no byte-exact locking of numbers, citations, or quotes is described or implemented.",
  "mechanism": "A Claude Code skill file (SKILL.md) encoding 21 named anti-patterns for Russian AI-generated text. At invocation Claude reads the skill and applies rule-based substitutions: nominalized verbs → active verbs, genitive chains → simpler constructions, passive → active voice, removal of flagged lexical items (\"ключевой\", \"демонстрирует\", \"способствует\""
 },
 {
  "name": "ptec07/new-obsdn-skill",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring, and no iteration. The rules are applied once by the LLM following the prompt instructions in korean-humanize.md. The word \"detector\" does not appear anywhere in ",
  "meaning_verification": "Prompt-only self-check: korean-humanize.md ends with a four-question checklist the LLM is supposed to answer mentally (\"Protected spans unchanged? Any new claim added? Any number/date/name/link change",
  "fact_preservation": "Prompt instruction only: \"Meaning, facts, numbers, dates, units, names, terms, and causality must stay the same.\" and \"Preserve frontmatter ",
  "mechanism": "This is a Hermes Agent skill — a structured set of Markdown instruction files that tell an LLM how to behave, not executable code. The humanization component lives entirely in references/korean-humanize.md, which is a rule table + prose prompt. When invoked, the agent reads the rules and applies them in a single LLM pass. The rules cover: (1) a 10-"
 },
 {
  "name": "Yoon-robin/claude-humanizer",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite only; no detector is called before or after, no score drives iteration",
  "meaning_verification": "none automated — SKILL.md instructs Claude to hold meaning, not drop facts, not invent specifics, and run a mental read-aloud test before delivering, but all of this is advisory text inside the prompt",
  "fact_preservation": "Instruction-level only: SKILL.md defines \"Protected spans\" — proper nouns, numbers, prices, dates, URLs, quoted text, legal copy, feature ke",
  "mechanism": "Pure Claude Agent Skill — a set of markdown files installed into Claude Code's skills directory. The implementation lives entirely in SKILL.md (universal workflow, 5 tell families, protected-span rules, guardrails) plus per-language catalogs in references/languages/korean.md and references/languages/english.md. At runtime, Claude detects the target"
 },
 {
  "name": "iljung1106/korean-style-humanizer-capstone",
  "stars": 0,
  "category": "fine-tuned-model",
  "detector_in_loop": "yes — but only during training, not at inference. A translationese SVM classifier and a style-metric reward function act as training-time signal in the GRPO stages. At inference the trained model rewr",
  "meaning_verification": "none — explicitly confirmed in gui_style_scoring.py: no semantic similarity, cosine distance, NLI, or any other meaning gate exists.",
  "fact_preservation": "none — masking.py confirms no special handling for numerals, citations, or quoted strings; all tokens are treated uniformly.",
  "mechanism": "Three-stage LoRA fine-tuning pipeline on a Korean LLM (Gemma-based, loaded via pipeline_v2/lib/gemma4_loader.py): Stage 1 is supervised fine-tuning on AI-text→human-novel pairs (stage02_format_task_sft.py); Stage 2 is SimPO/CPO preference training favouring human originals over AI alternatives (stage03_simpo_curriculum.py / stage08b_simpo_cpo_from_"
 },
 {
  "name": "corca-ai/Chimaera",
  "stars": 0,
  "category": "rule-based-rewriter",
  "detector_in_loop": "no. There is no AI-text detector anywhere in the pipeline. The \"humanization gate\" scores residual AI-tell pattern counts and a change rate, then assigns a letter grade (A/B/C/D). Grade is derived fro",
  "meaning_verification": "none enforced mechanically. The output object's `rules` array states \"의미, 수치, 고유명사, 인용은 보존\" (preserve meaning, numbers, proper nouns, citations) as a declared principle, but no cosine similarity, NLI ",
  "fact_preservation": "none mechanically enforced. Numbers and citations are not byte-exact locked or skipped by the regex replacer. The stated policy (\"수치 보존\") is",
  "mechanism": "Two-stage Korean-language rule-based post-processor applied after Claude generates a draft, then a graded quality gate. Stage 1 (`humanizeDraft` in `src/core/humanization.js`): 13 named regex patterns (A-1 through J-1) targeting Korean-specific AI tells — translationese constructs (~에 대해, ~을 통해, 에 있어서), passive voice (에 의해), AI signature phrases (결"
 },
 {
  "name": "github-trending-humanize-korean (costrict-plugins-repo mirror)",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — no AI-text detector (GPTZero, Originality, local ensemble) is in the loop. The diagnostician agent identifies Korean surface patterns, not a classifier score. There is no score→rewrite→re-score c",
  "meaning_verification": "LLM-as-judge: the humanize-finalizer.md agent compares original vs. rewritten text on 15 dimensions. Additionally a change-rate gate blocks outputs where token modification exceeds 30% (warning) or 50",
  "fact_preservation": "Claimed in prompt instructions — \"Facts, numbers, proper nouns, direct quotes preserved 100%\" — but enforced only by LLM instruction, not by",
  "mechanism": "Korean-specific rule-based rewriter delivered as a Claude Code / Gemini CLI skill. The repo contains: (1) a quantitative pre-scoring shim that computes comma density, connector frequency, and lexical diversity to emit a deterministic route_hint (light/standard/heavy); (2) LLM agent prompt files — agents/humanize-diagnostician.md identifies 3-6 domi"
 },
 {
  "name": "arzumanoff/humanizer-ru-pro",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no. The scanner produces an editorial score but there is no rewrite→re-score→iterate cycle anywhere in the codebase. The SKILL.md workflow is single-pass: audit facts, rewrite, verify fact-lock by ins",
  "meaning_verification": "none mechanically. SKILL.md instructs the LLM to \"compare the result against the fact-lock\" (step 8 of 9) and never invent facts, but this is a prompt-level directive with no cosine similarity, NLI, s",
  "fact_preservation": "Prompt-only fact-lock: SKILL.md lists names, dates, sums, numbers, units, citations, document names, commands, filenames, and causal relatio",
  "mechanism": "Two-layer system. The core is SKILL.md — a structured system prompt that instructs a Codex-compatible AI agent to act as a Russian-language editor. It defines nine rewrite modes (audit, light, neutral, conversational, business, technical, legal-safe, marketing, voice-match) and a nine-step workflow. The LLM does all text transformation; the skill f"
 },
 {
  "name": "dappvibe/humanizer-ru",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration. The prompt runs once and outputs the rewritten text.",
  "meaning_verification": "none automated. SKILL.md tells the LLM to self-check \"does it sound natural aloud?\" and \"is the meaning preserved?\" before outputting, but this is an unverified LLM self-assertion, not a mechanical ga",
  "fact_preservation": "none. No mention of preserving numbers, citations, or quoted text anywhere in SKILL.md or README.",
  "mechanism": "Single-pass LLM prompt. The entire repo is two files: README.md and SKILL.md. SKILL.md is a detailed system-prompt delivered to any LLM (Claude Code natively via /humanizer-ru, or pasted into any other model's system prompt). It instructs the model to scan for 21 labelled Russian-language AI-tell patterns and rewrite them in one pass. No external c"
 },
 {
  "name": "humanizer-ru (yasikvlad/humanizer-ru)",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — single-pass rewrite with no scoring step at any point. The skill runs once and outputs the result; there is no iteration, no re-scoring, no feedback loop.",
  "meaning_verification": "Instructional only. The process checklist in SKILL.md asks Claude to verify \"Сохранён ли смысл?\" (is meaning preserved?) as a self-check, but there is no automated gate: no NLI entailment, no cosine s",
  "fact_preservation": "none — no byte-exact locking, no numeral retention check, no citation protection. The skill actually instructs Claude to replace vague text ",
  "mechanism": "A Claude Code skill — pure prompt/instruction set, no executable code. The repo contains only README.md and SKILL.md. When invoked via /humanizer-ru in Claude Code, the 25 KB SKILL.md is loaded as a system instruction and Claude performs a single-pass LLM rewrite of Russian text. The instruction set defines 21 named AI-tell patterns organized into "
 },
 {
  "name": "ForgePlan/slopocop-humanizer-ru",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the scanner is a manual diagnostic tool, not a closed feedback loop. The SKILL.md describes it as \"the machine half of the audit\" and instructs users to run scan.py before and after rewriting to ",
  "meaning_verification": "none automated. The SKILL.md contains a \"Fact-lock\" (Факт-замок) principle that instructs Claude to preserve all numbers, dates, names, percentages, and citations verbatim and prohibits inventing new ",
  "fact_preservation": "Fact-lock principle in SKILL.md: prompt-level instruction to preserve all source numerals, dates, proper names, and percentages without dist",
  "mechanism": "Two-layer architecture. Primary layer: a 51 KB Claude Code skill (SKILL.md) that instructs Claude to act as a Russian-language editor applying (1) 20 HARD BANS — absolute-removal phrases matched by regex (defined in scripts/humanizer_metrics/markers.py, e.g. \"не просто X, а Y\", \"данный/данная\", em-dash, \"в современном мире\"); (2) 54 catalogued AI-t"
 },
 {
  "name": "humanizer-ru-uk",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — the README explicitly disclaims detector evasion (\"Если вы ищете «обход детекторов» — этот скилл не для вас\"). No scoring loop of any kind. The offline scanner (audit_text.py) is an editorial pat",
  "meaning_verification": "Partial/editorial only. Before rewriting, the LLM is instructed to build a fact-lock pinning numbers, dates, names, URLs, quotes, confidence levels, and explicitly stated first-person experience. Thes",
  "fact_preservation": "Yes — explicit. The fact-lock step pins numbers, dates, proper names, URLs, citations, conditions, and stated confidence modifiers before an",
  "mechanism": "Two-layer design. The primary layer is SKILL.md — a YAML-frontmatter agent skill file loaded by Claude Code, OpenClaw, or similar agents. It instructs the LLM through a six-step editorial procedure: (1) build an internal fact-lock of numbers/dates/names/quotes/confidence levels, (2) detect language and platform, (3) scan patterns from three referen"
 },
 {
  "name": "Rasteo123/Humanizer_ru",
  "stars": 0,
  "category": "prompt-guide",
  "detector_in_loop": "no — there is no detector, no scoring step, and no iteration loop of any kind. The skill produces one output and stops.",
  "meaning_verification": "none",
  "fact_preservation": "none — no mention of protecting numbers, citations, or quotes anywhere in the five files.",
  "mechanism": "A Claude-skill prompt collection (five markdown files, no executable code). SKILL.md defines the skill metadata and core writing principles. The remaining files — ai-markers.md, anti-patterns.md, good-patterns.md, examples.md — are reference catalogues that the LLM reads as context. The technique is rule-based stylistic substitution: replace deverb"
 }
]
